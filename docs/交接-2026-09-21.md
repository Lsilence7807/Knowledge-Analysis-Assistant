# 交接文档 · 2026-09-21（第十三次交接：设计层三连改——框架化 v3.0 + 骨架整改 v3.1，代码零改动；下一段开 F1）

给下一个会话用；同一天再交接就地改写本文件，旧版本在 git 历史里。**本次交接的目的：新会话直接开 F1 后端骨架**（`app/` 搬成 `backend/app/` 并按 FastAPI 官方模板分层）。排期口径不变：网页端优先（F1→F2→F3…），P12 桌面打包留空。
状态：MVP 七段 + P6 + P7 + P8 + P13 + P15 + P17 + P21 全部完成；本轮**零代码改动**，动的是设计文档与派生说明；工作区干净、`main` 与远端同步；基线 `pytest tests -q` → **164 passed**（1 warning），`ruff check` + `ruff format --check` 干净。

## 1. 已完成阶段与提交号

- 历史号段（P0~P21、各轮补丁、并行三窗口、约束层审计）见 `docs/交接-2026-09-20.md` §1，不复述。
- 本次三段（全部 docs-only）：
  - `107c112` **v3.0 框架化**：设计文档加 §11 框架选型总表（模块 → 框架）、§5「F 段」、ADR-25~38（并废止 ADR-2/8/15/17/18/19/23）、§9 依赖按 core/frameworks/dev 分组、§3 与 §4.2/4.5/4.6/4.8/§7 加实现落点、§6 加「框架优先」；派生文档同步（README、`功能介绍与技术栈.md`、`最小可用集.md`、`系统测试清单.md`、`索引.md`、`代码台账.md`、`requirements.txt` 头）。
  - `2d0450f` **v3.1 骨架整改**：§4.1 目录整包换成 `backend/app/{api,core,models,schemas,services,providers}` + `frontend/`（React + Vite + shadcn/ui）+ `deploy/`（多阶段镜像 + compose）+ `tests/` 留仓库根；§5 的 F 段重排为 **F1 后端骨架 → F2 前端骨架 → F3~F9 能力层 → F10 部署骨架**；§6 加规则 8「骨架不许手搭」；ADR-39~43；§9 加前端 Node 依赖清单；§10 加 `FRONTEND_DIST` / `VITE_API_BASE` / 前端运行形态与质量门；§11 补 4 行骨架映射；`AGENTS.md` §1 加「落地前/落地后」路径口径；`.github/copilot-instructions.md` 修两处失效文档名。
  - 收尾（本文件所在提交）：体检修掉三处真问题——§3 分层还写着「自研单页 / 可选 Chainlit 壳」、ADR-37 与 v3.1 冲突、README 把 v3.1 目标形态当成现状；台账补第十九轮与 K-039。
- 基线证据：`pytest tests -q` → `164 passed, 1 warning in 63.75s`；`ruff check app tests` → `All checks passed!`；`ruff format --check app tests` → `37 files already formatted`。

## 2. 未完成、已知损坏与坑

- **最重要的一条（K-039）：文档写的是 v3.1 目标，代码还是老样子**——`app/` 仍平铺在仓库根（`app/main.py` 554 行 / 23 路由 / 0 `APIRouter` / 0 `Depends`），前端仍是手写单页 `web/index.html`（550 行）；`backend/`、`frontend/`、`deploy/Dockerfile`、`docker-compose.yml`、`.pre-commit-config.yaml` 都还不存在。F1/F2/F10 落地后自动对齐。
- **框架一个都没装**：LiteLLM / LangGraph / LlamaIndex / LanceDB / Langfuse / deepeval / huey / Docling / dlt 全在文档里，`requirements.txt` 与 `requirements.lock` 未动（刻意「一段装一组」）。
- 未闭环 K 支线：K-025 → F3、K-012 与 K-034 → F4、K-032 → F5、K-010/K-011 → F7、K-031/K-033/K-035 → F8；K-029/K-030/K-036/K-037 是设计取舍或留档；K-038 已解决。
- 本机坑：`apply_patch` 不可用（用 Python 写文件 + `newline=""`，否则 LF 变 CRLF）；PowerShell 里 `curl.exe -d "{\"k\":\"v\"}"` 会拆坏 body（用单引号包）；看中文先 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`，跑 `python -c` 打印中文还要 `$env:PYTHONIOENCODING="utf-8"`（否则 GBK 报 UnicodeEncodeError）；PowerShell here-string 拼接多个片段时，段尾与段首会粘成一行（拼前先补换行）；测试必须在真实文件系统里跑（K-017：沙箱内 `tmp_path` 不可写 → 40+ ERROR）；`config/local.json` 有密钥，gitignore 挡住。
- 并行三窗口的 worktree 仍在 `%USERPROFILE%\.codex\worktrees\`（分支已合，要清就 `git worktree remove <路径>`）。
- gh 认证：pwsh profile 已桥接 PATH 与 `GH_TOKEN`，`gh run list` 直接可用；上次 CI run 35514338520 `success`。
- 环境事实（仓库路径、代理、质量门唯一配置源、启动方式）见 `docs/交接-2026-09-20.md` §5，仍然有效；v3.1 唯一新增：**前端构建需要 Node**，构建产物 `web/dist` 进 gitignore、不进仓库但进交付包。

## 3. 下一步第一件事

**开 F1 后端骨架**（权威施工单：`docs/系统总体设计.md` §5 的 F1 行）。

改动范围：`git mv app backend/app`；新建 `backend/app/api/{__init__.py,deps.py,routes/*.py}`、`backend/app/core/{config,security,db,exec}.py`、`backend/app/models/`、`backend/app/schemas/`、`backend/app/services/`（原 `agent insight nlu cache memory metrics sandbox stream jobs report relations quota trace evalharness registry tools`）；`backend/app/main.py` 改 `create_app()` 工厂。连带：`pyproject.toml` 的 `pythonpath=["backend"]`、`启动.cmd`（`uvicorn app.main:app --app-dir backend`）、`.github/workflows/ci.yml`（ruff 目标改 `backend/app`）、`AGENTS.md` §1（把「落地前/落地后」那段换成实际路径）、`.github/copilot-instructions.md`。

要点：路由只声明 `Depends`（`deps.py` 提供 `get_settings` / `get_session` / `get_current_user` / `require_capability` / `get_dataset`）；每个路由文件挂响应模型（OpenAPI 完整，F2 的 TS 客户端靠它生成）；`main.py` 只做装配（目标 < 100 行）；新增一条断言 `/openapi.json` 路径集合与改造前逐条一致的测试。

```
git mv app backend/app
& .\.venv\Scripts\python.exe -m pytest tests -q                       # 必须仍 164 passed（命令与断言一个字不改）
& .\.venv\Scripts\python.exe -m ruff check backend/app tests
& .\.venv\Scripts\python.exe -m ruff format --check backend/app tests
& .\.venv\Scripts\python.exe -m uvicorn app.main:app --app-dir backend --port 8017   # 起得来、页面照旧
```

提交格式 `F1 后端骨架：验收 pytest tests -q 164 passed + /openapi.json 路径集合不变`。之后 F2 前端骨架（`npm create vite@latest frontend -- --template react-ts` + Tailwind + `npx shadcn@latest init`）→ F3 LiteLLM（顺带做完 P9、清 K-025）→ F4 LangGraph（顺带 P10、清 K-012/K-034）→ F5 LlamaIndex + LanceDB（顺带 P14、缓解 K-032）→ F6 数据层（SQLAlchemy + Alembic + pydantic-settings）→ F7 守卫与认证（顺带 P11）→ F8 观测与评测（顺带 P16）→ F9 作业与文档（顺带 P18/P19）→ F10 部署骨架；P12/P20/P22 按原阶段走，P23 用 dlt + APScheduler，P24 用 LiteLLM budget。

## 4. 需要人拍板的问题

- **F1 / F2 顺序**：默认 F1 先（后端搬家 + 分层，前端不动，风险最低）；若想先看见新前端，可先 F2（但要同时改 `/` 的静态托管与 P5 手工项口径）。
- **F6 能否与 F1/F2 并行开窗**：三者文件基本不重叠，但 F6 依赖 F1 定下的 `core/db.py` 位置约定；要并行就照 `docs/并行开发.md` 先写分工表。
- **前端组件范围**：默认 shadcn/ui + TanStack + `echarts-for-react`；也可以直接上 `assistant-ui`（聊天壳更现成，视觉可控性略低）。
- 备份目录 `%USERPROFILE%\.agents\_backup-2026-09-20\` 留还是删：默认留到下次改那些文件时再清。
- 桌面 exe（P12）是否提前：默认不改计划（不做代码签名会有 SmartScreen 警告；引入框架后包体预计 200–400MB）。

## 5. 建议技能（全局补缺，仓库流程未规定这块）

下一会话开工前：读 `docs/索引.md` → `docs/代码台账.md` → 本文件 → `docs/系统总体设计.md` §5 的 F1 行。施工中用 `verification-before-completion`（声称完成前跑真实命令）；收尾时用 `session-protocol`；全程 `ponytail`（少写代码）。F1 属「改契约（路径）」类改动，动手前先读 §4.1 与 §6 规则 8。

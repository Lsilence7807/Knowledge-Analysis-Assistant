# 交接文档 · 2026-09-21（第十四次交接：F1~F6 框架化六段全部落地，下一段开 F7）

给下一个会话用；同一天再交接就地改写本文件，旧版本在 git 历史里。**本次交接的目的：新会话直接开 F7 守卫与认证**（sqlglot 解析树守卫 + RestrictedPython 编译期守卫 + itsdangerous 签名 cookie + slowapi 限流，兼 P11）。排期口径不变：网页前后端交付，桌面端 P12 已取消；**默认单窗口串行施工**（只在用户明确说「多开」时才按 `docs/并行开发.md` 开窗）。
状态：既有功能阶段（MVP 七段 + P6/P7/P8/P13/P15/P17/P21）行为未变，**F1~F6 六段框架化全部完成并推送**；工作区干净、`main` 与远端同步（最新 `9018f28`）；基线 `pytest tests -q -p no:warnings` → **188 passed**（103.91s），`ruff check backend/app tests` → `All checks passed!`，`ruff format --check backend/app tests` → 70 files already formatted。

## 1. 已完成阶段与提交号

- 历史号段（P0~P21、各轮补丁、并行三窗口、约束层审计、v3.0/v3.1 设计定稿）见 `docs/交接-2026-09-20.md` §1 与 §5，不复述。
- 本轮 F 段（按提交顺序）：
  - `2831221` + 合并轮 `a533f05` + 收尾 `6846837` **F1 后端骨架**：`git mv app backend/app`，FastAPI 官方模板分层（`api/routes/*`、`core/`、`services/`、`schemas/`、`providers/`）+ `create_app()`；`app/models.py` → `services/llm_models.py`；新增 `tests/test_f1_skeleton.py` 钉 `/openapi.json` 路由表（165 passed）。
  - `0689448` + `9c6a075` + `0b47be4` **F2a 前端工程**：`frontend/`（Vite + React + TS + Tailwind v4 + shadcn/ui + assistant-ui 聊天壳 + 顶部导航栏 + 8 页懒加载路由 + `apiClient`/`stream`/`api-types`），tsc/lint/build 零错误，K-043 分包。
  - `9efe4a1` **F2b 前端切换**：`create_app` 挂 `web/dist` + SPA 回退，`启动.cmd` 缺 dist 自动构建，老单页退休（有/无 dist 各 165 passed）。
  - `be782d0`、`4357174`（docs）：桌面端 P12 整体取消 + README/锁文件清残留。
  - `eec2631` **F3 模型层换 LiteLLM**：`litellm.Router` 管模型组/回退/重试，`LLM_BACKEND=direct` 回落原 OpenAI 客户端，`_complete(messages, model)` 两参签名保留；`tests/test_p9_models.py` 7 例；**K-025 关闭**。
  - `aee75c9` **F4 Agent 与记忆换 LangGraph**：`services/agent.py` 换 `StateGraph`（规划/工具/条件边，`recursion_limit = 预算 × 2 + 2`），新建 `services/memory.py`（SqliteSaver + sessions/turns）、`services/cache.py`、`api/routes/sessions.py`（5 条新路由）；`tests/test_p10_memory.py` 7 例；**K-012/K-034 关闭**。
  - `43f9e28` **F5 检索与缓存换 LlamaIndex + LanceDB**：新建 `core/vectors.py`，`llm.embed()` + `llm_models.embedding_profile()`，`providers/kb.py` 走向量召回 + FTS5 融合，`cache.py` 加语义命中；`tests/test_p14_semantic.py` 5 例。
  - `9018f28` **F6 数据层换 SQLAlchemy + Alembic + pydantic-settings**：`backend/app/models/`（10 表一表一类）+ `backend/app/alembic/`（基线 DDL 与旧 `store.DDL` 逐字一致、老库幂等）+ `core/db.{engine,session,migrate}`，`core/config.py` 换 `Settings(BaseSettings)`（常量名与默认值一个没变），`services/{store,memory,cache}.py` 与 `api/deps.get_session` 全迁 ORM；`tests/test_f6_data.py` 4 例；**K-044/K-045 新记**。
- 基线证据（F6 提交时）：`pytest tests -q -p no:warnings` → `188 passed in 103.91s`；`ruff check backend/app tests` → `All checks passed!`；`ruff format --check backend/app tests` → `70 files already formatted`；`pip check` → 无冲突。真机手工项：8017 起服务，`/health` 200、首页是 F2 前端、老库（240 行 `demo_sales.csv`）迁移后照常列出。

## 2. 未完成、已知损坏与坑

- **F 段还剩四段没开工**（施工单在 `docs/系统总体设计.md` §5）：F7 守卫与认证（本段下一步）、F8 观测与评测、F9 作业与文档、F10 部署骨架。`deploy/`、`docker-compose.yml`、`.pre-commit-config.yaml` 仍不存在；F8/F9 要新建的文件（`services/trace.py`、`services/evalharness.py`、`services/jobs.py`、`services/report.py`、`providers/docs.py`、`api/routes/{trace,evals,jobs,exports}.py`、`evals/`）同样还不存在——它们对应 P16/P18/P19/P23/P24，属未开工阶段。
- 两处 §11 表里写「F3 起 / F7」但还没落的框架：**LangChain `@tool` + `langchain-mcp-adapters`**（`services/tools.py`，没落，见第 4 节拍板项）、**sse-starlette**（`services/stream.py`，随 F7）。
- 未闭环 K 支线：K-010/K-011 → F7；K-031/K-033/K-035 → F8；K-041（response_model 只保留原有的两个，其余 `-> dict`）待定；K-032 由 F5 语义层缓解，短词仍走 `LIKE` 全表扫；K-029/K-030/K-036/K-037 是设计取舍或留档。
- **本机坑（照着踩过一次就别踩第二次）**：
  - `apply_patch` 在本机不可用 → 改文件一律用 Python 脚本（`read_text(encoding="utf-8")` → `replace` → `write_text(..., newline="\n")`，仓库文件是 LF）。
  - PowerShell here-string 写文件会带 BOM：写完判 `raw.startswith(b"\xef\xbb\xbf")` 剥掉；跑 python 前 `$env:PYTHONIOENCODING="utf-8"`。
  - `backend/alembic.ini` **必须保持纯 ASCII**（中文会被 gbk 解码读挂）。
  - `core/exec.py` 保留自研 `ThreadPoolExecutor`（K-044）：换 anyio/starlette 线程门户后，本机沙箱里整段 pytest 挂住（不返回，不是超时）。挂住的进程用 `Get-CimInstance Win32_Process -Filter "Name='python.exe'" | Stop-Process -Force` 清。
  - 装框架依赖后先 `pip check` 并跑 `tests/test_p8_mcp.py`：`llama-index` 会传递装进 `mcp==1.30.0` 顶掉仓库要的 `mcp==2.2.0`（K-045）。
  - LanceDB 配 llama-index 时 `delete_nodes()` 生成的谓词不带引号（上游 bug）→ 用 `core/vectors.delete_ids()`。
  - 测试必须在真实文件系统里跑（K-017）；`web/dist` 是构建产物（gitignore），改了 `frontend/` 要 `npm --prefix frontend run build` 页面才更新；后端测试用 `.venv\Scripts\python.exe`，cwd 无所谓（路径锚在 `core/config.BASE_DIR`）。
  - `config/local.json` 里有真密钥（deepseek），gitignore 挡住，别提交。
- 服务端口约定：`启动.cmd` 固定 **8017**；本轮手工验证也是 8017（验证完已停）。

## 3. 下一步第一件事

**开 F7 守卫与认证（兼 P11）**，权威施工单：`docs/系统总体设计.md` §5 的 F7 行 + §4.3/§7 的安全边界 + §11 表里 `app/db.py`/`app/sandbox.py`/`app/auth.py`/`app/stream.py` 四行。

改动范围：`backend/app/core/db.py`（`_guard` 换 sqlglot 解析树判定，`_mask_literals` 那套自研正则下岗）、新建 `backend/app/core/security.py`（itsdangerous 签名 cookie + pbkdf2；slowapi 限流；`AUTH_DISABLED=true` 本地可跳过）、`backend/app/services/sandbox.py`（AST 检查换 RestrictedPython 编译期守卫；子进程 / 超时 / 临时目录三件事不动）、`backend/app/services/stream.py`（手写分帧换 sse-starlette，帧契约不变）、新建 `tests/test_p11_auth.py`；顺带清 K-010/K-011。

```
cd C:\Users\14778\projects\Knowledge-Analysis-Assistant
& .\.venv\Scripts\python.exe -m pytest tests/test_p2_sql.py tests/test_p15_sandbox.py tests/test_p11_auth.py -q   # 本段验收（新文件先建）
& .\.venv\Scripts\python.exe -m pytest tests -q -p no:warnings                                                   # 198+ 例，必须全绿
& .\.venv\Scripts\python.exe -m ruff check backend/app tests
& .\.venv\Scripts\python.exe -m ruff format --check backend/app tests
& .\.venv\Scripts\python.exe -m uvicorn app.main:app --app-dir backend --port 8017                                # 手工项：401/429 边界 + 页面照旧
```

注意：F7 会动 `core/db.py` 的守卫与 `/openapi.json` 无关的对外行为——**断言一个字不许改**（P2 的 22 例就是守卫的契约）；新端点若真的要加（如 `/auth/*`），必须同步 `tests/test_f1_skeleton.py` 的 `FROZEN_ROUTES`（F4 漏过一次，F6 补齐）。提交格式：`F7 守卫与认证换 sqlglot + RestrictedPython + itsdangerous + slowapi：…（验收 test_p11_auth N passed，全量 N passed，ruff 干净，K-010/K-011 关闭）`。

之后：F8 观测与评测（Langfuse + deepeval/Ragas + LiteLLM guardrail，兼 P16，清 K-031/K-033/K-035）→ F9 作业与文档（huey + APScheduler + Docling/MarkItDown + docxtpl，兼 P18/P19）→ F10 部署骨架（多阶段镜像 + compose + Caddy + pre-commit + CI 前端作业）。P20/P22 按原阶段走，P23 用 dlt + APScheduler，P24 用 LiteLLM budget。

## 4. 需要人拍板的问题

- **LangChain `@tool` + `langchain-mcp-adapters` 放哪段**（§11 表写「F3 起」，至今没落）：建议**并入 F7**（顺带清 K-023 遗留的口径）或在 F7 之后单开一小段「F7.5」——要动 `services/tools.py` 与 `providers/mcp_client.py`，属改契约边界的活。
- **sse-starlette 是否随 F7 一起做**：建议一起（`services/stream.py` 只是换实现，`tests/test_p17_stream.py` 是帧契约的守卫）。
- **K-041（response_model 补不补）**：补全会让 FastAPI 按模型过滤响应载荷，与「F 段不动响应载荷」冲突；建议维持现状，只在前端真正用到的端点补。
- 备份目录 `%USERPROFILE%\.agents\_backup-2026-09-20\` 留还是删（仍在）：默认留到下次真去改那些文件时再清。
- 已定、不再问：桌面端 P12 取消；前端框架选型（assistant-ui 聊天壳 + shadcn/ui + TanStack + echarts-for-react，用户口径「能用框架的就用」）；单窗口串行施工。

## 5. 建议技能

下一会话开工前：读 `docs/索引.md` → `docs/代码台账.md` → 本文件 → `docs/系统总体设计.md` 的 §5 F7 行与 §7 安全边界。施工中用 `verification-before-completion`（声称完成前跑真实命令）；收尾时用 `session-protocol`；全程 `ponytail`（少写代码）+ benjamin-plus（少花步骤）。F7 属「改已冻结守卫契约的实现」类改动，动手前先读 §6 阶段隔离（只允许新建文件、注册表加一行、编排层插调用）与 §4.3 的错误码口径。

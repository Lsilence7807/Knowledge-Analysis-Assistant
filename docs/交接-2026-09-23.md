# 交接文档 · 2026-09-23（第十八次交接：P5.3 视觉风格 v2 收尾 + 本机认证启动 + 文档对账，下一段开 P20）

给下一个会话用；同一天再交接就地改写本文件，旧版本在 git 历史里。**本次交接的目的：新会话直接开 P20 日常使用功能**（口径与上一份交接相同，本轮没动它）。排期口径不变：网页前后端交付，桌面端 P12 已取消；**默认单窗口串行施工**（只在用户明确说「多开」时才按 `docs/并行开发.md` 开窗）。

状态：本轮只动**前端视觉层 + 文档 + 仓库外的简历材料**，后端代码与 §4 契约零改动。基线（2026-09-23 收尾轮实测）：`pytest tests -q -p no:warnings` → **250 passed, 7 skipped**；浏览器套件 `KAA_E2E=1 pytest tests/test_ui_browser.py -q` → **7 passed**；`ruff check backend/app tests` 与 `ruff format --check` 干净；`pip check` 无冲突；前端 `npm run lint` / `npx tsc --noEmit` / `npm run build` 干净并已重建 `web/dist`（结果卡页签已是横排）。

## 1. 已完成阶段与提交号

- 历史号段（P0~P21、各轮补丁、并行三窗口、约束层审计、v3.0/v3.1 设计定稿、F1~F10 十段框架化、第三十五~三十七轮）见 `docs/交接-2026-09-20.md` §1/§5、`docs/交接-2026-09-21.md` §1、`docs/交接-2026-09-22.md` §1，均不复述。
- 本轮（本次交接这一天的三段，按提交顺序）：
  - `5b8c606` docs：登记 **P5.3 视觉风格 v2**（§4.16 视觉风格契约 + §5 阶段行 + §4.1 插画目录 + ADR-44）。
  - `bd4bf6d` **P5.3 视觉风格 v2**：新建装饰层 `frontend/src/components/decor.tsx`（Eyebrow / Sticker / TiltNote / Blob / Illustration）+ 三张**自写 SVG 插画** `frontend/public/illustrations/{data-board,chart-buddy,welcome-key}.svg` + 卡片 / 按钮 / 输入类把 3px 描边与硬阴影收进默认类 + 首页 hero 与登录页贴纸 + `tests/test_p5_style.py`（4 例，本轮加到 5 例）。
  - `e2ba64c` docs：新增 `启动-需登录.cmd` 与 `config/auth.local.json`（gitignore 挡住）——**让本机 8017 像公网那样需要登录**；问题总表补口令生成命令与两条实测错路。
  - 「891a92a」 docs：**README 改为「完工形态」介绍**（用户 2026-09-23 口径：README 写整个系统，不写现阶段进度）——补 P20/P22/P23/P24 与检索重排、Rubrics 等完工能力，能力 / 安全边界 / 架构图 / 技术栈 / 性能契约全部按 §4 契约口径，另保留「路线图」一节标明哪些已落地、哪些还没开工。
- 本次（第三十九轮，收尾）：**修 K-057**（结果卡页签被撑成 332px 竖排）+ 重拍 README 两张预览图 + README 全面对账（badge / 例数 / 围栏 / 技术栈错项 / 启动说明）+ 文档数字对齐（系统测试清单、系统总体设计 §5、最小可用集、问题总表、功能介绍与技术栈）+ 桌面求职材料 `Agent岗-STAR重写-项目与工作经历.md` 重写（对齐框架化后的技术栈）。细节见 `docs/代码台账.md` 第三十九轮。

## 2. 未完成、已知损坏与坑

- **还开着的问题共 10 条**，逐条「现象 / 为什么没关 / 怎么关」见 `docs/问题总表.md` §1：4 条等人拍板或装东西（K-041 响应模型、K-048 Docker、K-052 评测基线、K-053 登出失效），6 条是刻意保留的上限（K-026 / K-029 / K-030 / K-032 / K-035 / K-044）。本轮的 K-057 已修复并入表。
- **还没测到的**（要装什么、补的时候动哪儿都写死了）见 `docs/留白-未测项.md` 四节。
- **换机器 / 换 agent 必读的坑**见 `docs/问题总表.md` §3（Windows + PowerShell 施工坑、端口约定、依赖坑、测试注意事项）。**本轮实测出来的两条新坑**：
  - **认证不要写 `config/local.json`，也不要设用户级环境变量**：`Settings` 只读环境变量（`env_file=None`、`extra="ignore"`），写本地 JSON 不生效；写成用户级环境变量更糟——会被之后每个终端与 pytest 继承，`pytest tests/test_p0_skeleton.py` 立刻红（实测 1 failed）。要开认证就双击 `启动-需登录.cmd`（PowerShell 只给本进程树设环境变量）。
  - **README 的效果图必须用真服务 + 真模型重截**：截整页前先把对话面板的 `height` 置 `auto`、滚动区 `overflow` 置 `visible`、`scrollTo(0,0)`，再按整页宽度裁；sticky 导航会盖住元素顶部，所以**不要**用元素级截图（会把第一行截掉）。
- 本机残留（不影响代码）：8017 上挂着本会话起的 uvicorn（带认证 + 全部能力开关 true，口令串在 `config/auth.local.json`，别外传）；`%TEMP%\kaa-*.py` 是临时脚本；`%TEMP%\kaa-core` 空 venv 约 400MB，本机策略拦住 `Remove-Item -Recurse`，要人工删。
- **真机评测结果不好看不是 bug**：`fidelity_rate` 达不到 §4.10 基线属真实表现，见 K-052，别为了让指标好看去改断言或 golden。
- **README 的定位是「完工形态的系统介绍」，不是实现进度表**（用户 2026-09-23 拍板）：里面的 P20/P22/P23/P24 与重排、Rubrics 是按设计文档 §4/§5 写的完工口径，**看到 README 与当前代码有差距时不要当成吹牛去「修正」**；进度与已落地文件一律看 `docs/代码台账.md`，差距对照看 README 的「路线图」一节。
- **仓库外改过一份文件**：桌面 `求职材料\Agent岗-STAR重写-项目与工作经历.md`（旧版备份为同目录 `*.bak-2026-09-20`，确认新版没问题可删）。

## 3. 下一步第一件事

**开 P20 日常使用功能**（收藏问题 + 数据集重导入与 schema drift 报告，兼前端页面）。权威施工单：`docs/系统总体设计.md` §5 的 **P20 行**（前置 P10，配合 P18）+ §4.2 端点表里的 `/saved`（GET/POST/DELETE）与 `/datasets/{id}/reimport` + §4.4 相关表 + §11 的 `ingest.py` 行（清洗规则换 **pandera** schema 随 P20）。

改动范围：新建 `tests/test_p20_utility.py`；后端加 `/saved` 与 `/datasets/{id}/reimport`（用 pandera 比对 schema，报字段增删与类型变更）；前端加对应页面与导航项（`frontend/src/routes/` + `frontend/src/router.ts`，按 §4.3 错误码口径）；新端点同步 `tests/test_f1_skeleton.py` 的 `FROZEN_ROUTES`。

```
cd C:\Users\14778\projects\Knowledge-Analysis-Assistant
& .\.venv\Scripts\python.exe -m pytest tests/test_p20_utility.py -q     # 本段验收（新文件先建）
& .\.venv\Scripts\python.exe -m ruff check backend/app tests
& .\.venv\Scripts\python.exe -m ruff format --check backend/app tests
& .\.venv\Scripts\python.exe -m pytest tests -q -p no:warnings         # 全量回归（当前 250 passed / 7 skipped）
& .\.venv\Scripts\python.exe -m uvicorn app.main:app --app-dir backend --port 8017   # 手工项：收藏与重导入页面
$env:KAA_E2E="1"; & .\.venv\Scripts\python.exe -m pytest tests/test_ui_browser.py -q # 动了前端就补跑浏览器验收
```

注意：新依赖（pandera）装完先 `pip check` + 跑 `tests/test_p8_mcp.py`，并当轮重生成 `requirements.lock`（K-045 / K-050 都栽在这）；动了前端要 `npm run lint` + `npm run build` 重建 `web/dist`。提交格式：`P20 日常使用功能：…（验收 test_p20_utility N passed，ruff 干净）`。

之后：**P22 多表关系与 join 校验**（`app/relations.py` + `/relations*` + sqlglot 校验 join）→ **P23 数据源直连**；P16 已由 F8 落地，P24 未开工。

## 4. 需要人拍板的问题

- **K-052 评测 fidelity 基线**：真机 0.6667 对基线 0.95，三选一（重定基线 / 先定 faithfulness 口径 / 只当告警）。
- **K-053 登出不失效**：签名 cookie 无服务端状态，二选一（接受无状态并写进口径 / 加口令版本或 jti + 撤销表）。
- **K-041 响应模型补不补**：建议维持现状，只在前端真正用到的端点补。
- **K-048 要装 Docker**：装好后 `docker compose up --build` 才能验镜像与反代（含 `FRAMEWORK_PROFILE=full` 那档）。
- **本轮新增：能力开关要不要给一个「默认全开」档**？KB / Skills / Sandbox / MCP / Stream / Jobs 默认全关是设计口径（§6：新增能力默认关，免得没配依赖就报错），但 README 把它们当卖点——本轮只在 README 写清「六个开关怎么开」。若想要「双击就全开」，做法是再加一个 `启动-全能力.cmd`；**属改口径，动手前先回设计文档登记**。
- 已定、不再问：桌面端 P12 取消；前端框架选型（assistant-ui + shadcn/ui + TanStack + echarts-for-react）；单窗口串行施工；LangChain `@tool` 并入 F7、sse-starlette 随 F7；不装 `langchain-mcp-adapters`（K-046）、不装 `ragas`（K-047）；阻塞执行器保留自研 25 行线程池（K-044）。
- 可选清理（等你一句话）：8017 老进程、`%TEMP%\kaa-core`、`data/` 下探针产物、桌面简历材料的 `.bak-2026-09-20` 备份。

## 5. 建议技能

开新会话读法：`docs/索引.md` → `docs/代码台账.md` → 本文件 → `docs/问题总表.md`（问题视角）→ `docs/系统总体设计.md` 的 §5 P20 行与 §4.2/§4.4 相关契约。动了视觉层再读 §4.16（视觉风格契约：所有颜色 / 描边 / 硬阴影只从 `frontend/src/index.css` 的 `:root` 旋钮取）。施工中：`ponytail`（少写代码）+ benjamin-plus（少花步骤）；声称完成前用 `verification-before-completion` 跑真实命令；收尾用 `session-protocol`。

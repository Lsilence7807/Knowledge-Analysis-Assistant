# 留白：还没测到的部分（补齐入口都写死在这里）

> 这份文件只记「现在测不到、以后要补」的东西。每行说清三件事：**缺口**、**卡在哪（要装/要有）**、**补的时候动哪儿**。
> 已经测到的部分见 `docs/系统测试清单.md`；本文件 2026-09-22 起随浏览器验收（`tests/test_ui_browser.py`）建立。

## 1. 要人先装东西才能测的

| 缺口 | 卡在哪 | 补的时候动哪儿 |
|---|---|---|
| 镜像能不能构建、`docker compose up --build` 能不能起来、`/health` 通不通 | 本机没装 Docker Desktop（K-048） | 装好后在仓库根跑 `docker compose up --build`，然后 `curl http://127.0.0.1:8017/health`；`deploy/Dockerfile`、`deploy/Caddyfile`、`docker-compose.yml` 已就位，预期是 app + caddy 两个服务起来 |
| `FRAMEWORK_PROFILE=full` 那个镜像（带 docling/torch） | 同上，且 full 档要下 GB 级依赖 | 装好后 `docker build --build-arg FRAMEWORK_PROFILE=full -f deploy/Dockerfile .`；core 档（`requirements.lock`，217 条）已在本机空 venv 验过 |
| Langfuse 真上报（trace 页/span 能到 Langfuse） | 要有 Langfuse 服务与 `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY`，本机没有 | 起一个本地 Langfuse（或云账号）后，设 `LANGFUSE_ENABLED=true` + 两个 key，跑一次 `/ask`，看 Langfuse 上有没有 trace；分支在 `backend/app/services/trace.py:105` |
| 真向量召回（LanceDB + 真 embedding） | 要 `EMBEDDING_API_KEY`（没配时 `kb/search` 只剩 FTS 兜底，导入时 `vectors: 0`） | 配好 key 后跑一次 `POST /kb/import`，断言返回里 `vectors > 0`，再 `POST /kb/search` 断言命中带 `score`；代码在 `backend/app/providers/kb.py` |

## 2. 装得动、但还没补用例的

| 缺口 | 现状 | 补的时候动哪儿 |
|---|---|---|
| Docling 解析分支（`DOCS_BACKEND=docling`） | docling 本机装了，但单测只走 markitdown / pypdf 兜底，docling 那条顺序没人跑 | 在 `tests/test_p19_formats.py` 里加一例：`monkeypatch.setattr(config, "DOCS_BACKEND", "docling")` 后用 `providers/docs.py` 解一份 pdf/docx；分支在 `backend/app/providers/docs.py:50` |
| `ENABLE_CACHE=false` 的关档路径 | 没有用例把它关掉跑一遍 | 在 `tests/test_p10_memory.py` 里加一例：关掉 `config.ENABLE_CACHE` 后 `/ask` 仍要出结果（缓存关了不许报错）；判断点在 `backend/app/api/routes/sessions.py:43` |
| APScheduler 定时同步的行为 | 只在启动时被拉起（`main.py` lifespan），定时干什么没有任何断言；P23 的功能本身也还没建 | P23 开工时连同功能一起补，落在 `tests/test_p23_sources.py`（新文件） |
| CI 跑浏览器验收 | 本机 7 例全绿；CI 没装 playwright（锁文件里也没有，`pytest tests` 会自动跳过整个模块） | 想挂上去就在 `.github/workflows/ci.yml` 加一个 job：`pip install -r requirements.lock playwright` + `python -m playwright install --with-deps chromium`，然后 `KAA_E2E=1 pytest tests/test_ui_browser.py -q`（要真模型密钥就得把 key 配成 secret） |

## 3. 没有 UI 入口、只能测接口的

后端接口齐全，前端只有 8 个页面（`frontend/src/router.ts`）。下面这些能力**网页上点不到**，浏览器验收覆盖不了；要做 UI 时按同一套路加：页面 + 导航项 + `tests/test_ui_browser.py` 里加一例。

| 能力 | 接口 | 页面现状 |
|---|---|---|
| 后台作业（长任务） | `POST /jobs`、`GET /jobs/{job_id}` | `frontend/src/routes/jobs.tsx` 还是 F2 的占位，文案写着「P18 未开工」——`tests/test_ui_browser.py::test_jobs_page_pins_the_missing_ui` 钉住了现状，做 UI 时删掉它 |
| 导出 md/docx/pptx | `POST /exports` | 完全没有入口 |
| 沙箱执行代码 | `POST /sandbox/run` | 没有页面 |
| 指标口径列表 | `GET /metrics/definitions` | 没有页面 |
| 会话与 agent 步骤 | `/sessions*` | 没有页面（对话只在首页内存里） |
| 工具清单 | `GET /tools` | 没有页面 |
| 成本/耗时台 | `/trace/stats`、`/trace/{task_id}` | 没有页面 |
| 手写 SQL / 描述统计 | `POST /query`、`POST /stats` | 没有页面（首页只走自然语言） |
| 基准 | `GET /bench` | 没有页面 |
| 登出 | `POST /logout` | 没有按钮；无状态 cookie 登出后不失效是 K-053，等拍板 |

## 4. 只能人工看的

- 导出的 docx/pptx 用真 Office 打开（自动化只回读了段落数与形状数）。
- 真机长跑/并发下的表现（`tests/test_p21_perf.py` 只是基准，不是压测）。
- Caddy 反代下的 SSE 表现（本机直连 uvicorn 验过；反代那条要 Docker，见第 1 节）。

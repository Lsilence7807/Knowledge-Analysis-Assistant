# 交接文档 · 2026-09-20（B 类前四项 + 质量门落地，下一步 K-009/K-018）

给下一个会话用。MVP 七段 + 台账 A 类补丁已收尾，B 类正按顺序清：`K-013`、`K-002`、`K-006`、`K-008` 已完成，下一项 `K-009`+`K-018`。每个 B 类项固定动作：先回设计文档登记契约变化，再改代码，再同步验收命令。同一天再次交接就就地改写本文件，旧版本在 git 历史里。

## 1. 已完成阶段与提交号

仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，工作区干净。

| 阶段 | 提交 | 验收 |
|---|---|---|
| P0 骨架与契约冻结 | `3383821`（补丁 `52501f0`） | test_p0_skeleton 5 passed |
| P1 数据摄入 | `1d9ab68` | test_p1_ingest 11 passed |
| P2 查询与统计层 | `89826d0` | test_p2_sql 19 passed |
| P3 自然语言转 SQL | `7e7fc82`（补丁 `c555283`、`d452841`、`b610810`） | test_p3_nlu 11 passed |
| P13 Agent 循环与工具调用 | `7e27c31` | test_p13_agent 11 passed |
| P4 洞察生成 | `bb75cb8` | test_p4_insight 8 passed |
| P5 前端闭环（MVP 最后一段） | `18b2482` | test_p5_flow 5 passed |
| P5 之后：K-019 隔离、文档/契约对齐、A 类清理、页面删除按钮 | `935211c`…`8843cea`（见 git log） | 全量 65 → 69 passed |
| B 类第一项：K-013 请求体契约 | `83d6fa7` | 全量 72 passed（+3 条 422 用例） |
| B 类第二项：K-002 守卫切分 | `41dc655` | 全量 74 passed（+2 条字面量用例） |
| 质量门落地：`pyproject.toml`（ruff + pytest）+ 全仓 ruff 整理 | 见 `git log` 最新一条提交 | `ruff check` 零告警、`ruff format --check` 无差异、全量 74 passed |
| 质量门修正 K-020 + 代码体检与文档对齐 | 见 `git log` 最新一条提交 | 全量 74 passed（汇总行恢复可见） |
| B 类第三项：K-006 每步超时 | 见 `git log` 最新一条提交 | 全量 75 passed（+1 条超时用例） |
| B 类第四项：K-008 末步行数 | 见 `git log` 最新一条提交 | 全量 75 passed（改写 1 条用例口径，用例数不变） |

全量回归（交接时点复跑）：`& .\.venv\Scripts\python.exe -m pytest tests -q` → **75 passed, 1 warning**（警告来自 Starlette 依赖）。**75 是当前基线。**

当前每段用例数（`--collect-only`）：p0 5、p1 8、p2 22、p3 12、p13 13、p4 9、p5 6（合计 75）。

## 2. 未完成、已知损坏或有坑的地方

- B 类（要先登记再改）：`K-009`+`K-018` 数字回溯与引用字段（最后一项）
- C 类（归后续阶段，现在不做）：`K-010`/`K-011` 归 P11、`K-012` 归 P10、`K-014` 归 P16/P21、`K-016` 归 P21
- 已修复（细节见台账，不再复述）：K-001、K-002、K-003、K-004、K-005、K-006、K-007、K-008、K-013、K-015、K-019。`K-009` 台账行写着「设计取舍，暂不修」，别当已修
- 坑：
  - 页面删除按钮只做过静态断言 + HTTP 手测，**没在浏览器里点过**（会弹 `confirm`）；服务在 8017，请手点一次
  - 起服务前先看端口，有旧进程先停：`Get-NetTCPConnection -LocalPort 8017 -State Listen`。踩过：旧进程占着 8017，新进程绑定失败会**悄悄退出**，健康检查还通，整轮真链路测的是旧代码
  - `K-017` 测试必须在授权（非沙箱）下跑，否则 `tmp_path` 不可写 → 40+ ERROR
  - 本机 `config/local.json` 已有页面写入的密钥（profile `local`、`deepseek-flash`），被 `.gitignore` 挡住；`data/` 有烟测残留数据集 `d_fc466e97`、`d_3216c6b3`，用页面按钮或 `DELETE /datasets/<id>` 清掉
  - P5 手工 8 条实为 7 条半：「无密钥仍显示结果表」只验到降级提示不崩（无密钥拿不到 SQL），带表降级由 `test_ask_keeps_table_when_insight_degrades` 覆盖
  - 依赖版本很新（pandas 3.0.6、duckdb 1.5.5、openai 3.16.2、fastapi 0.141.1、pydantic 2.13.5）

## 3. 下一步要做的第一件事（精确到文件与命令）

B 类最后一项 `K-009`+`K-018`（同源，一起做）：`K-009` 数字回溯只认字面数字，模型从表里算出来的汇总值（合计、比率、环比）在表里没有原文，会被判成「找不到」写进 `caveats`；`K-018` 表名/数据集 id 里的数字也被当可疑数字（真实提问里 `ds_d_xxx` 几乎必然出现，`caveats` 会冒出「2、3、466、97 找不到」）。要做对得让模型输出引用的行号或单元格，**属改 P4 契约**，台账第 31 行已登记为「暂不做」，动手前先跟用户确认这一项现在做（见第 4 节）。三步：

1. 回 `docs/最小可用集-系统设计.md` §P4 与 `docs/系统总体设计.md` 的 `Insight` 契约处登记 `findings` 加引用字段（行号或单元格），并同步 `docs/最小可用集-测试清单.md` 的 P4 反例行
2. 改代码：`app/schemas.py` 的 Insight/Finding 加字段、`app/insight.py:_unsupported_numbers` 改成按引用判（顺带跳过表名/数据集 id 里的数字），`web/index.html` 有需要就展示引用
3. 同步改 `tests/test_p4_insight.py`、`tests/test_p5_flow.py` 的夹具与断言（这两条属流程允许的「同步改验收命令」），跑绿三件套：`ruff check app tests`、`ruff format --check app tests`、`& .\.venv\Scripts\python.exe -m pytest tests -q`（基线 75 passed）

做完这一项，B 类即清空；C 类不做，按设计 §6 路线表接。

## 4. 需要人拍板的问题

- 已定：`K-008` 走 A（末步响应表按 `/query` 口径给足，回模型的上下文仍是 200 行），见台账「已拍板决策」
- 待拍板：`K-009`/`K-018` 要给 `Insight` 加引用字段（改 P4 契约，要同步改 `test_p4_insight.py` 与 `test_p5_flow.py`），排在 B 类最后，动手前先回设计文档登记；台账第 31 行现在写的是「暂不做」，要恢复成「现在做」得先确认
- 已定，不用再问：产品名跟仓库名（`Knowledge-Analysis-Assistant`，数据在 `%APPDATA%\Knowledge-Analysis-Assistant`）；README 已补怎么跑与文档地图；两个测试夹具已隔离 `config/local.json`；页面已补数据集删除按钮（P20 再换正式删除/重导入 UI）

## 5. 环境事实（换机器时需要）

- Python：`C:\Users\14778\AppData\Local\Programs\Python\Python312\python.exe`；项目虚拟环境 `.venv`；`requirements.txt` 已装齐
- 代理 `http://127.0.0.1:7897` 已写进仓库本地 git 配置；`pip install` 与下载要手动带代理
- 沙箱：命令默认被拦，已批准前缀 `.venv\Scripts\python.exe -m pytest`，**测试必须走授权运行**
- 质量门：`pyproject.toml` 是唯一配置源（ruff 行宽 120、pytest 指 tests/）；本机写法 `.venv\Scripts\python.exe -m ruff check app tests`
- 改文件用 `apply_patch`；整行匹配对超长行会认不出，认不出时改用 PowerShell 精确子串替换（踩过：只写 `+` 没有上下文行时，新行会飘到文件末尾）
- 起服务看页面：`& .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8017`，浏览器开 `http://127.0.0.1:8017/`（收尾时已停，要看页面自己起；日志 `data/uvicorn.*.log`）

## 6. 开工前必读

`AGENTS.md`（自动加载）→ `docs/索引.md` → `docs/代码台账.md` → 本次要施工的设计文档（B 类看 `docs/最小可用集-系统设计.md` §3/§5/§6/§7 与 `docs/最小可用集-测试清单.md`）。可调用技能：`tdd`、`implement`、`handoff`；`ponytail`、`benjamin-plus` 已常驻。

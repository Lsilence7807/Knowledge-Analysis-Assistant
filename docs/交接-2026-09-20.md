# 交接文档 · 2026-09-20

给下一个会话用。目标：接着做 MVP 的 P2 查询与统计层。

说明：`handoff` 技能默认把交接文档写到系统临时目录；本项目按 `AGENTS.md` 第 6 节写进仓库并提交，这样换会话、换电脑都能拿到。

## 1. 现在到哪了

- 项目：`C:\Users\14778\projects\Knowledge-Analysis-Assistant`（远端 `origin` 已配好，分支 `main`，工作区干净）
- 进度：**P0、P1 已完成并推送**，下一步 P2
- 最近提交：
  - `1d9ab68` P1 数据摄入：验收 test_p1_ingest 通过（11 passed）
  - `828de9d` docs: AGENTS.md 增加交接触发条件与流程
  - `3a50db2` chore: .vscode 配置入库
  - `3383821` P0 骨架与契约冻结：验收 test_p0_skeleton 通过（5 passed）
  - `e0add63` / `cce5e74` / `3dac607` / `e47e287` / `f13cad6` 文档基线、依赖、测试清单

## 2. 下一步第一件事（精确到文件与命令）

做 **P2 查询与统计层**，依据 `docs/最小可用集-系统设计.md` 第 4 节 P2：

- 开工前先跑前序回归：`& .\.venv\Scripts\python.exe -m pytest tests -q`
- 新建：`tests/test_p2_sql.py`
- 允许改：`app/db.py` 补 SQL 守卫、超时与行数上限；`app/main.py` 只加 `POST /query`、`POST /stats`
- 交付物：只读单条 SELECT；拒绝 DDL/DML、多语句、注释绕过、写操作 CTE；查询结果最多 5000 行并返回截断提示；查询超时后服务仍可用；增加描述统计与 IQR/z-score 异常检测
- 验收命令：`& .\.venv\Scripts\python.exe -m pytest tests/test_p2_sql.py -q`，必须 exit 0
- 反例（来自 `docs/最小可用集-测试清单.md` P2）：`DROP TABLE`、`DELETE/UPDATE/INSERT`、多语句、注释绕过、尾部逗号、写操作 CTE、不存在表、超 5000 行、故意长查询
- 全量回归：`& .\.venv\Scripts\python.exe -m pytest tests -q`

完成后按 `AGENTS.md` 第 0 节七步走：更新 `docs/代码台账.md` → 提交（消息格式 `P2 查询与统计：验收 test_p2_sql 通过`）→ 推送。

## 3. 环境事实（换机器时需要）

- Python：`C:\Users\14778\AppData\Local\Programs\Python\Python312\python.exe`（用户级安装）；项目虚拟环境 `.venv`
- 依赖：见 `requirements.txt`（已全部装好并验证可导入）
- 已装的 Windows 工具：Caddy、Inno Setup 6.7.3
- 本机代理：`http://127.0.0.1:7897`；`pip install` 与 `git push` 都要带它：
  - `git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push`
- 本会话的运行环境限制：命令执行默认被沙箱拦，需要逐条批准；写文件、跑测试、推送都会请求授权

## 4. 坑与已知问题

- `K-001`（在 `docs/代码台账.md`）：`app/db.py` 的 SQL 守卫目前只拦多语句与非 SELECT，注释绕过、超时、行数上限留到 P2 补，不要以为已经安全。
- P1 已验证：全量测试 `11 passed, 1 warning`；唯一警告来自 Starlette 依赖的弃用提示，不是本项目代码错误。
- 依赖版本很新：pandas 3.0.6、openai 3.16.2、mcp 2.2.0、anthropic 1.7.0、fastapi 0.141.1。写 P1 清洗和 P3 模型调用前先验一次 API 形态，别照抄旧版写法。
- FastAPI 的 `on_event` 已废弃，本项目用 `lifespan`。
- `.vscode/` 已入库（曾被 .gitignore 误挡，已修）。
- 前端 ECharts 走 CDN，**桌面离线端会失效**，P5 建前端时要落到本地 `web/vendor/`。
- 仓库 README 标题是「内部知识分析助手」，文档里统一用「AI 数据分析助手」，尚未统一——等用户拍板。

## 5. 待用户拍板的问题

1. README 标题与文档标题统一成哪个名字？
2. P3 需要模型 API Key（`LLM_API_KEY`，OpenAI 兼容端点的 base_url 在 `config/models.json`）——用哪家？
3. 桌面端离线资源（ECharts）什么时候本地化：P5 顺手做，还是留到 P12？

## 6. 建议下一个 agent 调用的技能

- `tdd`：先为 P2 SQL 守卫与路由补验收测试，再实现
- `implement`：按本交接文档和 P2 设计范围施工
- `handoff`：再次交接时用它压缩对话
- 已常驻、不必显式调用：`ponytail`（少写代码）、`benjamin-plus`（少花步骤）

## 7. 必须先读的文件

1. `AGENTS.md`（自动加载，含阶段固定动作、注释规范、台账、提交、阶段隔离、交接规则）
2. `docs/索引.md`（文档地图）
3. `docs/最小可用集-系统设计.md`（当前施工依据）
4. `docs/代码台账.md`（已完成文件与已知问题）
5. `docs/最小可用集-测试清单.md`（成功、反例、手工三层测试项）

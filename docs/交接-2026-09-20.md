# 交接文档 · 2026-09-20（A 类清理 + 页面删除按钮完成后改写）

给下一个会话用。目标：MVP 七段 + 台账 A 类补丁全部收尾，接下来按 B 类顺序改契约（每个 B 类项都要先回设计文档登记、再同步改验收命令）。

说明：`handoff` 技能默认把交接文档写到系统临时目录；本项目按 `AGENTS.md` 第 6 节写进仓库并提交，换会话、换电脑都能拿到。同一天再次交接就就地改写本文件，旧版本在 git 历史里。

## 1. 已完成阶段与对应提交号

仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，工作区干净。

| 阶段 | 提交 | 验收 |
|---|---|---|
| P0 骨架与契约冻结 | `3383821`、补丁 `52501f0` | `tests/test_p0_skeleton.py` 5 passed |
| P1 数据摄入 | `1d9ab68` | `tests/test_p1_ingest.py` 11 passed |
| P2 查询与统计层 | `89826d0` | `tests/test_p2_sql.py` 19 passed |
| P3 自然语言转 SQL | `7e7fc82`，补丁 `c555283`、`d452841`、`b610810` | `tests/test_p3_nlu.py` 11 passed |
| P13 Agent 循环与工具调用 | `7e27c31` | `tests/test_p13_agent.py` 11 passed |
| P4 洞察生成 | `bb75cb8` | `tests/test_p4_insight.py` 8 passed |
| P5 前端闭环（MVP 最后一段） | `18b2482` | `tests/test_p5_flow.py` 5 passed |
| P5 后补：测试隔离（K-019） | `935211c` | 全量仍 65 passed，耗时 18.5s → 9.9s |
| P5 收尾：文档对齐 | `3279fb7` | docs only：6 处漂移 |
| P5 收尾：README + p13 夹具隔离 | `d8d2b5f` | 全量 65 passed |
| P5 收尾：契约层对齐 | `daadb29` | docs only |
| P5 收尾：交接改写 | `34c9add` | docs only |
| A 类清理：K-005、K-015 | 见 `git log` | 全量 68 passed（新增 3 条用例） |
| 文档对齐：产品名 + A 类登记 | 见 `git log` | docs only |
| 页面删除数据集按钮 | 见 `git log` | 全量 69 passed；真实链路复验（真实模型 / tasks 落库 / DELETE 200）全绿 |

全量回归（交接时点复跑）：`& .\.venv\Scripts\python.exe -m pytest tests -q` → **69 passed, 1 warning in 11.15s**（警告来自 Starlette 依赖）。

P5 落地的形状（后续阶段要接着用）：

- `web/index.html`：单文件原生 JS，无构建链；页面分 6 块——模型配置 / 数据 / 提问（含 SQL 折叠区与降级提示条）/ 步骤 / 结论卡片（caveats 单列）/ 结果（图表切换 + 表格）
- 静态挂载：`app/main.py` 末尾 `app.mount("/", StaticFiles(directory=config.BASE_DIR / "web", html=True))`，**必须排在全部 API 路由之后**，否则 `Mount("/")` 会抢走 `/health`、`/ask` 等
- `GET /settings/models`：只回 `id / provider / base_url / model / has_key`，密钥明文不出网关
- `PUT /settings/models`：写 `config/local.json` 的 `models` + `api_keys`，并把 `default` 设成该 profile —— **保存即生效**，不用重启；传空 `api_key` 等于清掉
- `app/config.py:save_settings(**patch)`：合并写盘（字典按键合并、带 id 的列表按 id 合并），其余模块只读不写
- ECharts 5.6.1 在 `web/vendor/echarts.min.js`（1.0MB，Apache-2.0）；`typeof echarts === "undefined"` 时只显示提示与表格，不报错
- 真实模型链路已出网验证：单跳、多跳（2 步 `run_sql`）、insight 全部跑通，`K-004`、`K-007` 已转「已修复」

A 类补丁（本次）落地的形状：

- `app/store.py:insert_task()`：`/ask` 两条路径（agent 与 P3 单跳）共用一次调用，落 `tasks`（`id/dataset_id/session_id/kind=ask/question/sql/status/degraded_json/error`）；`status` 由 `degraded` 决定，`error` **只在降级时**写 message（成功时 message 是模型收尾话术，混进 `error` 会被测试当场抓住）；写失败只记日志，不拖垮提问
- `DELETE /datasets/{dataset_id}`：DuckDB 表 + 原始上传文件 + 元数据 + 该数据集的 `tasks`/`agent_steps` 流水一起清，返回 `{dataset_id, table, deleted, removed_source}`；不存在返回 404
- 上传清洗失败（`IngestError`）时当场删掉刚写下的 `data/files/upload_*`
- pp/db.py:drop_table() 是唯一新增的 DuckDB **写**路径，查询路径仍只走只读连接
- web/index.html 加「删除数据集」按钮：confirm 后调 DELETE /datasets/{id}、刷新数据集列表，被删的正是当前结果所属数据集时一并清空结果区（用户显式要求，不受「不改已验收阶段产物」限制）

本次真实链路复验（8017、profile `local`、`deepseek-flash`）：

- 上传 `smoke.csv` → `d_2df212c5`（3 行 × 2 列）
- 真实提问 → SQL：`SELECT region, SUM(amount) AS total FROM ds_d_2df212c5 GROUP BY region ORDER BY total DESC`；1 步 `run_sql`（22ms、3 行）；`insight` 完整（confidence high、3 findings、2 suggestions、3 caveats）；`degraded` 为空
- `tasks` 表已落一行：`status=ok`、`session_id=s_live`、`error` 空、`degraded_json=[]`；`agent_steps` 1 行
- `DELETE /datasets/d_2df212c5` → 200 `{"deleted":true,"removed_source":true}`；画像 404、tasks 0 行、再删 404

## 2. 未完成、已知损坏或有坑的地方

**MVP 已无未完成阶段**，剩下的是台账遗留问题（详见 `docs/代码台账.md`）：

- B 类（要先登记再改）：`K-002` 守卫一刀切、`K-006` 统计工具超时固定 10s、`K-008` 提问末步 200 行上限、`K-009`+`K-018` 数字回溯与引用字段、`K-013` 四个路由请求体是裸 `dict`（字段错是 400/默认值而不是 422）
- C 类（归后续阶段，现在不做）：`K-010`/`K-011` 归 P11、`K-012` 归 P10、`K-014` 归 P16/P21、`K-016` 归 P21
- 已修复：`K-001`、`K-003`、`K-004`、`K-005`、`K-007`、`K-015`、`K-019`（`K-002`、`K-008`、`K-009` 是「设计取舍，暂不修」，别当成已修）
- `K-017` 测试必须在授权（非沙箱）下跑，否则 `tmp_path` 不可写会 40+ ERROR

**其他要留意的**

- 页面删除按钮只有静态断言 + 手测覆盖：JS 语法用 `node --check` 验过、端点用真机 HTTP 验过，但**没在浏览器里点过**（点它要弹 `confirm`，自动化被 URL 策略挡住）。交接时标签页停在 `http://127.0.0.1:8000/`（拒绝连接），页面地址是 8017，请手点一次确认
- P5 手工 8 条验了 7 条半：「删掉 API Key 后提问仍显示结果表」只验到「降级提示 + 页面不崩」——无密钥时根本拿不到 SQL，本来就没有表；「带表降级」由 `tests/test_p4_insight.py:test_ask_keeps_table_when_insight_degrades` 覆盖
- 本机 `config/local.json` 已有页面写入的密钥（profile `local`，`deepseek-flash`）；`.gitignore` 挡住，不进仓库
- `data/` 里有烟测留下的数据集 `d_fc466e97`（dirty.csv）与本次复验的 `d_3216c6b3`（smoke.csv），还有 `data/uvicorn.*.log`；都被 gitignore，但页面下拉里能看到，用页面「删除数据集」按钮或 `DELETE /datasets/<id>` 清掉即可
- 依赖版本很新（pandas 3.0.6、duckdb 1.5.5、openai 3.16.2、fastapi 0.141.1、pydantic 2.13.5）

## 3. 下一步要做的第一件事（精确到文件与命令）

按台账排期进 B 类，建议顺序：**`K-013` → `K-002` → `K-006` → `K-008` → `K-009`/`K-018`**。每个 B 类项的三步固定动作：

1. 回 `docs/最小可用集-系统设计.md` §3 登记契约变化（端点请求体、函数签名），需要时同步 `docs/系统总体设计.md` §4
2. 改代码：`K-013` 在 `app/main.py` 给 `/query`、`/stats`、`/ask`、`/insight` 换 pydantic 请求体（`app/schemas.py` 加模型）；`K-002` 在 `app/db.py:_guard` 按引号切分后再判注释与分号；`K-006` 给 `app/db.py:describe` 加 `timeout_s`；`K-008` 调 `config/tools.json` 的 `max_rows_per_step`
3. 同步改验收命令并跑绿：`& .\.venv\Scripts\python.exe -m pytest tests -q`（当前基线 69 passed）

C 类不做，按设计 §6 路线表接：`K-010`/`K-011` 归 P11、`K-012` 归 P10、`K-014` 归 P16/P21、`K-016` 归 P21。

## 4. 需要人拍板的问题

- 已定，不用再问：桌面产品名跟仓库名——安装包 `Knowledge-Analysis-Assistant-Setup.exe`、数据在 `%APPDATA%\Knowledge-Analysis-Assistant`（4 处旧名已改齐）；README 已补怎么跑与文档地图；两个测试夹具已隔离 `config/local.json`（K-019）
- 待拍板：`K-009`/`K-018` 要给 `Insight` 加引用字段（改 P4 契约、要同步改 `test_p4_insight.py` 与 `test_p5_flow.py`），按 B 类顺序排在最后，动手前先回设计文档登记
- 已定：页面补了数据集删除按钮（后端端点 + 页面入口都在，`confirm` 一次；P20 再换正式删除/重导入 UI）

## 5. 环境事实（换机器时需要）

- Python：`C:\Users\14778\AppData\Local\Programs\Python\Python312\python.exe`；项目虚拟环境 `.venv`
- 依赖：`requirements.txt` 已装齐（A 类补丁没加新依赖）
- 代理 `http://127.0.0.1:7897` 已写进仓库本地 git 配置；`pip install` 与下载要手动带代理
- 本机沙箱：命令默认被拦；已批准前缀 `.venv\Scripts\python.exe -m pytest`。**测试必须走授权运行**
- 改文件用：`& 'C:\Users\14778\AppData\Local\OpenAI\Codex\bin\cdef5aaf3e41ab53\codex.exe' --codex-run-as-apply-patch $patch`（整行匹配对超长行会认不出，认不出时改用 PowerShell 精确子串替换）；新建文件用 `[System.IO.File]::WriteAllText($path, $text, (New-Object System.Text.UTF8Encoding($false)))`
- 起服务看页面：`& .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8017`，浏览器开 `http://127.0.0.1:8017/`；本次交接时 8017 正在跑（日志 `data/uvicorn.*.log`）
- **起服务前先看端口**：`Get-NetTCPConnection -LocalPort 8017 -State Listen`，有旧进程先停。本次踩过：旧进程（13:22 启动、改动之前的代码）占着 8017，新进程绑定失败会**悄悄退出**，健康检查还通，于是整轮真链路测的是旧代码（`tasks_row null`、`DELETE` 405 就是这么来的）

## 6. 建议下一个 agent 调用的技能

- `tdd`：改测试/加测试时先写断言
- `implement`：按设计文档施工
- `handoff`：再次交接时用它
- 已常驻、不必显式调用：`ponytail`、`benjamin-plus`

## 7. 必须先读的文件

1. `AGENTS.md`（自动加载）
2. `docs/索引.md`
3. `docs/代码台账.md`（已完成文件、K-002~K-019、已拍板决策）
4. `docs/最小可用集-系统设计.md`（§3 契约、§5 扩展契约、§6 路线表、§7 刻意不做）
5. `docs/最小可用集-测试清单.md`（逐段测试项与 DoD）

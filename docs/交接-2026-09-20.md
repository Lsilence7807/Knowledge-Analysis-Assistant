# 交接文档 · 2026-09-20（P4 完成后改写，含问题登记与修复排期）

给下一个会话用。目标：接着做 MVP 的 P5 前端闭环（最后一段），之后按本文第 3 节末尾的清单清台账。

说明：`handoff` 技能默认把交接文档写到系统临时目录；本项目按 `AGENTS.md` 第 6 节写进仓库并提交，换会话、换电脑都能拿到。同一天再次交接就就地改写本文件，旧版本在 git 历史里。

## 1. 已完成阶段与对应提交号

仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，工作区干净；本交接文档自身的提交看 `git log -1`。

| 阶段 | 提交 | 验收 |
|---|---|---|
| P0 骨架与契约冻结 | `3383821`、补丁 `52501f0` | `tests/test_p0_skeleton.py` 5 passed |
| P1 数据摄入 | `1d9ab68` | `tests/test_p1_ingest.py` 11 passed |
| P2 查询与统计层 | `89826d0` | `tests/test_p2_sql.py` 19 passed |
| P3 自然语言转 SQL | `7e7fc82`，补丁 `c555283`、`d452841`、`b610810` | `tests/test_p3_nlu.py` 11 passed |
| P13 Agent 循环与工具调用 | `7e27c31` | `tests/test_p13_agent.py` 11 passed |
| P4 洞察生成 | `bb75cb8` | `tests/test_p4_insight.py` 8 passed |
| 决定登记与文档对齐 | `1fe3adf`、`9ae6d19`、`f27fa93`、`d328c8a`、`ab8ca3b`、`3065722`、`2d8635d`、`4e26ba8` | docs only |

全量回归：`& .\.venv\Scripts\python.exe -m pytest tests -q` → **60 passed, 1 warning**（警告来自 Starlette 依赖，不是本项目代码）。

P4 落地的形状（P5 要照着渲染）：

- `app/insight.py`：`summarize(question, result, profile, prior) -> Insight`、`PROMPT_VERSION="p4-1"`、限 50 行喂表、数字回溯写 caveats、失败抛 `InsightError`（`.code` 为 `insight:llm` / `insight:contract`）
- `app/schemas.py` 的 `Insight` / `Finding` / `Suggestion` 字段全部必填：缺字段即不合契约 → 重试 1 次 → 降级
- `app/main.py` 加 `POST /insight`（body：`dataset_id` + `question` + `sql`）与 `_attach_insight()`；`/ask` 的 agent 与 P3 两条路径都会补 `insight`
- `app/registry.py` 注册 `insight` 能力；`/capabilities` 现在 5 个能力在有密钥时都可为 true
- 降级只追加 `degraded`，表格与 SQL 永不受影响；结果表 0 行时不调模型、`insight` 为 null

## 2. 未完成、已知损坏或有坑的地方

**登记在册（详见 `docs/代码台账.md` 已知问题汇总）**

- `K-002` `_guard` 一刀切：字符串字面量里含 `--`、`/*`、`;` 的 SQL 被拒（`SELECT 'a;b'` 跑不了）
- `K-004` / `K-007` 真实模型链路从未出网：P3（`_complete`）、P13（`chat_tools`）、P4（insight）全走替身。**P5 配好密钥后一次验掉三件事**：单跳、多跳工具调用、insight
- `K-005` `/ask` 不落 `tasks` 表；`K-006` 统计工具超时固定 10s（tools.json 的 15s 无效）；`K-008` 提问末步 200 行上限（`/query` 是 5000）；`K-009` 数字回溯只认字面数字，caveats 会偏吵
- `K-010` 无鉴权/无 CORS、`RATE_LIMIT_PER_MIN` 读了不用；`K-011` SQL 只拦写不限读（可读任意表）；`K-012` `session_id`/`prior` 未启用 —— 三条都是设计 §7 明写「刻意不做」，归 P11/P10
- `K-013` 四个路由的请求体是裸 `dict`：字段错是 400/默认值，不是 422；`K-014` 默认 10s 超时未压测（`threading.Timer` + `conn.interrupt()`）；`K-015` 存储只增不减（上传文件无清理、无删除数据集接口）；`K-016` 并发无策略（DuckDB/SQLite 连接各开各的）；`K-017` 测试必须在授权下跑，沙箱内 `tmp_path` 不可写会 40+ ERROR

**其他要留意的**

- 前端拿到的东西：`steps`（`n/tool/args_digest/ok/ms/rows`，失败步骤多 `error`）、`insight`、`degraded`（`query:llm` / `agent:llm` / `agent:max_steps` / `insight:llm` / `insight:contract`）、`message`、`truncated`、`hint`；caveats 不是错误，单独展示
- `web/` 目录还没建，`web/vendor/echarts.min.js` 需要下载（已拍板走本地文件、不走 CDN、缺文件时只出表格不报错）
- 依赖版本很新（pandas 3.0.6、duckdb 1.5.5、openai 3.16.2、fastapi 0.141.1、pydantic 2.13.5），写新模块前先验一次 API 形态

## 3. 下一步要做的第一件事（精确到文件与命令）

做 **P5 前端闭环**（MVP 最后一段），依据 `docs/最小可用集-系统设计.md` §3.2（`GET/PUT /settings/models`）与 P5 段，以及 `docs/最小可用集-测试清单.md` P5 段（自动 3 条 + 手工 8 条）：

- 开工前回归：`& .\.venv\Scripts\python.exe -m pytest tests -q`（应为 60 passed）
- 新建：`web/index.html`（单文件、原生 JS、不用构建工具）、`tests/test_p5_flow.py`、`web/vendor/echarts.min.js`（见第 4 节）
- 允许改：`app/main.py` 加静态挂载与 `GET/PUT /settings/models`；`app/config.py` 加 `save_settings()`（合并写 `config/local.json`）
- 复用现成件：`models.list_models()` 已合并 `local.json`，`config.api_key(profile)` 按「本地文件 → profile 的 `api_key_env` → 通用 `LLM_API_KEY`」解析且调用时读文件（保存即生效）；`PUT` 只需写盘
- 页面元素（对应手工清单）：上传框、数据集下拉、提问框、SQL 折叠区、结果表、步骤面板、结论卡片（caveats 单列）、ECharts 图表、模型配置表单、降级提示条
- 测试口径：`GET /settings/models` 不回传密钥明文；`PUT` 后 `/ask` 走新 profile 且不降级；模型一律替身（`_complete` / `chat_tools`），不真出网；`LOCAL_SETTINGS` 指向 `tmp_path`（照抄 `tests/test_p4_insight.py` 的夹具）
- 验收：`& .\.venv\Scripts\python.exe -m pytest tests/test_p5_flow.py -q`，再全量回归；浏览器手测跑测试清单里那 8 条
- 范围外：不引入打包工具/框架、不做桌面 exe（P11/P12）、不做会话记忆（P10）
- 收尾：更新台账 → 提交（`P5 前端闭环：验收 test_p5_flow 通过`）→ 推送

**P5 之后：按 A/B/C 清台账（已在台账「已拍板决策」登记）**

- A 类零风险，随手修且全量回归必绿：`K-005` 落 `tasks` 表；`K-015` 上传文件清理与数据集删除端点（删除/重导入正式端点归 P20）
- B 类要先回设计文档登记、再同步改对应验收命令：`K-013` 请求体改 pydantic、`K-002` 守卫按引号切分（注释与多语句仍须拒）、`K-006` 给 `db.describe` 加 `timeout_s`、`K-008` 调 `max_rows_per_step`、`K-009` 给 Insight 加引用字段
- C 类不做，按设计 §6 路线表接：`K-010`/`K-011` 归 P11、`K-012` 归 P10、`K-014` 归 P16/P21、`K-016` 归 P21；`K-017` 属环境，接 CI 前解决

## 4. 需要人拍板的问题

- `web/vendor/echarts.min.js` 从哪来：本机沙箱默认拦网。默认做法是按已拍板决策用 `http.proxy=http://127.0.0.1:7897` 下载约 1MB 的 ECharts 单文件到 `web/vendor/` 并提交；不下载就按「缺文件只出表格」降级，图表留到以后。
- 修复顺序确认：默认 **P5 先做**，再清 A 类、逐条登记做 B 类（已记入台账）。要改顺序就说一声。

## 5. 环境事实（换机器时需要）

- Python：`C:\Users\14778\AppData\Local\Programs\Python\Python312\python.exe`；项目虚拟环境 `.venv`
- 依赖：`requirements.txt`，已装齐并验证可导入（前端不需要新依赖）
- 代理 `http://127.0.0.1:7897` 已写进仓库本地 git 配置（`http.proxy` / `https.proxy`），`git push` 直接跑；`pip install` 与下载都要手动带代理
- 本机沙箱：命令默认被拦。已批准前缀 `…\.venv\Scripts\python.exe -m pytest`、`git add`、`git push`；`git commit` 若报 `.git/index.lock: Permission denied`，加 `sandbox_permissions=require_escalated` 重跑同一条命令。**测试必须走授权运行**，否则 `tmp_path` 不可写会 40+ ERROR（不是 FAILED）
- 改文件用：`& 'C:\Users\14778\AppData\Local\OpenAI\Codex\bin\cdef5aaf3e41ab53\codex.exe' --codex-run-as-apply-patch $patch`（`$patch` 为 PowerShell here-string）。已知坑：**同一个 patch 里多个 `@@` 段必须按文件行号从前往后排**，且不要把前一段改过的行当后一段的上下文；稳妥做法是一个 `@@` 一段、分多次调用（本会话踩过两次）。整个 patch 是原子的，失败不留半截
- 新建文件可用 `[System.IO.File]::WriteAllText($path, $text, (New-Object System.Text.UTF8Encoding($false)))` 写 UTF-8 无 BOM

## 6. 建议下一个 agent 调用的技能

- `tdd`：先写 `tests/test_p5_flow.py` 再实现路由
- `implement`：按设计文档 P5 范围施工
- `handoff`：再次交接时用它
- 已常驻、不必显式调用：`ponytail`、`benjamin-plus`

## 7. 必须先读的文件

1. `AGENTS.md`（自动加载）
2. `docs/索引.md`
3. `docs/最小可用集-系统设计.md`（§3.2 HTTP API、P5 段、§5 扩展契约、§7 刻意不做）
4. `docs/代码台账.md`（已完成文件、K-002~K-017、已拍板决策）
5. `docs/最小可用集-测试清单.md`（P5 段的自动 3 条 + 手工 8 条）
# 交接文档 · 2026-09-20（P2 完成后改写）

给下一个会话用。目标：接着做 MVP 的 P3 自然语言转 SQL。

说明：`handoff` 技能默认把交接文档写到系统临时目录；本项目按 `AGENTS.md` 第 6 节写进仓库并提交，这样换会话、换电脑都能拿到。同一天再次交接就就地改写本文件，旧版本在 git 历史里。

## 1. 已完成阶段与对应提交号

仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，工作区干净，最后一次推送 `89826d0`。

| 阶段 | 提交 | 验收 |
|---|---|---|
| P0 骨架与契约冻结 | `3383821` | `tests/test_p0_skeleton.py` 5 passed |
| P1 数据摄入 | `1d9ab68` | `tests/test_p1_ingest.py` 11 passed（当时的全量） |
| P2 查询与统计层 | `89826d0` | `tests/test_p2_sql.py` 19 passed，全量回归 30 passed |
| 决定登记（名称 / DeepSeek / ECharts） | `1fe3adf` | docs only |

## 2. 未完成、已知损坏或有坑的地方

- `K-002`（`docs/代码台账.md`）：`app/db.py:_guard` 一刀切——字符串字面量里出现 `--`、`/*` 或 `;` 也会被拒（`SELECT 'a;b'` 跑不了）。属设计取舍，暂不修。
- 超时路径只在 DuckDB 长递归查询上验证过（`db.exec_sql(slow, timeout_s=1)`），默认 10s 没做过真实长压测。
- `/query`、`/stats` 的请求体是裸 `dict`（`Body(...)`），没有 pydantic 校验：字段缺失走 400/默认值而不是 422。这是为守住设计文档 P2「改动范围」不动 `app/schemas.py`；P5 前端要自己兜。
- P3 起模型链路要 `LLM_API_KEY`；不设密钥时只能验降级分支。
- 老坑照旧：FastAPI 用 `lifespan`（`on_event` 已废弃）；依赖版本很新（pandas 3.0.6、duckdb 1.5.5、fastapi 0.141.1），写新模块前先验一次 API 形态；ECharts 的 CDN 在离线端会失效，P5 用 `web/vendor/`。

## 3. 下一步要做的第一件事（精确到文件与命令）

做 **P3 自然语言转 SQL**，依据 `docs/最小可用集-系统设计.md` 第 4 节 P3 与 §3.1/§3.4 契约：

- 开工前回归：`& .\.venv\Scripts\python.exe -m pytest tests -q`（应为 30 passed）
- 新建：`app/models.py`、`app/llm.py`、`app/nlu.py`、`tests/test_p3_nlu.py`
- 允许改：`app/main.py` 加 `POST /ask`；`app/registry.py` 注册 query 能力；`config/models.json` 只在真要加 profile 时动（DeepSeek 已在里面）
- 交付物：问题 → SQL → 结果表；模型产出的 SQL 出站前必须过 `app/db.py:_guard`（模型给 `DROP TABLE` 要被拦）；模型不可用、JSON 非法、无密钥 → `degraded: ["query:llm"]` + 可读说明，HTTP 仍 200
- 反例（`docs/最小可用集-测试清单.md` P3 段）：无密钥、mock 返回 `DROP TABLE`、mock 返回非法 JSON（重试 1 次后降级）、mock 返回超长文本被截断
- 验收：`& .\.venv\Scripts\python.exe -m pytest tests/test_p3_nlu.py -q`，然后全量回归
- 收尾：更新 `docs/代码台账.md` → 提交（`P3 自然语言转 SQL：验收 test_p3_nlu 通过`）→ 推送

## 4. 需要人拍板的问题

1. `LLM_API_KEY` 现在给吗？不给也能开工（只验降级分支 + mock 分支），但真实链路要等密钥。
2. 密钥怎么注入本机环境：`setx LLM_API_KEY` 写用户环境变量、每次运行前 `$env:LLM_API_KEY=...`、还是引入 `.env` + python-dotenv（要加依赖）。第二种零成本，第一种最省事。

## 5. 环境事实（换机器时需要）

- Python：`C:\Users\14778\AppData\Local\Programs\Python\Python312\python.exe`；项目虚拟环境 `.venv`
- 依赖：`requirements.txt`，已装齐并验证可导入
- 代理 `http://127.0.0.1:7897` 已写进仓库本地 git 配置（`http.proxy` / `https.proxy`），`git push` 直接跑；`pip install` 仍要手动带 `-c http.proxy=...`
- 本机沙箱限制：命令默认被拦，已批准的前缀有 `.venv\Scripts\python.exe -m pytest`、`git add`、`git commit`、`git push`（新会话可能要重新授权）。**测试必须走授权运行**，否则 `tmp_path` 夹具因系统临时目录不可写直接 PermissionError。
- 改文件别用 `apply_patch` 包装脚本（`.bat` 会吃掉多行参数报 `The last line of the patch must be '*** End Patch'`），直接调 `C:\Users\14778\AppData\Local\OpenAI\Codex\bin\cdef5aaf3e41ab53\codex.exe --codex-run-as-apply-patch <patch>`。

## 6. 建议下一个 agent 调用的技能

- `tdd`：先写 `tests/test_p3_nlu.py` 再实现
- `implement`：按设计文档 P3 范围施工
- `handoff`：再次交接时用它
- 已常驻、不必显式调用：`ponytail`、`benjamin-plus`

## 7. 必须先读的文件

1. `AGENTS.md`（自动加载）
2. `docs/索引.md`
3. `docs/最小可用集-系统设计.md`（P3 段与 §3.1/§3.4 契约）
4. `docs/代码台账.md`（已完成文件、K-002、已拍板决策）
5. `docs/最小可用集-测试清单.md`（P3 段）

# 交接文档 · 2026-09-20（P13 完成后改写）

给下一个会话用。目标：接着做 MVP 的 P4 洞察生成。

说明：`handoff` 技能默认把交接文档写到系统临时目录；本项目按 `AGENTS.md` 第 6 节写进仓库并提交，换会话、换电脑都能拿到。同一天再次交接就就地改写本文件，旧版本在 git 历史里。

## 1. 已完成阶段与对应提交号

仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，工作区干净；本交接文档自身的提交看 `git log -1`。

| 阶段 | 提交 | 验收 |
|---|---|---|
| P0 骨架与契约冻结 | `3383821`、补丁 `52501f0` | `tests/test_p0_skeleton.py` 5 passed |
| P1 数据摄入 | `1d9ab68` | `tests/test_p1_ingest.py` 11 passed |
| P2 查询与统计层 | `89826d0` | `tests/test_p2_sql.py` 19 passed |
| P3 自然语言转 SQL | `7e7fc82`，补丁 `c555283`、`d452841`、`b610810` | `tests/test_p3_nlu.py` 11 passed |
| P13 Agent 循环与工具调用 | `7e27c31` | `tests/test_p13_agent.py` 11 passed |
| 决定登记与文档对齐 | `1fe3adf`、`9ae6d19`、`f27fa93`、`d328c8a`、`ab8ca3b`、`3065722` | docs only |

全量回归：`& .\.venv\Scripts\python.exe -m pytest tests -q` → **52 passed, 1 warning**（警告来自 Starlette 依赖，不是本项目代码）。

P13 落地的形状（P4 要接着用）：

- 新建 `app/tools.py`（注册表 + `run_sql` / `describe_stats` / `detect_anomalies` 三个只读工具）、`app/agent.py`（循环 + `AgentResult`）、`tests/test_p13_agent.py`
- `app/llm.py` 补 `chat_tools(messages, tools, model_id) -> {"content", "tool_calls":[{"id","name","arguments"}]}`；新增 `_client()`，`_complete` 保持原样
- `GET /tools` 返回工具、kinds、白名单与预算；`POST /ask` 默认走 agent（`config.ENABLE_AGENT=true`），`false` 回落 P3 单跳
- `store.py` 追加 `insert_agent_step()` 与 `log_capability()`（只是新增函数，没动已验收的函数体）
- `agent.run()` 返回的字段 = `/ask` 响应（`asdict` 直接返回）；模型不可用首步就失败 → `degraded=["query:llm"]`（与 P3 同一个标识），中途失败 → `["agent:llm"]`；步数用尽 → `["agent:max_steps"]` + 可读 message

## 2. 未完成、已知损坏或有坑的地方

- `K-002`：`app/db.py:_guard` 一刀切，字符串字面量里出现 `--`、`/*`、`;` 也会被拒（`SELECT 'a;b'` 跑不了）。设计取舍，暂不修。
- `K-004` / `K-007`：真实模型链路从未验证（无密钥）。P3 走 `llm._complete` 替身，P13 走 `llm.chat_tools` 替身，都没出网；真实 `tools` 参数与 tool_calls 回填格式同样没验过。P5 页面配好密钥后手工验。
- `K-005`：`/ask`（含 agent 路径）不落 `tasks` 表，`task_id` 只回给调用方。归 P16。
- `K-006`：`describe_stats` / `detect_anomalies` 没有独立超时，tools.json 里的 15s 不生效，实际走 `db.describe` 固定的 10s。要改需先回设计文档登记（动 P2 契约）。
- `K-008`：提问最后一步的行数上限是 `max_rows_per_step`(200)，比 `/query` 的 5000 小（截断用 `db.exec_sql` 的 limit 实现）。
- `/query`、`/stats`、`/ask` 的请求体是裸 `dict`（`Body(...)`），没有 pydantic 校验：字段错了走 400 或默认值而不是 422，P5 前端要自己兜。
- `ENABLE_AGENT` 默认 true：写测试时要用 `monkeypatch.setattr(config, "ENABLE_AGENT", ...)`，别改 `config.py` 的默认值（`tests/test_p3_nlu.py` 的 autouse 夹具就是这么钉住 P3 通道的）。
- `/ask` 现在还会返回 `steps` 与 `message`；P5 步骤面板直接用 `steps`（`n/tool/args_digest/ok/ms/rows`，失败步骤多一个 `error`）。
- 依赖版本很新（pandas 3.0.6、duckdb 1.5.5、openai 3.16.2、fastapi 0.141.1、pydantic 2.13.5），写新模块前先验一次 API 形态；FastAPI 用 `lifespan`；ECharts 走本地文件。

## 3. 下一步要做的第一件事（精确到文件与命令）

做 **P4 洞察生成**，依据 `docs/最小可用集-系统设计.md` §3.1（`insight.summarize` 签名）、§3.2（`/ask` 响应里的 `insight` 字段）、§3.4（Insight schema 与降级规则）、P4 段，以及 `docs/最小可用集-测试清单.md` P4 段：

- 开工前回归：`& .\.venv\Scripts\python.exe -m pytest tests -q`（应为 52 passed）
- 新建：`app/insight.py`、`tests/test_p4_insight.py`
- 允许改：`app/schemas.py` 加 `Insight` 模型；`app/main.py` 加 `POST /insight`，并在 `/ask` 里把 insight 并入响应
- 建议做法（编排层插调用，不动已验收契约）：`agent.run()` 的返回值不带 insight；在 `main.py` 的 `/ask` 拿到结果后调 `insight.summarize(...)` 补 `insight` 字段，agent 与 P3 两条路径都补。若改成给 `AgentResult` 加字段，就得同步改 `tests/test_p13_agent.py`，不建议
- 契约要点：`Insight = {summary, findings[], anomalies[], suggestions[], confidence(low|medium|high), caveats[]}`；失败重试 1 次 → 仍失败则 `insight` 置空 + `degraded` 追加 `insight:contract`，HTTP 仍 200；`prior` MVP 传空列表；结论里的数字必须能在结果表里找到（编造数字 → 写 `caveats` 或整体降级）
- 测试用模型替身（`llm._complete` 或 `llm.chat_json` 替身），**不要依赖真实密钥**
- 验收：`& .\.venv\Scripts\python.exe -m pytest tests/test_p4_insight.py -q`，然后全量回归
- 收尾：更新 `docs/代码台账.md` → 提交（`P4 洞察生成：验收 test_p4_insight 通过`）→ 推送 → 再交接一次（P5 起点）

## 4. 需要人拍板的问题

暂无。两件按默认做即可（有异议再说）：insight 在 agent 与 P3 两条 `/ask` 路径上都补；`/ask` 里的 insight 断言写在 `tests/test_p4_insight.py` 里，不改 `test_p3_nlu.py` / `test_p13_agent.py`。

## 5. 环境事实（换机器时需要）

- Python：`C:\Users\14778\AppData\Local\Programs\Python\Python312\python.exe`；项目虚拟环境 `.venv`
- 依赖：`requirements.txt`，已装齐并验证可导入
- 代理 `http://127.0.0.1:7897` 已写进仓库本地 git 配置（`http.proxy` / `https.proxy`），`git push` 直接跑；`pip install` 仍要手动带 `-c http.proxy=...`
- 本机沙箱：命令默认被拦。已批准前缀 `…\.venv\Scripts\python.exe -m pytest`、`git add`、`git push`；`git commit` 若报 `.git/index.lock: Permission denied`，用 `sandbox_permissions=require_escalated` 重跑同一条命令即可。**测试必须走授权运行**，否则 `tmp_path` 夹具因系统临时目录不可写直接 PermissionError（表现是 40 个 ERROR 而不是 FAILED）
- 改文件用 apply-patch：`& 'C:\Users\14778\AppData\Local\OpenAI\Codex\bin\cdef5aaf3e41ab53\codex.exe' --codex-run-as-apply-patch $patch`，`$patch` 用 PowerShell here-string（`@'…'@`）。**同一个 patch 里的 hunk 必须按文件行号从前往后排列**，乱序会报 `Failed to find expected lines`；整个 patch 是原子的，失败不会留下半截改动
- 新建文件可用 `[System.IO.File]::WriteAllText($path, $text, (New-Object System.Text.UTF8Encoding($false)))` 写 UTF-8 无 BOM

## 6. 建议下一个 agent 调用的技能

- `tdd`：先写 `tests/test_p4_insight.py` 再实现
- `implement`：按设计文档 P4 范围施工
- `handoff`：再次交接时用它
- 已常驻、不必显式调用：`ponytail`、`benjamin-plus`

## 7. 必须先读的文件

1. `AGENTS.md`（自动加载）
2. `docs/索引.md`
3. `docs/最小可用集-系统设计.md`（§3.1 接口签名、§3.2 `/ask` 与 `/insight`、§3.4 Insight 契约、P4 段）
4. `docs/代码台账.md`（已完成文件、K-002~K-008、已拍板决策）
5. `docs/最小可用集-测试清单.md`（P4 段）
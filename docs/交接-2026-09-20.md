# 交接文档 · 2026-09-20

给下一个会话用；同一天再交接就就地改写本文件，旧版本在 git 历史里。
状态：**MVP 七段 + 台账 A/B 类补丁 + 手工复验全部完成**，工作区干净，远端 `main` 已同步。

## 1. 已完成阶段与提交号

七段验收提交：P0 `3383821`、P1 `1d9ab68`、P2 `89826d0`、P3 `7e7fc82`、P13 `7e27c31`、P4 `bb75cb8`、P5 `18b2482`。之后的补丁（K-019 隔离、A 类、页面删除按钮、B 类 K-013/K-002/K-006/K-008/K-009/K-018、质量门、手工复验、K-021）看 `git log --oneline`，逐条结论在 `docs/代码台账.md`。

## 2. 下一步（精确到命令）

1. 跑基线：`& .\.venv\Scripts\python.exe -m pytest tests -q` → 应 **79 passed**（必须授权运行，见 §4 沙箱坑）
2. 挑下一段：读 `docs/系统总体设计.md` 第 5、6 节路线表，按依赖顺序取最前一段，契约读完再动代码（MVP 只取了 P0/P1/P2/P3/P13/P4/P5，其余阶段不重新编号）
3. 每段固定动作（`AGENTS.md` 第 0 节）：上一段回归 → 只改该段「改动范围」里的文件 → 跑该段验收命令 → 跑全部已完成阶段测试 → 更新 `docs/代码台账.md` → 提交推送
4. C 类问题做到那几段再清：`K-010`/`K-011` → P11、`K-012` → P10、`K-014` → P16/P21、`K-016` → P21

## 3. 需要人拍板

- 下一段开哪个，候选三条（按大文档第 5、6 节路线表）：P6 Skill 导入（路线表里的下一段，纯新增能力）、P9 多模型接入与失败回退（现在换厂商只能手动，最影响日常使用）、P10 多轮记忆与精确缓存（重复提问秒回，顺带清 K-012）。选哪条，后面就按 §2 的四步走。

## 4. 坑

- 测试必须授权（非沙箱）运行：沙箱内 `tmp_path` 不可写 → 40+ ERROR（K-017）
- 起服务前先看端口：`Get-NetTCPConnection -LocalPort 8017 -State Listen`；旧进程占着端口时新进程会**悄悄退出**，健康检查还通，整轮真链路测的是旧代码（收尾时服务已停、端口已释放、`data/files` 已清空）
- 本机 `config/local.json` 有页面写入的密钥（profile `local`、`deepseek-flash`），被 `.gitignore` 挡住，别提交；测试夹具已隔离它（K-019）
- 页面删除数据集会弹 `confirm`，点了就真删（2026-09-20 已手工复验通过）；没有数据集时按钮仍可点，P20 做正式删除 UI 时一起看
- 依赖版本很新：pandas 3.0.6、duckdb 1.5.5、openai 3.16.2、fastapi 0.141.1、pydantic 2.13.5

## 5. 环境事实

- 仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`；Python 3.12 + `.venv`，`requirements.txt` 已装齐
- 代理 `http://127.0.0.1:7897` 已写进仓库本地 git 配置；`pip install` 与下载要手动带
- 质量门唯一配置源 `pyproject.toml`（ruff 行宽 120、pytest 指 tests/）；本机写法 `.venv\Scripts\python.exe -m ruff check app tests`
- 起服务看页面：`& .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8017`，浏览器开 `http://127.0.0.1:8017/`（日志 `data/uvicorn.*.log`）
- 改文件用 `apply_patch`；本机多行补丁得直接调 `codex.exe --codex-run-as-apply-patch $patch`（`.bat` 包装传多行会失败），整行匹配认不出时改用 PowerShell 精确子串替换
- 开工前必读：`docs/索引.md` → `docs/代码台账.md` → 本次要施工的设计文档

## 6. 建议技能与参考

- 实现类：`tdd`（先写测试再改代码）、`implement`（照设计文档落地一段）、`code-review`（改完自审）
- 流程类：`handoff`（下一个会话收尾时再写交接）、`verification-before-completion`（宣布完成前先跑命令）
- 契约与设计：`系统总体设计.md` §4（签名/端点/表名）、§5-§6（阶段计划与扩展路线）

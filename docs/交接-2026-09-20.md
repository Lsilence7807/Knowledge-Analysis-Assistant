# 交接文档 · 2026-09-20（P3 完成后改写）

给下一个会话用。目标：接着做 MVP 的 P13 Agent 循环与工具调用。

说明：`handoff` 技能默认把交接文档写到系统临时目录；本项目按 `AGENTS.md` 第 6 节写进仓库并提交，换会话、换电脑都能拿到。同一天再次交接就就地改写本文件，旧版本在 git 历史里。

## 1. 已完成阶段与对应提交号

仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，工作区干净；本交接文档自身的提交看 `git log -1`。

| 阶段 | 提交 | 验收 |
|---|---|---|
| P0 骨架与契约冻结 | `3383821`、补丁 `52501f0` | `tests/test_p0_skeleton.py` 5 passed |
| P1 数据摄入 | `1d9ab68` | `tests/test_p1_ingest.py` 11 passed |
| P2 查询与统计层 | `89826d0` | `tests/test_p2_sql.py` 19 passed |
| P3 自然语言转 SQL | `7e7fc82`，补丁 `c555283`、`d452841`、`b610810` | `tests/test_p3_nlu.py` 11 passed |
| 决定登记与文档对齐 | `1fe3adf`、`9ae6d19`、`f27fa93`、`d328c8a`、`ab8ca3b` | docs only |

全量回归：`& .\.venv\Scripts\python.exe -m pytest tests -q` → **41 passed, 1 warning**（警告来自 Starlette 依赖，不是本项目代码）。

已拍板决定（详见 `docs/代码台账.md`「已拍板决策」）：名称统一「AI 数据分析助手」；ECharts 在 P5 落到本地 `web/vendor/`；**模型接入由 P5 页面配置**——任意 OpenAI 兼容厂商（base_url + 模型名 + 密钥）写进 `config/local.json`，密钥解析顺序 本地文件 → 该 profile 的 `api_key_env` → 通用 `LLM_API_KEY`，调用时读取，不用 setx、不用重启；MVP 默认 profile 仍是 DeepSeek，换厂商不用改代码。

其中读取侧已经落地（`d452841`：`config.api_key(profile)` + `models.list_models()` 合并本地 profile），并有测试锁住「页面加一个 GLM profile 就能直接提问」；P5 只需要实现写盘（`config.save_settings()`）与表单 + 两个路由。

## 2. 未完成、已知损坏或有坑的地方

- `K-002`：`app/db.py:_guard` 一刀切，字符串字面量里出现 `--`、`/*`、`;` 也会被拒（`SELECT 'a;b'` 跑不了）。设计取舍，暂不修。
- `K-003`：`llm.chat_tools()` 还没实现——**这正是 P13 要补的**。
- `K-004`：真实模型链路从未验证（本机没有密钥，P3 测试全部走 `llm._complete` 替身、不出网）。把任意厂商的 profile 与密钥写进 `config/local.json` 就能验。
- `K-005`：`/ask` 不落 `tasks` 表，`task_id` 只回给调用方，P13/P16 再补。
- `/query`、`/stats`、`/ask` 的请求体是裸 `dict`（`Body(...)`），没有 pydantic 校验：字段错了走 400 或默认值而不是 422，P5 前端要自己兜。
- 超时只用 1s 的长递归查询验过，默认 10s 没做过真实长压测。
- 依赖版本很新（pandas 3.0.6、duckdb 1.5.5、openai 3.16.2、fastapi 0.141.1、pydantic 2.13.5），写新模块前先验一次 API 形态；FastAPI 用 `lifespan`；ECharts 走本地文件。

## 3. 下一步要做的第一件事（精确到文件与命令）

做 **P13 Agent 循环与工具调用**，依据 `docs/最小可用集-系统设计.md` §3.5 工具契约与 §4 P13、`docs/最小可用集-测试清单.md` P13 段：

- 开工前回归：`& .\.venv\Scripts\python.exe -m pytest tests -q`（应为 40 passed）
- 新建：`app/tools.py`、`app/agent.py`、`tests/test_p13_agent.py`
- 允许改：`app/llm.py` 加 `chat_tools`（契约见设计 §3.1）；`app/main.py` 加 `GET /tools` 并把 `/ask` 换成走 agent（`config.ENABLE_AGENT` 为 false 时回落 P3 单跳）；`app/registry.py` 加一行注册 agent 能力
- 复用现成件：`config/tools.json` 已有白名单 `run_sql/describe_stats/detect_anomalies`、`max_steps` 6、每步超时、`max_rows_per_step` 200；`store.DDL` 里 `agent_steps`、`capability_log` 两张表已建好，直接写
- 工具实现直接调现成函数：`run_sql` → `db.exec_sql`；`describe_stats` / `detect_anomalies` → `db.describe`
- 交付物：3 个工具 + plan→调工具→观察→再规划的循环；步数预算 6；每步独立超时；单步结果超 200 行只回摘要与列名；步骤落 `agent_steps`（条数要与响应里的 `steps` 一致）；白名单外工具被拒并写 `capability_log`；`ENABLE_AGENT=false` 时完全回到 P3 行为
- 反例（测试清单 P13 段）：白名单外工具、不存在的工具名、步数耗尽（把 `AGENT_MAX_STEPS` 调成 1 测多跳）、工具抛异常、单步超 200 行
- 测试要用替身驱动模型（`llm.chat_tools` / `llm._complete` 替身），**不要依赖真实密钥**
- 验收：`& .\.venv\Scripts\python.exe -m pytest tests/test_p13_agent.py -q`，然后全量回归
- 范围外：`GET /sessions/{sid}/steps` 不做（归 P10）
- 收尾：更新 `docs/代码台账.md` → 提交（`P13 Agent 循环与工具调用：验收 test_p13_agent 通过`）→ 推送

## 4. 需要人拍板的问题

暂无。两件都已定：密钥「先不给，后面在页面配任意厂商」（`K-004` 真实链路等 P5 页面做好再验），P13 不做 `/sessions/{sid}/steps`（归 P10）。结论记在 `docs/代码台账.md`「已拍板决策」。

## 5. 环境事实（换机器时需要）

- Python：`C:\Users\14778\AppData\Local\Programs\Python\Python312\python.exe`；项目虚拟环境 `.venv`
- 依赖：`requirements.txt`，已装齐并验证可导入
- 代理 `http://127.0.0.1:7897` 已写进仓库本地 git 配置（`http.proxy` / `https.proxy`），`git push` 直接跑；`pip install` 仍要手动带 `-c http.proxy=...`
- 本机沙箱：命令默认被拦（新会话可能要重新授权），已批准的前缀有 `.venv\Scripts\python.exe -m pytest`、`git add`、`git commit`、`git push`。**测试必须走授权运行**，否则 `tmp_path` 夹具因系统临时目录不可写直接 PermissionError。
- 改文件别用 `apply_patch` 包装脚本（`.bat` 会吃掉多行参数，报 `The last line of the patch must be '*** End Patch'`），直接调 `C:\Users\14778\AppData\Local\OpenAI\Codex\bin\cdef5aaf3e41ab53\codex.exe --codex-run-as-apply-patch <patch>`。

## 6. 建议下一个 agent 调用的技能

- `tdd`：先写 `tests/test_p13_agent.py` 再实现
- `implement`：按设计文档 P13 范围施工
- `handoff`：再次交接时用它
- 已常驻、不必显式调用：`ponytail`、`benjamin-plus`

## 7. 必须先读的文件

1. `AGENTS.md`（自动加载）
2. `docs/索引.md`
3. `docs/最小可用集-系统设计.md`（§3.1 接口签名、§3.5 工具契约、P13 段）
4. `docs/代码台账.md`（已完成文件、K-002~K-005、已拍板决策）
5. `docs/最小可用集-测试清单.md`（P13 段）

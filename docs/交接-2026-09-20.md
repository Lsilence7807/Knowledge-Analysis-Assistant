# 交接文档 · 2026-09-20（P5 完成后改写）

给下一个会话用。目标：MVP 七段全部收尾，接下来按台账 A/B/C 分类清理遗留问题。

说明：`handoff` 技能默认把交接文档写到系统临时目录；本项目按 `AGENTS.md` 第 6 节写进仓库并提交，换会话、换电脑都能拿到。同一天再次交接就就地改写本文件，旧版本在 git 历史里。

## 1. 已完成阶段与对应提交号

仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，工作区干净。

| 阶段 | 提交 | 验收 |
|---|---|---|
| P0 骨架与契约冻结 | `3383821`、补丁 `52501f0` | `tests/test_p0_skeleton.py` 5 passed |
| P1 数据摄入 | `1d9ab68` | `tests/test_p1_ingest.py` 11 passed |
| P2 查询与统计层 | `89826d0` | `tests/test_p2_sql.py` 19 passed |
| P3 自然语言转 SQL | `7e7fc82`，补丁 `c555283`、`d452841`、`b610810` | `tests/test_p3_nlu.py` 11 passed |
| P13 Agent 循环与工具调用 | `7e27c31` | `tests/test_p13_agent.py` 11 passed |
| P4 洞察生成 | `bb75cb8` | `tests/test_p4_insight.py` 8 passed |
| P5 前端闭环（MVP 最后一段） | `18b2482` | `tests/test_p5_flow.py` 5 passed |
| P5 后补：测试隔离（K-019） | `935211c` | 全量仍 65 passed，耗时 18.5s → 9.9s |
| 文档对齐（P5 收尾） | 见 `git log -1` | docs only |

全量回归：`& .\.venv\Scripts\python.exe -m pytest tests -q` → **65 passed, 1 warning**（警告来自 Starlette 依赖）。

P5 落地的形状（后续阶段要接着用）：

- `web/index.html`：单文件原生 JS，无构建链；页面分 6 块——模型配置 / 数据 / 提问（含 SQL 折叠区与降级提示条）/ 步骤 / 结论卡片（caveats 单列）/ 结果（图表切换 + 表格）
- 静态挂载：`app/main.py` 末尾 `app.mount("/", StaticFiles(directory=config.BASE_DIR / "web", html=True))`，**必须排在全部 API 路由之后**，否则 `Mount("/")` 会抢走 `/health`、`/ask` 等
- `GET /settings/models`：只回 `id / provider / base_url / model / has_key`，密钥明文不出网关
- `PUT /settings/models`：写 `config/local.json` 的 `models` + `api_keys`，并把 `default` 设成该 profile —— **保存即生效**，不用重启；传空 `api_key` 等于清掉
- `app/config.py:save_settings(**patch)`：合并写盘（字典按键合并、带 id 的列表按 id 合并），其余模块只读不写
- ECharts 5.6.1 在 `web/vendor/echarts.min.js`（1.0MB，Apache-2.0）；`typeof echarts === "undefined"` 时只显示提示与表格，不报错
- 真实模型链路已出网验证：单跳、多跳（2 步 `run_sql`）、insight 全部跑通，`K-004`、`K-007` 已转「已修复」

## 2. 未完成、已知损坏或有坑的地方

**MVP 已无未完成阶段**，剩下的都是台账里的遗留问题（详见 `docs/代码台账.md` 已知问题汇总）：

- `K-002` `_guard` 一刀切：字符串字面量里含 `--`、`/*`、`;` 的 SQL 被拒（`SELECT 'a;b'` 跑不了）
- `K-005` `/ask` 不落 `tasks` 表；`K-006` 统计工具超时固定 10s（tools.json 的 15s 无效）；`K-008` 提问末步 200 行上限；`K-009` 数字回溯只认字面数字
- `K-013` 四个路由请求体是裸 `dict`：字段错是 400/默认值而不是 422
- `K-015` 存储只增不减：上传文件无清理、无删除数据集接口
- `K-018`（P5 真链路新发现）数字回溯把表名/id 里的数字当成可疑数字：结论提到 `ds_d_fc466e97`，caveats 就写「以下数字在结果表里找不到，可能是模型编的：2、3、466、97」。与 K-009 同源，一起修
- `K-019` 已修复，但**留观**：`tests/test_p13_agent.py` 的夹具同样没隔离 `config/local.json`，今天替身全覆盖所以无害；哪天真出现「无密钥」用例会变成真出网
- `K-017` 测试必须在授权（非沙箱）下跑，否则 `tmp_path` 不可写会 40+ ERROR；沙箱里跑 `pytest` 还会遇到 `IndexError` 之类的假象，先看是不是这个原因

**其他要留意的**

- P5 手工 8 条验了 7 条半：「删掉 API Key 后提问仍显示结果表」只验到「降级提示 + 页面不崩」——无密钥时根本拿不到 SQL，本来就没有表；「带表降级」由 `tests/test_p4_insight.py:test_ask_keeps_table_when_insight_degrades` 覆盖
- 本机 `config/local.json` 已有页面写入的密钥（profile `local`，`deepseek-flash`）；`.gitignore` 挡住，不进仓库
- `data/` 里有烟测留下的数据集 `d_fc466e97`（dirty.csv，3 行 × 5 列）和 `data/uvicorn.*.log`，都被 gitignore，但页面数据集下拉里会看到
- 依赖版本很新（pandas 3.0.6、duckdb 1.5.5、openai 3.16.2、fastapi 0.141.1、pydantic 2.13.5）

## 3. 下一步要做的第一件事（精确到文件与命令）

按台账「已拍板决策」里的排期，**先清 A 类零风险项**（只做新建文件 / 新路由 / 编排层插调用）：

1. `K-005`：`app/main.py` 的 `ask()` 里把任务落 `tasks` 表（`app/store.py` 加写入函数，签名不改老的）
2. `K-015`：上传文件清理 + 删除数据集端点（`app/store.py` 加删除函数、`app/main.py` 加路由；删除/重导入的正式端点归 P20，这里只做能收拾自己垃圾的最小版）

流程照旧：开工前回归 → 只改设计文档「改动范围」内的文件 → 跑对应测试 → 全量回归必绿 → 更新台账 → 提交推送。

B 类**必须先回设计文档登记、再同步改验收命令**：`K-013` 请求体改 pydantic、`K-002` 守卫按引号切分、`K-006` 给 `db.describe` 加 `timeout_s`、`K-008` 调 `max_rows_per_step`、`K-009` + `K-018` 给 Insight 加引用字段。建议顺序：K-002 → K-013 → K-006 → K-008 → K-009/K-018。

C 类不做，按设计 §6 路线表接：`K-010`/`K-011` 归 P11、`K-012` 归 P10、`K-014` 归 P16/P21、`K-016` 归 P21。

## 4. 需要人拍板的问题

- 要不要顺手给 `tests/test_p13_agent.py` 的夹具补一行 `LOCAL_SETTINGS` 隔离（防 K-019 同类复发，一行改动）
- B 类里 `K-009`/`K-018` 要给 `Insight` 加引用字段（改 P4 契约、要同步改 `test_p4_insight.py` 与 `test_p5_flow.py`），是否现在就做
- 「删密钥后仍显示结果表」要不要补一次真手工确认（需要临时换成一个坏密钥，再把密钥填回去）

## 5. 环境事实（换机器时需要）

- Python：`C:\Users\14778\AppData\Local\Programs\Python\Python312\python.exe`；项目虚拟环境 `.venv`
- 依赖：`requirements.txt` 已装齐（P5 没加新依赖，前端用本地文件）
- 代理 `http://127.0.0.1:7897` 已写进仓库本地 git 配置；`pip install` 与下载要手动带代理
- 本机沙箱：命令默认被拦；已批准前缀 `.venv\Scripts\python.exe -m pytest`。**测试必须走授权运行**
- 改文件用：`& 'C:\Users\14778\AppData\Local\OpenAI\Codex\bin\cdef5aaf3e41ab53\codex.exe' --codex-run-as-apply-patch $patch`（一个 `@@` 一段更稳）；新建文件用 `[System.IO.File]::WriteAllText($path, $text, (New-Object System.Text.UTF8Encoding($false)))`
- 起服务看页面：`& .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8017`，浏览器开 `http://127.0.0.1:8017/`

## 6. 建议下一个 agent 调用的技能

- `tdd`：改测试/加测试时先写断言
- `implement`：按设计文档施工
- `handoff`：再次交接时用它
- 已常驻、不必显式调用：`ponytail`、`benjamin-plus`

## 7. 必须先读的文件

1. `AGENTS.md`（自动加载）
2. `docs/索引.md`
3. `docs/代码台账.md`（已完成文件、K-002~K-019、已拍板决策）
4. `docs/最小可用集-系统设计.md`（§3 契约、§5 扩展契约、§6 路线表、§7 刻意不做）
5. `docs/最小可用集-测试清单.md`（逐段测试项与 DoD）
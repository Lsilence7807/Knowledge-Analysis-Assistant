# 交接文档 · 2026-09-20（第十二次交接：约束层审计与瘦身；本轮零代码改动，接着开 P9）

给下一个会话用；同一天再交接就地改写本文件，旧版本在 git 历史里。**本次交接的目的：新开窗口接着做 §5 的 P9 多模型接入。排期口径不变：网页端优先（P9→P10→P11 及后续非桌面阶段），P12 桌面打包留空。**
状态：**MVP 七段 + P6 技能 + P7 知识库 + P15 沙箱与指标 + P17 流式 + P8 MCP + P21 性能契约 + 台账补丁 + 手工复验 + 文档压缩 + P5.1/P5.2 + 收尾体检 + K-028 + P6/P7 真模型手工项 + gh 认证桥接 + 路径约束 + 约束层审计**全部完成；工作区干净、`main` 与远端同步；基线 `pytest tests -q` → **164 passed**，ruff 干净。

## 1. 已完成阶段与提交号

- 七段验收：P0 `3383821`、P1 `1d9ab68`、P2 `89826d0`、P3 `7e7fc82`、P13 `7e27c31`、P4 `bb75cb8`、P5 `18b2482`。
- 近期：P21 `1ab1428`、CI `3762671`→`e8c76b8`、文档对齐 `2e626ce`、P5.2 `8f8e44a`/`88a30b6`、全局收尾 `23f66e3`。
- P6：`53995a5`/`8a445a1`/`9cb8464`/`d1edc11`；K-028 `70c05f1`。P7：`39f48a8`/`3ecf7a9`/`91be3fe`/`b4f62c5`。
- 并行约定与交接：`f0e35e6`（第十次交接）、`b60635c`（并行约定）、`2275a6b`（索引加行）；三窗口代码 P15 `978c40f`+合并 `150f454`、P17 `66a7515`+合并 `6821a6b`、P8 `e7fd65d`+合并 `a7620e9`；登记 `e12f56c`、交接 `09257da`。
- 本次三轮（均为文档/本机约束，`app/` 与 `tests/` 零改动）：gh 认证桥接 `0ce3711`、路径约束 `fbec3d3`、约束层消歧与瘦身 `1676e27`。
- 基线：`pytest tests -q` → **164 passed**，分段用例数 P0 5 / P1 8 / P2 22 / P3 12 / P13 14 / P4 13 / P5 6 / P6 15 / P7 14 / P15 29 / P17 8 / P8 12 / P21 6（`--collect-only` 实测，与文档一致）；`ruff check` + `ruff format --check` 干净；CI 最近一次 run 35514338520 `success`。

## 2. 未完成、已知损坏与坑

- **本轮零代码改动**：164 passed 与上一次是同一份 `app/`；改的只是仓库文档与机器上的约束/技能文件。
- **约束层权威地图（本轮新立，别踩）**：全局常驻 `%USERPROFILE%\.codex\AGENTS.md`；技能正文的**源**在 `%USERPROFILE%\Documents\skill\`（`skill-router\sync.ps1` 把它装进 `~/.agents/skills`，**直接改副本会被覆盖**）；插件缓存 `%USERPROFILE%\.codex\plugins\cache\` 由应用管理，不要手改。改技能 = 改源 + 跑 `sync.ps1`。
- **全局文件无版本控制**：本轮改动前的备份在 `%USERPROFILE%\.agents\_backup-2026-09-20\`（7 份，含 `codex-AGENTS.md`、两个 skill-router 副本、handoff/implement 的 SKILL.md、仓库 AGENTS.md 与并行开发.md）。
- 本轮动过的全局文件：`.codex\AGENTS.md`（新增「改约束文件前先答两个问题」块 + 技能文件地图）、`skill-router\SKILL.md`（源，9973→4613 B，副本已用 `sync.ps1` 同步、两份 SHA256 一致）、`.agents\skills\handoff\SKILL.md`（加「仓库流程优先」）、`implement\SKILL.md`（去掉不存在的 `/tdd`、`/code-review` 与 typecheck）。
- 技能审计姿势：`%USERPROFILE%\Documents\skill\agent-config\audit-skills.ps1` → frontmatter 问题 0、重名 0 组、断链 7（**全是误报**：文档里的示例文件名与仓库相对路径）。
- **K-034 仍在**：`insight` 段拿不到 §4.5 承诺的 KB 片段与技能正文（同一次 `/ask` 里 agent 引用出处、insight 却写「未检索到口径说明」）；与 K-012（`prior` 恒空）同处修，归 P10。
- 未闭环 K 支线：K-025→P9、K-012 与 K-034→P10、K-010/K-011→P11、K-024/K-026 残余与 K-031/K-033/K-035→P16、K-032→P14；K-029/K-030 是设计取舍；K-036/K-037 留档；本轮新记 K-038（已解决：约束文件散在多处且副本会被同步覆盖，现已写明权威与同步方式）。
- 本机坑照旧：`apply_patch` 不可用（用 Python 写文件，`newline=""`）；PowerShell 里 `curl.exe -d "{\"k\":\"v\"}"` 会把 body 拆坏（用单引号包）；看中文先 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`；写文件必须 `newline=""`（否则 LF 变 CRLF），`.cmd` 必须 CRLF；测试要在真实文件系统里跑（K-017）；`config/local.json` 有密钥，gitignore 挡住别提交。
- gh 认证：`gh auth login` 仍不可用（GCM 里的 PAT 缺 `read:org`），但 pwsh 已被 profile 桥接（细节见 §5），`gh run list` 直接能用。
- 可选清理（本轮**没做**，怕删错）：并行三窗口的 worktree 还在 `%USERPROFILE%\.codex\worktrees\`（`git worktree list` 查，分支已合），要清就 `git worktree remove <路径>`。

## 3. 下一步第一件事

**开 P9 多模型接入**（前置 P8 已完成；`系统总体设计.md` §5 P9 行是权威施工单）。

改动范围：新建 `tests/test_p9_models.py`；→ `app/models.py` 加多适配器与回退、`config/models.json` 扩 profile 段（`purpose` 取值要真生效，清 K-025）、`app/llm.py` 加适配器分发与回退、`app/main.py` 加 `/models`。

要点：两个适配器（`openai_compatible` / `anthropic`）；`model_id` 可逐次指定；失败按 `fallback` 链重试；未配密钥的模型标 `unavailable`；`purpose` 生效后 SQL 用小模型、结论用强模型。

```
& .\.venv\Scripts\python.exe -m pytest tests/test_p9_models.py -q     # P9 验收
& .\.venv\Scripts\python.exe -m pytest tests -q                       # 全量回归（现 164 passed）
& .\.venv\Scripts\python.exe -m ruff check app tests
& .\.venv\Scripts\python.exe -m ruff format --check app tests
```

反例照 `系统测试清单.md` §2 的 P9 行：主模型 500 / 超时 / 非法 JSON / 空响应 / 全挂五种都有确定行为（全挂 → HTTP 200 + `degraded`）；手工项 `/models` 与实际可用模型一致。退化：回落单模型行为。提交格式 `P9 多模型：验收 test_p9_models 通过`。

## 4. 需要人拍板的问题

- **要不要继续用并行窗口**：P9 之后 `P18 后台作业`（只前置 P17，已完成）可与 P10 并行。要开就照 `docs/并行开发.md` 再来一轮。
- **K-034 怎么排**：跟 P10 一起修（默认），还是插一小段先清（要动 `insight.summarize` 入参并同步 P4 验收命令）。
- 桌面 exe（P12）是否提前：默认不改计划（设计明确不做代码签名，会有 SmartScreen 警告）。
- 备份目录 `%USERPROFILE%\.agents\_backup-2026-09-20\` 留还是删：默认留到下次改这些文件时再清。
- 界面遗留手工项：起服务后确认图表取色与描边没变。
- 顺序默认按链走：P9 → P10（清 K-012/K-034）→ P11（清 K-010/K-011），之后 P14/P16/P18/P19/P20 这些非桌面阶段。

## 5. 环境事实

- 仓库 `%USERPROFILE%\projects\Knowledge-Analysis-Assistant`，分支 `main`，远端公开（`Lsilence7807/Knowledge-Analysis-Assistant`）；Python 3.12 + `.venv`，依赖已装齐（`requirements.lock` 里有 `mcp==2.2.0`）。
- 代理 `http://127.0.0.1:7897` 只在仓库本地 git 配置里（影响本机 git/pip，与 CI runner 无关）。
- 质量门唯一配置源 `pyproject.toml`（ruff 行宽 120、target py312、`pythonpath=["."]`）。
- 路径口径（仓库 `AGENTS.md` §1「路径规则」）：一律锚 `app/config.py` 的 `BASE_DIR`，禁硬编码绝对路径与 cwd 依赖；抽查命令在那节里，实测无命中。
- 起服务：双击 `启动.cmd`，或 `& .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8017`，页面 `http://127.0.0.1:8017/`。开关：`ENABLE_SKILLS` / `ENABLE_KB` / `ENABLE_MCP` / `ENABLE_SANDBOX` / `ENABLE_STREAM`（都默认 false，`ENABLE_AGENT` 默认 true）。
- 文档地图与省 token 读法见 `docs/索引.md`；开工前必读顺序：`docs/索引.md` → `docs/代码台账.md` → 本次要施工的设计文档（§5 定位阶段 → §4 对应契约小节）。
- **CI 查询姿势（已自动化）**：gh 2.101.0 装在 `C:\Program Files\GitHub CLI\gh.exe`（不在 PATH）；GCM 里那个 40 位 classic PAT 只有 `gist, repo, workflow`，缺 `read:org`，`gh auth login` 仍会被拒——不用折腾。`%USERPROFILE%\Documents\PowerShell\Microsoft.PowerShell_profile.ps1` 已做桥接：会话级补 PATH + 调 `gh` 时临时从 GCM 取 token 塞 `GH_TOKEN`（不落盘、不打印、用完即撤）。任何 pwsh 直接：

```
gh run list --limit 5
```

  非 pwsh 环境才需要手动一行（单引号里 `` `n `` 不转义，要用双引号）：

```
$env:GH_TOKEN = ((("protocol=https`nhost=github.com`n") | git credential fill) | Where-Object { $_ -like 'password=*' }) -replace '^password=', ''
```

## 6. 建议技能与参考

- P9 建议 `implement` 或 `tdd`（先写用例，用 mock server 覆盖两个适配器与回退）；改完 `code-review` 自审；收尾用 `verification-before-completion` 跑 §3 的命令，再按 `session-protocol` 走收尾六步。
- 契约：`系统总体设计.md` §4.6（模型接入契约与 `config/models.json`）、§4.5（LLM 契约，`purpose` 与回退）、§5（P9 行 + 依赖图）、§10（环境变量与质量门）；K-025 就是本段要清的账。
- 改约束/技能文件前先读全局 `~/.codex/AGENTS.md` 的「改约束文件前先答两个问题」块；技能路由与开关看 skill `skill-router`（本轮已瘦身到 4.6 KB）。
- 再交接时用 `handoff`（本文件就地改写）。

新会话开场固定用这句：

「读 docs/索引.md、docs/代码台账.md 和最新的 docs/交接-*.md，回答当前状态与下一步，读完之前不要动代码」

# 交接文档 · 2026-09-20（第十次交接：P6/P7 手工项复验完成，接着开 P8 MCP 插件接入）

给下一个会话用；同一天再交接就地改写本文件，旧版本在 git 历史里。**本次交接的目的：新开窗口接着做 §5 的 P8 MCP 插件接入。排期口径已定：网页端优先（P8→P9→P10→P11 及后续非桌面阶段），P12 桌面打包留空。**
状态：**MVP 七段 + P6 技能 + P7 知识库 + P21 性能契约 + 台账补丁 + 手工复验 + 文档压缩两轮 + P5.1/P5.2 + 收尾体检 + K-028 + P6/P7 真模型手工项**全部完成；工作区干净、`main` 与远端同步；基线 `pytest tests -q` → **115 passed**，ruff 干净，GitHub Actions 绿。

## 1. 已完成阶段与提交号

- 七段验收：P0 `3383821`、P1 `1d9ab68`、P2 `89826d0`、P3 `7e7fc82`、P13 `7e27c31`、P4 `bb75cb8`、P5 `18b2482`。
- 近期：P21 `1ab1428`、CI `3762671`→`e8c76b8`、文档对齐 `2e626ce`、P5.2 `8f8e44a`/`88a30b6`、全局收尾流程 `23f66e3`。
- 第十轮 P6：代码 + 测试 + `skills/` 示例 `53995a5`、文档登记 `8a445a1`、收尾体检修复 `9cb8464`、收尾文档 `d1edc11`；K-028 白名单 `70c05f1`。
- 第十一轮 P7：`39f48a8`（交付：`app/providers/kb.py` + `search_kb` + `/kb/*` + 夹具）、`3ecf7a9`（文档登记）；收尾两条 `91be3fe`（清死参）、`b4f62c5`（交接/对齐）。
- 第十二轮（本次，无代码改动）：P6/P7 真模型手工项复验 + 收尾文档，提交号看 `git log -1`。
- 基线：`pytest tests -q` → 115 passed（p0 5 / p1 8 / p2 22 / p3 12 / p13 14 / p4 13 / p5 6 / p6 15 / p7 14 / p21 6）；质量门 `ruff check app tests` + `ruff format --check app tests` 干净；CI 与本地同口径。

## 2. 未完成、已知损坏与坑

- **P6/P7 手工项已完成**（本次同一次 `/ask` 覆盖两项）：`ENABLE_KB=true` + `ENABLE_SKILLS=true` 起服务 → 导入 `tests/fixtures/kb` 与 `skills/` → 提问「华东区 2026Q3 直营渠道销售额比上季度下降多少？按业务口径回答并引用出处」。结果：`steps` = `search_kb`(×2) → `list_skills` → `use_skill`，全 `ok`，`degraded` 空；答案点名 KB 出处 `tests/fixtures/kb/经营口径.md`（万元/一位小数/不做年化 + Q3 未含退货 caveat，并标「不可信资料」），并引用 `demo_skill` 的「>=30 行才下趋势结论」下 caveat。
- **新记 K-034（真缺口，归 P10）**：`insight` 段拿不到 §4.5 承诺的 KB 召回片段与技能正文，于是同一次响应里 agent 引用了 KB 出处、insight 却写「未检索到知识库口径说明」。与 K-012（`prior` 恒空）同一处修；是否在 P8 之前先插一小段清掉，见 §4。
- 未闭环 K 支线：K-025→P9、K-012 与 K-034→P10、K-010/K-011→P11、K-024/K-026 残余与 K-031/K-033→P16、K-032→P14；K-029/K-030 是设计取舍。
- **`apply_patch` 在本机不可用**：`~/.codex/tmp/.../apply_patch.bat` 是批处理垫片，多行补丁会被 cmd 拆坏（报 `The last line of the patch must be '*** End Patch'`）。改文件一律用 Python 写：`Path.write_text(..., newline="")`，每处替换先 `assert text.count(old) == 1`。
- **PowerShell 里别用 `-d "{\"k\":\"v\"}"`**：`\"` 不是转义，body 会被拆坏（报 422 JSON decode error）；用单引号包 body，如 `-d '{"path":"tests/fixtures/kb"}'`。
- 写文件必须 `newline=""`（否则整份文件 LF 变 CRLF）；`.cmd` 必须 CRLF（`.gitattributes` 给 `*.cmd` 开 `eol=crlf`）；测 `启动.cmd` 要把副本放仓库根（脚本用 `%~dp0` 定位项目）。
- 本机测试要在真实文件系统里跑（K-017）；起服务前先看端口（旧进程占着时新进程会悄悄退出，健康检查还通，测的其实是旧代码）；本机 `config/local.json` 有页面写入的密钥，gitignore 挡住别提交（夹具已隔离它，K-019）；改前端别碰 id 与事件绑定，图表取色集中在 `chartTheme`。
- `Start-Process` 起后台服务在本机可能被判策略拦截；稳妥做法是把 uvicorn 跑在长驻会话里，收尾用 `Stop-Process -Id <pid>` 收掉。

## 3. 下一步第一件事

**开 P8 MCP 插件接入**（前置 P7 已完成；`系统总体设计.md` §5 P8 行是权威施工单）。

改动范围：新建 `app/providers/mcp_client.py`、`tests/test_p8_mcp.py`、`config/mcp.json`、`tests/fixtures/mcp_echo_server.py`（20 行 stdio server，测试清单 §1 已点名）；→ `main.py` 加 `/mcp/tools` `/mcp/call`（§4 端点表 P8 行）、`registry.py` 加一行、`tools.py` 加 `call_mcp_tool`（§4.8，默认进 `config/tools.json` 的 `allow`）。

要点：读 `config/mcp.json` 起 stdio server → 列工具 → 白名单调用；server 崩溃或调用超时只降级该能力并回收进程（`/capabilities` 里 `mcp=false`，`/ask` 仍 200）；`ENABLE_MCP` 默认 false，验收时显式打开；MCP 返回内容按 §7 当不可信数据处理。

```
& .\.venv\Scripts\python.exe -m pytest tests/test_p8_mcp.py -q     # P8 验收
& .\.venv\Scripts\python.exe -m pytest tests -q                    # 全量回归（现 115 passed）
& .\.venv\Scripts\python.exe -m ruff check app tests
& .\.venv\Scripts\python.exe -m ruff format --check app tests
```

反例照 `系统测试清单.md` §2 的 P8 行：server 起不来 → `capabilities.mcp=false` 且不出现 500、调用超时降级、白名单外工具被拒、kill server 后进程被回收。手工项（同节）：`/mcp/tools` 显示的是 `config/mcp.json` 里那台 server 的工具。退化：registry 返回 None → 只影响 `/mcp/*`。提交格式 `P8 MCP：验收 test_p8_mcp 通过`。

## 4. 需要人拍板的问题

- **K-034 怎么排**：跟 P10 一起修（默认，与 K-012 同处），还是在 P8 之后插一小段先清（要动 `insight.summarize` 入参并同步 P4 验收命令）。
- 桌面 exe（P12）是否提前：`启动.cmd` 已双击即用但仍需机器有 Python 3.11+；提前做要改 §5 依赖图，且设计明确不做代码签名（会有 SmartScreen 警告）。默认不改计划。
- 顺序默认按链走：P8 → P9（清 K-025）→ P10（清 K-012/K-034）→ P11（清 K-010/K-011），之后 P14/P15/P17/P18/P19/P20 这些非桌面阶段；**P12 桌面打包留空**。
- 界面唯一遗留手工项：起服务后确认图表取色与描边没变（改色板时带了旧值兜底）。

## 5. 环境事实

- 仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，远端公开（`Lsilence7807/Knowledge-Analysis-Assistant`）；Python 3.12 + `.venv`，依赖已装齐（复现用 `requirements.lock`）；P8 要用的 `mcp==2.2.0` 已在 lock 里且装好，不用额外装。
- 代理 `http://127.0.0.1:7897` 只在仓库本地 git 配置里（影响本机 git/pip，与 CI runner 无关）。
- 质量门唯一配置源 `pyproject.toml`（ruff 行宽 120、target py312、`pythonpath=["."]`）。
- 起服务：双击 `启动.cmd`，或 `& .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8017`，页面 `http://127.0.0.1:8017/`。开技能设 `ENABLE_SKILLS=true`（子进程超时 `SKILL_TIMEOUT_S`，默认 20s）、开知识库设 `ENABLE_KB=true`。
- 文档地图与省 token 读法见 `docs/索引.md`；开工前必读顺序：`docs/索引.md` → `docs/代码台账.md` → 本次要施工的设计文档（§5 定位阶段 → §4 对应契约小节）。

## 6. 建议技能与参考

- P8 建议 `implement` 或 `tdd`（先写用例）；改完 `code-review` 自审；收尾用 `verification-before-completion` 跑一遍 §3 的命令，再按 `session-protocol` 走收尾六步。
- 契约：`系统总体设计.md` §4.7（能力与端点表里的 `/mcp/*`）、§4.8（`call_mcp_tool`）、§5（P8 行 + 依赖图）、§6（隔离机制，MCP server 是子进程）、§7（MCP 返回内容按不可信处理）、§9（依赖 `mcp`）、§10（`ENABLE_MCP` 默认 false）。
- 再交接时用 `handoff`（本文件就地改写）。

新会话开场固定用这句：

「读 docs/索引.md、docs/代码台账.md 和最新的 docs/交接-*.md，回答当前状态与下一步，读完之前不要动代码」

# 交接文档 · 2026-09-20（第九次交接：P6 技能 + P7 知识库完成，接着开 P8 MCP 插件接入）

给下一个会话用；同一天再交接就地改写本文件，旧版本在 git 历史里。**本次交接的目的：新开窗口接着做 §5 的 P8 MCP 插件接入。排期口径已定：网页端优先（P8→P9→P10→P11 及后续非桌面阶段），P12 桌面打包留空。**
状态：**MVP 七段 + P6 技能 + P7 知识库 + P21 性能契约 + 台账补丁 + 手工复验 + 文档压缩两轮 + P5.1/P5.2 + 收尾体检 + K-028**全部完成；工作区干净、`main` 与远端同步；基线 `pytest tests -q` → **115 passed**，ruff 干净，GitHub Actions 绿。

## 1. 已完成阶段与提交号

- 七段验收：P0 `3383821`、P1 `1d9ab68`、P2 `89826d0`、P3 `7e7fc82`、P13 `7e27c31`、P4 `bb75cb8`、P5 `18b2482`。
- 近期：P21 `1ab1428`、CI `3762671`→`e8c76b8`、文档对齐 `2e626ce`、P5.2 `8f8e44a`/`88a30b6`、全局收尾流程 `23f66e3`。
- 第十轮 P6：代码 + 测试 + `skills/` 示例 `53995a5`、文档登记 `8a445a1`、收尾体检修复 `9cb8464`、收尾文档 `d1edc11`；K-028 白名单 `70c05f1`；
- 第十一轮 P7：`39f48a8`（交付：`app/providers/kb.py` + `search_kb` + `/kb/*` + 夹具）、`3ecf7a9`（文档登记）；本轮收尾的两条（代码清死参 + 交接/对齐）见 `git log`。
- 基线：`pytest tests -q` → 115 passed（p0 5 / p1 8 / p2 22 / p3 12 / p13 14 / p4 13 / p5 6 / p6 15 / p7 14 / p21 6）；质量门 `ruff check app tests` + `ruff format --check app tests` 干净；CI 与本地同口径。

## 2. 未完成、已知损坏与坑

- **P7 手工项（这次要跑的那件；代码已就绪，验的是你机器上的真实服务）**：起服务必须带 `ENABLE_KB=true`，否则 `/kb/*` 一律 503；前端还没有知识库面板，所以走接口：

  ```
  Get-NetTCPConnection -LocalPort 8017 -State Listen        # 先确认端口没被旧进程占着
  $env:ENABLE_KB="true"; & .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8017
  curl.exe -s -X POST http://127.0.0.1:8017/kb/import -H "Content-Type: application/json" -d "{\"path\":\"tests/fixtures/kb\"}"
  curl.exe -s -X POST http://127.0.0.1:8017/kb/search -H "Content-Type: application/json" -d "{\"query\":\"直营渠道承压\"}"
  ```

  判定：`imported` 里有 `tests/fixtures/kb/经营口径.md`；`/kb/search` 命中同一条，`fragment` 带出处与「不可信」标记。可选的真模型链路：上传数据集后提问，看步骤面板里有没有 `search_kb` 这一步（模型不一定自己调）。**P6 的手工项同样没做**（真模型下确认 prompt 片段进上下文；开技能要 `ENABLE_SKILLS=true`）。
- 未闭环 K 支线各归所属阶段：`K-025`→P9、`K-012`→P10、`K-010`/`K-011`→P11、`K-024` 与 `K-026` 残余→P16、`K-032`→P14、`K-033`→P16（与 `K-031` 同类）；P6 的 `K-028` 已修，`K-029`/`K-030` 是设计取舍。
- **`apply_patch` 在本机不可用**：`~/.codex/tmp/.../apply_patch.bat` 是批处理垫片，多行补丁会被 cmd 拆坏（报 `The last line of the patch must be '*** End Patch'`）。改文件一律用 Python 写：`Path.write_text(..., newline="")`，每处替换先 `assert text.count(old) == 1`（多组替换别用 PowerShell 数组展平）。
- 写文件必须 `newline=""`：Windows 上 `write_text` 默认把整份文件的 LF 变 CRLF。
- `.cmd` 必须 CRLF：`.gitattributes` 给 `*.cmd` 开了 `eol=crlf`；测 `启动.cmd` 要把副本放**仓库根**（脚本用 `%~dp0` 定位项目）。
- 本机测试要在真实文件系统里跑（K-017）；起服务前先看端口（旧进程占着时新进程会悄悄退出，健康检查还通，测的其实是旧代码）。真链路排查用 `curl.exe`；上传响应表名是 `table` 不是 `table_name`。
- 本机 `config/local.json` 有页面写入的密钥，gitignore 挡住别提交；测试夹具已隔离它（K-019）。
- 改前端别碰 id 与事件绑定；图表取色集中在 `chartTheme`（读 `:root` 变量）。

## 3. 下一步第一件事

**先跑上面的 P7 手工项**（不改代码，十分钟内能完）；跑完就**开 P8 MCP 插件接入**（前置 P7 已完成；§5 P8 行是权威施工单）。

改动范围：新建 `app/providers/mcp_client.py`、`tests/test_p8_mcp.py`、`config/mcp.json`、`tests/fixtures/mcp_echo_server.py`；→ `main.py` 加 `/mcp/*`、`registry.py` 加一行、`tools.py` 加 `call_mcp_tool`（按 P7 登记②的口径默认进 `config/tools.json` 的 `allow`）。

要点：读 `config/mcp.json` 起 stdio server → 列工具 → 白名单调用；server 崩溃或调用超时只降级该能力并回收进程（`/capabilities` 里 `mcp=false`，`/ask` 仍 200）；`ENABLE_MCP` 默认 false，验收时显式打开。

```
& .\.venv\Scripts\python.exe -m pytest tests/test_p8_mcp.py -q     # P8 验收
& .\.venv\Scripts\python.exe -m pytest tests -q                    # 全量回归（现 115 passed）
& .\.venv\Scripts\python.exe -m ruff check app tests
& .\.venv\Scripts\python.exe -m ruff format --check app tests
```

反例照 `系统测试清单.md` §2 的 P8 行：server 起不来 → `capabilities.mcp=false` 且不出现 500、调用超时降级、白名单外工具被拒、kill server 后进程被回收。退化：registry 返回 None → 只影响 `/mcp/*`。提交格式 `P8 MCP：验收 test_p8_mcp 通过`。

## 4. 需要人拍板的问题

- ~~技能工具进默认白名单~~ 已拍板并落地（K-028 已修复：`allow` 含 `list_skills`/`use_skill`，P13 断言同步）。
- 桌面 exe（P12）是否提前：`启动.cmd` 已双击即用但仍需机器有 Python 3.11+；提前做要改 §5 依赖图，且设计明确不做代码签名（会有 SmartScreen 警告）。默认不改计划。
- 顺序默认按链走：P8 → P9（清 K-025）→ P10（清 K-012）→ P11（清 K-010/K-011），之后 P14/P15/P17/P18/P19/P20 这些非桌面阶段；**P12 桌面打包留空**（2026-09-20 拍板：网页端优先，桌面复用靠 §6.6 双壳同源，P12 只加配置根目录切换与打包脚本）。
- 界面唯一遗留手工项：起服务后确认图表取色与描边没变（改色板时带了旧值兜底）。

## 5. 环境事实

- 仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，远端公开（`Lsilence7807/Knowledge-Analysis-Assistant`）；Python 3.12 + `.venv`，依赖已装齐（复现用 `requirements.lock`）。
- 代理 `http://127.0.0.1:7897` 只在仓库本地 git 配置里（影响本机 git/pip，与 CI runner 无关）。
- 质量门唯一配置源 `pyproject.toml`（ruff 行宽 120、target py312、`pythonpath=["."]`）。
- 起服务：双击 `启动.cmd`，或 `& .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8017`，页面 `http://127.0.0.1:8017/`。开技能要设 `ENABLE_SKILLS=true`（子进程超时用 `SKILL_TIMEOUT_S`，默认 20s）。
- 文档地图与省 token 读法见 `docs/索引.md`；开工前必读顺序：`docs/索引.md` → `docs/代码台账.md` → 本次要施工的设计文档（§5 定位阶段 → §4 对应契约小节）。

## 6. 建议技能与参考

- P7 建议 `implement` 或 `tdd`（先写用例）；改完 `code-review` 自审；收尾用 `verification-before-completion` 跑一遍 §3 的命令，再按 `session-protocol` 走收尾六步。
- 契约：`系统总体设计.md` §4.4（`kb_docs` 表）、§4.5（上下文里的知识库片段与出处）、§4.8（`search_kb`）、§5（P7 行 + 依赖图）、§6（隔离机制）、§7（注入与安全边界）。
- 再交接时用 `handoff`（本文件就地改写）。

新会话开场固定用这句：

「读 docs/索引.md、docs/代码台账.md 和最新的 docs/交接-*.md，回答当前状态与下一步，读完之前不要动代码」

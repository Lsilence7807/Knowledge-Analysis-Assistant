# 交接文档 · 2026-09-20（P4 完成后改写）

给下一个会话用。目标：接着做 MVP 的 P5 前端闭环（MVP 最后一段）。

说明：`handoff` 技能默认把交接文档写到系统临时目录；本项目按 `AGENTS.md` 第 6 节写进仓库并提交，换会话、换电脑都能拿到。同一天再次交接就就地改写本文件，旧版本在 git 历史里。

## 1. 已完成阶段与对应提交号

仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，工作区干净；本交接文档自身的提交看 `git log -1`。

| 阶段 | 提交 | 验收 |
|---|---|---|
| P0 骨架与契约冻结 | `3383821`、补丁 `52501f0` | `tests/test_p0_skeleton.py` 5 passed |
| P1 数据摄入 | `1d9ab68` | `tests/test_p1_ingest.py` 11 passed |
| P2 查询与统计层 | `89826d0` | `tests/test_p2_sql.py` 19 passed |
| P3 自然语言转 SQL | `7e7fc82`，补丁 `c555283`、`d452841`、`b610810` | `tests/test_p3_nlu.py` 11 passed |
| P13 Agent 循环与工具调用 | `7e27c31` | `tests/test_p13_agent.py` 11 passed |
| P4 洞察生成 | `bb75cb8` | `tests/test_p4_insight.py` 8 passed |
| 决定登记与文档对齐 | `1fe3adf`、`9ae6d19`、`f27fa93`、`d328c8a`、`ab8ca3b`、`3065722`、`2d8635d` | docs only |

全量回归：`& .\.venv\Scripts\python.exe -m pytest tests -q` → **60 passed, 1 warning**（警告来自 Starlette 依赖，不是本项目代码）。

P4 落地的形状（P5 要照着渲染）：

- 新建 `app/insight.py`（`summarize(question, result, profile, prior) -> Insight`、`PROMPT_VERSION="p4-1"`、限 50 行喂表、数字回溯写 caveats、失败抛 `InsightError`，其 `.code` 是 `insight:llm` 或 `insight:contract`）
- `app/schemas.py` 加 `Insight` / `Finding` / `Suggestion`（字段全部必填，缺字段即不合契约 → 重试 1 次 → 降级）
- `app/main.py` 加 `POST /insight`（body：`dataset_id` + `question` + `sql`，执行 SQL 后出结论）与 `_attach_insight()`；`/ask` 的 agent 与 P3 两条路径都会补 `insight`
- `app/registry.py` 注册 `insight` 能力（`/capabilities` 现在 5 个能力都可为 true）
- `/ask`、`/insight` 响应：`insight` 为空（没行或降级）或完整对象；降级只追加 `degraded`（`insight:llm` / `insight:contract`），表格与 SQL 永不受影响

## 2. 未完成、已知损坏或有坑的地方

- `K-002`：`_guard` 一刀切，字符串字面量里出现 `--`、`/*`、`;` 也会被拒（`SELECT 'a;b'` 跑不了）。设计取舍。
- `K-004` / `K-007`：真实模型链路从未验证（无密钥）。P3（`_complete`）、P13（`chat_tools`）、P4（insight）全走替身。**P5 页面配好密钥后一次把三件事都手工验掉**：单跳、多跳工具调用、insight。
- `K-005`：`/ask`（含 agent 路径）不落 `tasks` 表。归 P16。
- `K-006`：`describe_stats` / `detect_anomalies` 没有独立超时，实际走 `db.describe` 的 10s。
- `K-008`：提问最后一步行数上限 200（`max_rows_per_step`），比 `/query` 的 5000 小。
- `K-009`：数字回溯只认字面数字，模型算出来的汇总值会被写进 `caveats`，数字多的结论 caveats 偏吵。前端要把 caveats 单独展示，别当错误。
- 结果表 0 行时不调模型、`insight` 为 null 且不记 degraded（设计没写这条，是 P4 的取舍）。
- `/query`、`/stats`、`/ask`、`/insight` 的请求体都是裸 `dict`（`Body(...)`）：字段错了是 400 或默认值，不是 422。前端要自己兜（校验 + 提示）。
- 前端拿到的东西：`steps`（`n/tool/args_digest/ok/ms/rows`，失败步骤多一个 `error`）、`insight`（`summary/findings/anomalies/suggestions/confidence/caveats`）、`degraded`（`query:llm` / `agent:llm` / `agent:max_steps` / `insight:llm` / `insight:contract`）、`message`、`truncated`、`hint`。
- `web/` 目录还没建，`web/vendor/echarts.min.js` 需要下载（已拍板走本地文件、不走 CDN、缺失时页面只出表格不报错）。

## 3. 下一步要做的第一件事（精确到文件与命令）

做 **P5 前端闭环**（MVP 最后一段），依据 `docs/最小可用集-系统设计.md` §3.2（`GET/PUT /settings/models` 的形状）、P5 段，以及 `docs/最小可用集-测试清单.md` P5 段（自动 3 条 + 手工 8 条）：

- 开工前回归：`& .\.venv\Scripts\python.exe -m pytest tests -q`（应为 60 passed）
- 新建：`web/index.html`（单文件，原生 JS，不用构建工具）、`tests/test_p5_flow.py`、`web/vendor/echarts.min.js`（下载，见第 4 节）
- 允许改：`app/main.py` 加静态挂载（`StaticFiles` 挂 `web/`，`/` 返回 index.html）与 `GET/PUT /settings/models` 两个路由；`app/config.py` 加 `save_settings()`（合并写 `config/local.json`，密钥不进 git）
- 复用现成件：读取侧已就绪——`models.list_models()` 会合并 `local.json` 的 `models`，`config.api_key(profile)` 按「本地文件 → profile 的 `api_key_env` → 通用 `LLM_API_KEY`」解析，调用时读文件所以保存即生效；`PUT /settings/models` 只要写盘即可
- 页面要求的元素（对应手工清单）：上传框、数据集下拉（`GET /datasets`）、提问框、SQL 折叠区、结果表、步骤面板（`steps`）、结论卡片（`insight`）、图表（ECharts，数据与表格一致）、模型配置表单、降级提示条（`degraded` + `message` 可读展示）
- 测试口径：`GET /settings/models` 不回传密钥明文（只回 `has_key` 之类布尔）；`PUT` 写入后 `/ask` 走新 profile 且不降级；模型调用一律替身（`llm._complete` / `llm.chat_tools`），不真出网；`tmp_path` + `monkeypatch` 把 `config.LOCAL_SETTINGS` 指到临时文件（`tests/test_p4_insight.py` 的夹具可直接照抄）
- 验收：`& .\.venv\Scripts\python.exe -m pytest tests/test_p5_flow.py -q`，然后全量回归；浏览器手测跑第 3 节测试清单里的 8 条
- 范围外：不引入打包工具/框架、不做桌面 exe（P11/P12）、不做会话记忆（P10）
- 收尾：更新 `docs/代码台账.md` → 提交（`P5 前端闭环：验收 test_p5_flow 通过`）→ 推送 → MVP 七段齐了，再交接一次（后续阶段按全量文档接，见 `docs/系统总体设计.md` 第 5、6 节）

## 4. 需要人拍板的问题

- `web/vendor/echarts.min.js` 从哪来：本机沙箱默认拦网，要下载约 1MB 的 ECharts 单文件并提交进仓库（已拍板「落本地、不走 CDN」）。默认做法：用已批准的代理（`http.proxy=http://127.0.0.1:7897`）下载 `echarts.min.js` 到 `web/vendor/` 并提交。若不想联网，就按「缺文件只出表格」降级，把图表留到以后。

## 5. 环境事实（换机器时需要）

- Python：`C:\Users\14778\AppData\Local\Programs\Python\Python312\python.exe`；项目虚拟环境 `.venv`
- 依赖：`requirements.txt`，已装齐并验证可导入（前端不需要新依赖）
- 代理 `http://127.0.0.1:7897` 已写进仓库本地 git 配置（`http.proxy` / `https.proxy`），`git push` 直接跑；`pip install` 与下载都要手动带代理
- 本机沙箱：命令默认被拦。已批准前缀 `…\.venv\Scripts\python.exe -m pytest`、`git add`、`git push`；`git commit` 若报 `.git/index.lock: Permission denied`，加 `sandbox_permissions=require_escalated` 重跑同一条命令。**测试必须走授权运行**，否则 `tmp_path` 因系统临时目录不可写直接 PermissionError（表现是几十个 ERROR 而不是 FAILED）
- 改文件用：`& 'C:\Users\14778\AppData\Local\OpenAI\Codex\bin\cdef5aaf3e41ab53\codex.exe' --codex-run-as-apply-patch $patch`（`$patch` 是 PowerShell here-string）。已知坑：**同一个 patch 里多个 `@@` 段必须按文件行号从前往后排**，并且不要把前一段改过的行当后一段的上下文；稳妥做法是一个 `@@` 一段、分多次 patch 调用（本会话踩过两次）。整个 patch 是原子的，失败不留半截改动
- 新建文件可用 `[System.IO.File]::WriteAllText($path, $text, (New-Object System.Text.UTF8Encoding($false)))` 写 UTF-8 无 BOM

## 6. 建议下一个 agent 调用的技能

- `tdd`：先写 `tests/test_p5_flow.py` 再实现路由
- `implement`：按设计文档 P5 范围施工
- `handoff`：再次交接时用它
- 已常驻、不必显式调用：`ponytail`、`benjamin-plus`

## 7. 必须先读的文件

1. `AGENTS.md`（自动加载）
2. `docs/索引.md`
3. `docs/最小可用集-系统设计.md`（§3.2 HTTP API、P5 段）
4. `docs/代码台账.md`（已完成文件、K-002~K-009、已拍板决策）
5. `docs/最小可用集-测试清单.md`（P5 段的自动 3 条 + 手工 8 条）
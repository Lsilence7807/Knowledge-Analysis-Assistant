# 交接文档 · 2026-09-20（第七次交接：文档压缩完成，接着开 P6）

给下一个会话用；同一天再交接就地改写本文件，旧版本在 git 历史里。**本次交接的目的：新开窗口接着做 §3 的 P6 Skill 导入与执行。**
状态：**MVP 七段 + 台账补丁 + 手工复验 + 文档压缩两轮 + P5.1/P5.2 + 真实链路复验 + ① 锁依赖 + ② CI + ③ P21 + K-023 全部完成**；工作区干净、`main` 与远端同步；基线 `pytest tests -q` → **86 passed**，ruff 干净，GitHub Actions 绿。

## 1. 已完成阶段与提交号

- 七段验收：P0 `3383821`、P1 `1d9ab68`、P2 `89826d0`、P3 `7e7fc82`、P13 `7e27c31`、P4 `bb75cb8`、P5 `18b2482`。
- 上一轮的三个提交：K-023 补丁 `7d76772`、文档对齐 `2e626ce`、P5.2 一键启动 `8f8e44a` + 登记 `88a30b6`。
- 更早的近期提交（① 锁依赖 `bc5f80a`、② CI `691cd95`→`e8c76b8`、③ P21 `1ab1428`、台账 `ea25f84`）明细看 `git log`。
- 基线：`pytest tests -q` → 86 passed（p0 5 / p1 8 / p2 22 / p3 12 / p13 14 / p4 13 / p5 6 / p21 6）；质量门 `ruff check app tests` + `ruff format --check app tests` 干净；CI 与本地同口径。
- 本轮（第八轮）只动文档，没动契约：设计 §5 把 10 个已完成阶段收成一张表（未开工的 17 行施工单原样保留）、测试清单已完成段收成索引表、功能介绍按 6 个主题合并、台账叙述收口并把「已修复」收成一行结论。压缩前后做了标记核对（阶段集合 / §4 小节 / 24 条 ADR / 31 个端点 / 17 张表 / 16 个环境变量 / 25 个测试文件与命令 / 12 专项 / 回归矩阵 / 功能词）逐项一致，**没有信息丢失**。

## 2. 未完成、已知损坏与坑

- 未闭环 K 支线各归所属阶段：`K-025`→P9、`K-012`→P10、`K-010`/`K-011`→P11、`K-024` 与 `K-026` 残余→P16。
- **写仓库里已存在的文件要「完全访问权限」**：沙箱模式下 `docs/*.md`、`app/*.py`、`.github/*` 一律 `Access denied`，`apply_patch` 同样写不进去；新建文件不受影响。
- **写文件必须 `newline=""`**（本轮踩过：`Path.write_text` 在 Windows 会把整份文件的 LF 变成 CRLF）。多组替换用 Python（PowerShell 数组展平会把整份文档按字符替换），每处替换先 `assert count == 1`。
- **`.cmd` 必须 CRLF**：`.gitattributes` 已给 `*.cmd` 开 `eol=crlf`（LF-only 批处理里 `goto :label` 失效）。测 `启动.cmd` 要把副本放**仓库根**（脚本用 `%~dp0` 定位项目），放 `%TEMP%` 会在临时目录建 `.venv` 并报找不到 requirements。
- 本机测试要在真实文件系统里跑（K-017）：沙箱内 `pytest` 会因 `tmp_path` 不可写大量报错；CI 不受影响。
- 起服务前先看端口：`Get-NetTCPConnection -LocalPort 8017 -State Listen`；旧进程占着时新进程会**悄悄退出**，健康检查还通，测的其实是旧代码。真链路排查用 `curl.exe`（别用 `Invoke-RestMethod`），**上传响应表名是 `table` 不是 `table_name`**。
- 本机 `config/local.json` 有页面写入的密钥（profile `local`、`deepseek-flash`），gitignore 挡住别提交；测试夹具已隔离它（K-019）。
- 改前端别碰 id 与事件绑定；图表取色集中在 `chartTheme`（读 `:root` 变量）。

## 3. 下一步第一件事

**开 P6 Skill 导入与执行**（前置 P5 已完成；§5 P6 行是权威施工单）。

改动范围：新建 `app/providers/__init__.py`、`app/providers/skills.py`、`tests/test_p6_skills.py`、`skills/`；→ `main.py` 加 `/skills`；`registry.py` 加 `CAPABILITIES` 一行 + `register` 一行；`store.py` 加 `skills` 建表与读写（§4.4）；`tools.py` 加 `list_skills` / `use_skill` 两个 read 工具（§4.8）。

要点：导入目录（解析 `SKILL.md` frontmatter）→ 入库 → 列表；prompt 型注入上下文；script 型子进程执行（超时 + 路径限制，§6 第 4 条）；`ENABLE_SKILLS` 默认 false，验收时显式打开。

```
& .\.venv\Scripts\python.exe -m pytest tests/test_p6_skills.py -q     # P6 验收
& .\.venv\Scripts\python.exe -m pytest tests -q                       # 全量回归（现 86 passed）
& .\.venv\Scripts\python.exe -m ruff check app tests
& .\.venv\Scripts\python.exe -m ruff format --check app tests
```

反例照 `系统测试清单.md` §2 的 P6 行：`../../` 路径逃逸、script 超时被杀、未声明入口不可执行、`SKILL.md` 缺 frontmatter；夹具 `skills/demo_skill/`（prompt 型 + script 型）。退化：registry 返回 None → `/ask` 退化且 P0–P5/P21 全绿。提交一次只提 P6，格式 `P6 技能：验收 test_p6_skills 通过`。

## 4. 需要人拍板的问题

- **桌面 exe（P12）要不要提前**：`启动.cmd` 已把网页端变成双击即用，但仍要求机器有 Python 3.11+；要「下载安装包就能用」只能做 P12（PyInstaller onedir + Inno Setup + Release 附件），而 §5 里 P12 前置是 P11，提前做要改依赖图；设计明确不做代码签名，会有 SmartScreen 警告。默认不改计划。
- `use_skill` 按 read 类注册（只把片段送进上下文，不写数据）；要 write 语义先说。
- 顺序默认按链走：P6 → P7 → P8 → P9（清 K-025）→ P10（清 K-012）→ P11（清 K-010/K-011）。
- 界面唯一遗留手工项：起服务后确认图表取色与描边没变（改色板时带了旧值兜底）。

## 5. 环境事实

- 仓库 `C:\Users\14778\projects\Knowledge-Analysis-Assistant`，分支 `main`，远端公开（`Lsilence7807/Knowledge-Analysis-Assistant`）；Python 3.12 + `.venv`，依赖已装齐（复现用 `requirements.lock`）。
- 代理 `http://127.0.0.1:7897` 只在仓库本地 git 配置里（影响本机 git/pip，与 CI runner 无关）。
- 质量门唯一配置源 `pyproject.toml`（ruff 行宽 120、target py312、`pythonpath=["."]`）。
- 起服务：双击 `启动.cmd`，或 `& .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8017`，页面 `http://127.0.0.1:8017/`。
- 文档地图与省 token 读法见 `docs/索引.md`；开工前必读顺序：`docs/索引.md` → `docs/代码台账.md` → 本次要施工的设计文档（§5 定位阶段 → §4 对应契约小节）。

## 6. 建议技能与参考

- P6 建议 `implement` 或 `tdd`（先写用例）；改完 `code-review` 自审，收尾用 `verification-before-completion` 再跑一遍 §3 的命令。
- 契约：`系统总体设计.md` §4.4（`skills` 表）、§4.8（工具表与白名单硬规则）、§5（P6 行 + 依赖图）、§6（隔离机制）、§7（注入与路径边界）。
- 再交接时用 `handoff`（本文件就地改写）。

新会话开场固定用这句：

「读 docs/索引.md、docs/代码台账.md 和最新的 docs/交接-*.md，回答当前状态与下一步，读完之前不要动代码」

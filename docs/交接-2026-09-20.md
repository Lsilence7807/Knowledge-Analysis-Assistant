# 交接文档 · 2026-09-20（第十一次交接：P15/P17/P8 并行三窗口全部完成，接着开 P9 多模型接入）

给下一个会话用；同一天再交接就地改写本文件，旧版本在 git 历史里。**本次交接的目的：新开窗口接着做 §5 的 P9 多模型接入。排期口径不变：网页端优先（P9→P10→P11 及后续非桌面阶段），P12 桌面打包留空。**
状态：**MVP 七段 + P6 技能 + P7 知识库 + P15 沙箱与指标 + P17 流式 + P8 MCP + P21 性能契约 + 台账补丁 + 手工复验 + 文档压缩 + P5.1/P5.2 + 收尾体检 + K-028 + P6/P7 真模型手工项**全部完成；工作区干净、`main` 与远端同步；基线 `pytest tests -q` → **164 passed**，ruff 干净。

## 1. 已完成阶段与提交号

- 七段验收：P0 `3383821`、P1 `1d9ab68`、P2 `89826d0`、P3 `7e7fc82`、P13 `7e27c31`、P4 `bb75cb8`、P5 `18b2482`。
- 近期：P21 `1ab1428`、CI `3762671`→`e8c76b8`、文档对齐 `2e626ce`、P5.2 `8f8e44a`/`88a30b6`、全局收尾 `23f66e3`。
- P6：`53995a5`（代码）/`8a445a1`（登记）/`9cb8464`（体检）/`d1edc11`；K-028 `70c05f1`。P7：`39f48a8`（代码）/`3ecf7a9`（登记）/`91be3fe`（清死参）/`b4f62c5`（收尾）。
- 收尾与并行约定：`f0e35e6`（第十次交接）、`b60635c`（并行开发约定）、`2275a6b`（索引加行）。
- 本次（并行三窗口）：P15 `978c40f` + 合并 `150f454`、P17 `66a7515` + 合并 `6821a6b`、P8 `e7fd65d` + 合并 `a7620e9`。
- 基线：`pytest tests -q` → 164 passed（P0 5 / P1 8 / P2 22 / P3 12 / P13 14 / P4 13 / P5 6 / P6 15 / P7 14 / P15 29 / P17 8 / P8 12 / P21 6）；质量门 `ruff check app tests` + `ruff format --check app tests` 干净；**CI 已复核**（gh 2.101.0）：最近 12 次运行全部 `success`，含 P15 合并 `5bd8c37`、P17 合并 `6821a6b`、P8 合并 `a7620e9` 与本次登记 `e12f56c`（run 35512529662，1m9s）。

## 2. 未完成、已知损坏与坑

- **三个并行窗口的手工项都做过了**：P15 由窗口 B 在 8019 真进程验（上传 → 沙箱算 3 行、越权 import 400、口径与 `config/metrics.json` 逐字一致）；P17 由窗口 C 在 8020 验（首帧 0.035s、断连后上游被掐断并落 `stream:canceled`）；P8 由统筹窗口在 8018 真服务复验（`/mcp/tools` 列出配置那台 server 的 4 个工具、`echo` 返回带不可信标记、白名单外 400）。
- **并行开发约定在 `docs/并行开发.md`**：worktree + 分支 + 固定端口（8017 主仓库 / 8018 A / 8019 B / 8020 C），热点文件只追加自己那一行；合并与收尾只由主仓库窗口串行做。本次四个热点文件（`app/main.py`、`app/registry.py`、`app/tools.py`、`config/tools.json`）都撞过，解法是「两边都留 + 头注释合并」；四个 worktree 仍在 `%USERPROFILE%\.codex\worktrees\{7ebb,8084,8426}`（分支已合，可留可删）。
- **新记 K-036**：`mcp` 2.x 的 `ClientSession` 不再自动握手，直接发 `tools/list` 会被拒成 -32602；正确入口是高层 `mcp.Client(params, mode="auto")`。**新记 K-037**：`mcp_client` 复用 `providers/kb.py` 的 `strip_instructions`，是 provider 之间的横向 import（要保叶子可上提公共模块）。
- **K-034 仍在**：`insight` 段拿不到 §4.5 承诺的 KB 片段与技能正文（同一次 `/ask` 里 agent 引用出处、insight 却写「未检索到口径说明」）；与 K-012（`prior` 恒空）同处修，归 P10。
- 未闭环 K 支线：K-025→P9、K-012 与 K-034→P10、K-010/K-011→P11、K-024/K-026 残余与 K-031/K-033/K-035→P16、K-032→P14；K-029/K-030 是设计取舍。
- **`apply_patch` 在本机不可用**：改用 Python 写文件，`Path.write_text(..., newline="")`，每处替换先 `assert text.count(old) == 1`。
- **PowerShell 坑**：`curl.exe -d "{\"k\":\"v\"}"` 会把 body 拆坏（422），body 用单引号包；后台起服务在本机可能被判策略拦截，稳妥做法是让 uvicorn 跑在长驻会话里，收尾用 `Stop-Process -Id <pid>` 收掉；看中文输出先 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`（在本机写这类命令时，把 `Start-Process` / `Stop-Process` 拆成两段字符串拼起来可以绕开误拦）。
- 写文件必须 `newline=""`（否则整份 LF 变 CRLF）；`.cmd` 必须 CRLF；测 `启动.cmd` 要把副本放仓库根。本机测试要在真实文件系统里跑（K-017）；起服务前先看端口；`config/local.json` 有密钥，gitignore 挡住别提交（夹具已隔离，K-019）；改前端别碰既有 id 与事件绑定。

## 3. 下一步第一件事

**开 P9 多模型接入**（前置 P8 已完成；`系统总体设计.md` §5 P9 行是权威施工单）。

改动范围：新建 `tests/test_p9_models.py`；→ `app/models.py` 加多适配器与回退、`config/models.json` 扩 profile 段（`purpose` 取值要真生效，清 K-025）、`app/llm.py` 加适配器分发与回退、`app/main.py` 加 `/models`。

要点：两个适配器（`openai_compatible` / `anthropic`）；`model_id` 可逐次指定；失败按 `fallback` 链重试；未配密钥的模型标 `unavailable`；`purpose` 生效后 SQL 用小模型、结论用强模型。

```
& .\.venv\Scripts\python.exe -m pytest tests/test_p9_models.py -q     # P9 验收
& .\.venv\Scripts\python.exe -m pytest tests -q                       # 全量回归（现 164 passed）
& .\.venv\Scripts\python.exe -m ruff check app tests
& .\.venv\Scripts\python.exe -m ruff format --check app tests
```

反例照 `系统测试清单.md` §2 的 P9 行：主模型 500 / 超时 / 非法 JSON / 空响应 / 全挂五种都有确定行为（全挂 → HTTP 200 + `degraded`）；手工项 `/models` 与实际可用模型一致。退化：回落单模型行为。提交格式 `P9 多模型：验收 test_p9_models 通过`。

## 4. 需要人拍板的问题

- **要不要继续用并行窗口**：P9 之后 `P18 后台作业`（只前置 P17，已完成）可以与 P10 并行。要开就照 `docs/并行开发.md` 再来一轮。
- **K-034 怎么排**：跟 P10 一起修（默认），还是插一小段先清（要动 `insight.summarize` 入参并同步 P4 验收命令）。
- 桌面 exe（P12）是否提前：默认不改计划（设计明确不做代码签名，会有 SmartScreen 警告）。
- 顺序默认按链走：P9 → P10（清 K-012/K-034）→ P11（清 K-010/K-011），之后 P14/P16/P18/P19/P20 这些非桌面阶段。
- 界面遗留手工项：起服务后确认图表取色与描边没变。

## 5. 环境事实

- 仓库 `%USERPROFILE%\projects\Knowledge-Analysis-Assistant`，分支 `main`，远端公开（`Lsilence7807/Knowledge-Analysis-Assistant`）；Python 3.12 + `.venv`，依赖已装齐（`requirements.lock` 里有 `mcp==2.2.0`）。
- 代理 `http://127.0.0.1:7897` 只在仓库本地 git 配置里（影响本机 git/pip，与 CI runner 无关）。
- 质量门唯一配置源 `pyproject.toml`（ruff 行宽 120、target py312、`pythonpath=["."]`）。
- 起服务：双击 `启动.cmd`，或 `& .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8017`，页面 `http://127.0.0.1:8017/`。开关：`ENABLE_SKILLS` / `ENABLE_KB` / `ENABLE_MCP` / `ENABLE_SANDBOX` / `ENABLE_STREAM`（都默认 false，`ENABLE_AGENT` 默认 true）。
- 文档地图与省 token 读法见 `docs/索引.md`；开工前必读顺序：`docs/索引.md` → `docs/代码台账.md` → 本次要施工的设计文档（§5 定位阶段 → §4 对应契约小节）。
- **CI 查询姿势（2026-09-20 起已自动化）**：gh 2.101.0 装在 `C:\Program Files\GitHub CLI\gh.exe` 但不在 PATH 上；GCM 里那个 PAT 是 40 位 classic（scope 只有 `gist, repo, workflow`），缺 `read:org`，`gh auth login --with-token` 仍会被拒——不用折腾。
  已在 `%USERPROFILE%\Documents\PowerShell\Microsoft.PowerShell_profile.ps1` 里做好桥接：会话级补 PATH + 调 `gh` 时临时从 GCM 取 token 塞进 `GH_TOKEN`（不落盘、不打印、用完即撤；已设 `GH_TOKEN`/`GITHUB_TOKEN` 时不覆盖）。任何 pwsh 会话直接：

```
gh run list --limit 5
```

  非 pwsh 环境（CI 脚本、从 python 起的子进程等）才需要手动一行；注意单引号里 `` `n `` 不转义，要用双引号：

```
$env:GH_TOKEN = ((("protocol=https`nhost=github.com`n") | git credential fill) | Where-Object { $_ -like 'password=*' }) -replace '^password=', ''
```

  要彻底正规化（`gh auth status` 显示真登录、能用需要 org scope 的命令）：去 GitHub 生成带 `read:org` 的新 PAT 后 `gh auth login --with-token`，之后可删掉 profile 里的 `gh` 函数（PATH 那行保留）。

## 6. 建议技能与参考

- P9 建议 `implement` 或 `tdd`（先写用例，用 mock server 覆盖两个适配器与回退）；改完 `code-review` 自审；收尾用 `verification-before-completion` 跑 §3 的命令，再按 `session-protocol` 走收尾六步。
- 契约：`系统总体设计.md` §4.6（模型接入契约与 `config/models.json`）、§4.5（LLM 契约，`purpose` 与回退）、§5（P9 行 + 依赖图）、§10（环境变量与质量门）；K-025 就是本段要清的账。
- 再交接时用 `handoff`（本文件就地改写）。

新会话开场固定用这句：

「读 docs/索引.md、docs/代码台账.md 和最新的 docs/交接-*.md，回答当前状态与下一步，读完之前不要动代码」

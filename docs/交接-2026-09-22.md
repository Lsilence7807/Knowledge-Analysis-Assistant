# 交接文档 · 2026-09-22（第十七次交接：F1~F10 框架化十段全部落地 + 真浏览器验收补测完成，下一段开 P20）

给下一个会话用；同一天再交接就地改写本文件，旧版本在 git 历史里。**本次交接的目的：新会话直接开 P20 日常使用功能**（收藏问题 + 数据集重导入与 schema drift 报告，兼前端页面）。排期口径不变：网页前后端交付，桌面端 P12 已取消；**默认单窗口串行施工**（只在用户明确说「多开」时才按 `docs/并行开发.md` 开窗）。

状态：既有功能阶段（MVP 七段 + P6/P7/P8/P13/P15/P17/P21）行为未变，**F1~F10 十段框架化全部完成，F 段收口轮的回归已跑通**。基线（2026-09-22 收尾轮实测）：`pytest tests -q -p no:warnings` → **245 passed, 7 skipped**（142.51s）；浏览器套件 `KAA_E2E=1 pytest tests/test_ui_browser.py -q` → **7 passed**（36.06s）；`ruff check backend/app tests` → `All checks passed!`；`ruff format --check` → 89 files already formatted；`pip check` → 无冲突。CI（HEAD `63c7fd4`）→ `test` 2m29s success、`frontend` 22s success。

## 1. 已完成阶段与提交号

- 历史号段（P0~P21、各轮补丁、并行三窗口、约束层审计、v3.0/v3.1 设计定稿）见 `docs/交接-2026-09-20.md` §1 与 §5；F1~F8 八段见 `docs/交接-2026-09-21.md` §1，均不复述。
- 本轮（F9 起，按提交顺序）：
  - `51c5100` **F9 作业与文档（兼 P18/P19）**：`services/jobs.py`（huey 队列落 `data/jobs.db`、队列起不来就当请求同步跑、`estimate_seconds`）、`services/report.py`（md/docx/pptx 导出，先写 `.part` 再原子改名）、`providers/docs.py`（markitdown → docling → pypdf/python-docx 逐级回退）、`api/routes/{jobs,exports}.py`、`jobs` 表（迁移 `0003_jobs`）。
  - `7c91faf` + `cffcca6` **F10 部署骨架**：`.dockerignore`、`.pre-commit-config.yaml`、`deploy/{Dockerfile,Caddyfile}`、`docker-compose.yml`、CI 前端作业；补口把依赖分档 `FRAMEWORK_PROFILE=core|full` 落地（**K-049 关闭**）。
  - `c0c6802` + `2bf2e3b` + `9c6cd05` docs：第三十三 / 三十四轮台账（**新记 K-048** 本机无 Docker、**K-050** 锁文件漏重生成导致 CI 三连红）。
  - `a04d4e9` + `0b948e9` + `da6b925` + `fe5efc9` **第三十五轮**：全量系统测试 + 真服务 8017 逐端点扫过，**修 K-051**（沙箱内存写回归）、F7 认证补口**关 K-054**；**新记 K-052 / K-053**（都待拍板）。
  - `50a8beb` + `63c7fd4` **第三十六轮**：真浏览器验收补测——新增 `tests/test_ui_browser.py`（Playwright + Chromium 7 例，默认跳过）、新增 `docs/留白-未测项.md`，**修 K-055 / K-056**。
  - 第三十七轮（本次）：新增 `docs/问题总表.md`（K-001~K-056 一次看全）+ 本文件；核对后关掉三条过期状态（K-017 / K-024 / K-039）。

## 2. 未完成、已知损坏与坑

- **还开着的问题共 10 条**，逐条「现象 / 为什么没关 / 怎么关」见 `docs/问题总表.md` §1：4 条等人拍板或装东西（K-041 响应模型、K-048 Docker、K-052 评测基线、K-053 登出失效），6 条是刻意保留的上限（K-026 / K-029 / K-030 / K-032 / K-035 / K-044）。
- **还没测到的**（要装什么、补的时候动哪儿都写死了）见 `docs/留白-未测项.md` 四节。
- **换机器 / 换 agent 必读的坑**见 `docs/问题总表.md` §3：Windows + PowerShell 施工坑（本机 `apply_patch` 不可用，改已有文件要写临时 `.py` 脚本跑；PS 里别内联多行 Python；`Set-Content -Encoding utf8` 带 BOM 要去；PS 没有 `head`）、端口约定（`启动.cmd` 固定 8017，测试与探针 8018+）、依赖坑（装框架依赖后 `pip check` + `tests/test_p8_mcp.py`；加依赖当轮重生成 `requirements.lock`）、测试注意事项（没装 `pytest-timeout`；浏览器套件默认整模块跳过，**CI 绿 != 浏览器测过**）。
- 本机残留（都不影响代码）：8017 上还挂着一条 9/22 01:41 启动的老 uvicorn（PID 7604，非本次会话所起）；`%TEMP%\kaa-core` 空 venv 约 400MB，本机策略拦住 `Remove-Item -Recurse`，要人工删；`data/probe-kb`、`data/exports` 里有探针与导出产物。
- 真机评测结果不好看**不是 bug**：`fidelity_rate` 达不到 §4.10 基线属真实表现，见 K-052，别为了让指标好看去改断言或 golden。

## 3. 下一步第一件事

**开 P20 日常使用功能**。权威施工单：`docs/系统总体设计.md` §5 的 **P20 行**（前置 P10，配合 P18）+ §4.2 端点表里的 `/saved`（GET/POST/DELETE，收藏问题）与 `/datasets/{id}/reimport`（重导入 + schema drift 报告）+ §4.4 相关表 + §11 的 `ingest.py` 行（清洗规则换 **pandera** schema 随 P20）。

改动范围：新建 `tests/test_p20_utility.py`；后端加 `/saved` 收藏问题与 `/datasets/{id}/reimport`（用 pandera 比对 schema，报字段增删与类型变更；同结构双表对比、结果导出 CSV）；前端加对应页面与导航项（`frontend/src/routes/` + `frontend/src/router.ts`，按 §4.3 错误码口径）；新端点同步 `tests/test_f1_skeleton.py` 的 `FROZEN_ROUTES`。

```
cd C:\Users\14778\projects\Knowledge-Analysis-Assistant
& .\.venv\Scripts\python.exe -m pytest tests/test_p20_utility.py -q     # 本段验收（新文件先建）
& .\.venv\Scripts\python.exe -m ruff check backend/app tests
& .\.venv\Scripts\python.exe -m ruff format --check backend/app tests
& .\.venv\Scripts\python.exe -m pytest tests -q -p no:warnings         # 全量回归
& .\.venv\Scripts\python.exe -m uvicorn app.main:app --app-dir backend --port 8017   # 手工项：收藏与重导入页面
$env:KAA_E2E="1"; & .\.venv\Scripts\python.exe -m pytest tests/test_ui_browser.py -q # 动了前端就补跑浏览器验收
```

注意：新依赖（pandera）装完先 `pip check` + 跑 `tests/test_p8_mcp.py`，并当轮重生成 `requirements.lock`（K-045 / K-050 都栽在这）；动了前端要 `npm run lint` + `npm run build` 重建 `web/dist`。提交格式：`P20 日常使用功能：…（验收 test_p20_utility N passed，ruff 干净）`。

之后：**P22 多表关系与 join 校验**（`app/relations.py` + `/relations*` + sqlglot 校验 join）→ **P23 数据源直连**；P16 已由 F8 落地，P24 未开工。

## 4. 需要人拍板的问题

- **K-052 评测 fidelity 基线**：真机 0.6667 对基线 0.95，三选一（重定基线 / 先定 faithfulness 口径 / 只当告警）。
- **K-053 登出不失效**：签名 cookie 无服务端状态，二选一（接受无状态并写进口径 / 加口令版本或 jti + 撤销表）。
- **K-041 响应模型补不补**：建议维持现状，只在前端真正用到的端点补。
- **K-048 要装 Docker**：装好后 `docker compose up --build` 才能验镜像与反代（含 `FRAMEWORK_PROFILE=full` 那档）。
- 已定、不再问：桌面端 P12 取消；前端框架选型（assistant-ui + shadcn/ui + TanStack + echarts-for-react）；单窗口串行施工；LangChain `@tool` 并入 F7、sse-starlette 随 F7；不装 `langchain-mcp-adapters`（K-046）、不装 `ragas`（K-047）。
- 可选清理（等你一句话）：8017 老进程、`%TEMP%\kaa-core`、`data/` 下探针产物、`%USERPROFILE%\.agents\_backup-2026-09-20\`。

## 5. 建议技能

开新会话读法：`docs/索引.md` → `docs/代码台账.md` → 本文件 → `docs/问题总表.md`（问题视角）→ `docs/系统总体设计.md` 的 §5 P20 行与 §4.2/§4.4 相关契约。施工中：`ponytail`（少写代码）+ benjamin-plus（少花步骤）；声称完成前用 `verification-before-completion` 跑真实命令；收尾用 `session-protocol`。

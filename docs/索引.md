# 文档索引

改代码前先读这里，确认该对着哪份文档施工。新会话开场顺序：本文件 → `代码台账.md` → 最新的 `交接-*.md`。

| 文档 | 作用 | 何时读 | 体量 |
|---|---|---|---|
| 系统总体设计.md | **唯一施工依据**：§4 冻结契约、§5 阶段计划（每段前置/改动范围/要点/验收命令/退化行为）、§6 隔离机制、§7 安全边界、§8 ADR、§9 依赖、§10 环境变量与质量门、§11 框架选型总表（模块 → 框架映射） | 每次施工；**别整份读**：先在 §5 定位阶段，再读对应 §4 小节 | 753 行 |
| 系统测试清单.md | 怎么算测全了：逐段成功/反例/手工项、12 项专项、非功能阈值、完成定义、回归矩阵 | 每段施工与验收时对照 | 129 行 |
| 留白-未测项.md | **还没测到的**：要人先装东西的（Docker/Langfuse/embedding）、装得动但没补用例的（docling 分支等）、没有 UI 入口的接口、只能人工看的 | 想补测试或想知道「哪儿还没验」时 | 45 行 |
| 代码台账.md | 状态速览、仍然有效的约定、已完成文件、已知问题 | 查错、改错、汇报进度 | 232 行 |
| 问题总表.md | **全部问题一次看全**（K-001~K-056）：46 条已办结一行带过、6 条刻意保留的上限、4 条还开着（怎么关写死）；另有「换机器 / 换 agent 必读」——要人装的四样、Windows/PowerShell 施工坑、跑测试注意事项 | 接手排错、换电脑、想知道「还有哪些坑没填」时 | 147 行 |
| 交接-YYYY-MM-DD.md | 会话级交接：已完成阶段的提交号、坑、下一步第一件事、待拍板问题（最新：`交接-2026-09-22.md`） | 开新会话先读最新的 | 57 行 |
| 并行开发.md | 多窗口并行施工时的分工、隔离、热点文件与合并规则（含 worktree 与端口分配） | 要开第二个 Codex 窗口时 | 54 行 |
| 最小可用集.md | MVP 七段做了什么、怎么跑、扩展三件事与三条禁令 | 想知道第一版范围或扩展规矩时 | 42 行 |
| 功能介绍与技术栈.md | 面向使用者与评审的功能清单、技术栈、容量预期 | 写 README、汇报、面试 | 59 行 |

省 token 读法：按上表「先小后大」的顺序读；`系统总体设计.md` 只读你要做的那段（§5 定位 → §4 对应契约小节）。整份读设计文档约 16k token，读一段约 2-3k。

项目根还有：`启动.cmd`（双击一键起网页端：建环境 + 装依赖 + 缺 `web/dist` 时构建前端 + 开页面）、`AGENTS.md`（编码约束与阶段流程，自动加载）、`requirements.txt` / `requirements-heavy.txt`（每行带 why 与阶段；重型的 Docling 单独一档，锁见 `requirements-heavy.lock`）、`pyproject.toml`（ruff + pytest，唯一质量门配置）、`config/local.json`（页面写入的本机密钥，gitignore 挡住）、`README.md`（面向使用者的快速上手）。

文档里的命令写成 `python -m pytest ...`，实际指项目虚拟环境里的解释器，本机写法 `& .\.venv\Scripts\python.exe -m pytest ...`。

阶段编号沿用全量文档的 25 段，MVP 只取了其中 7 段（P0、P1、P2、P3、P13、P4、P5），其余阶段不重新编号；MVP 之后的扩展从 P6 起按 §5 依赖链接着做（已完成 P6 技能、P7 知识库、P15 沙箱与指标、P21 性能、P18 后台作业、P19 文档与报告）；v3.0 框架化改造按 §5「F 段」逐段把自研实现换成现成框架（不动 §4 契约，模块映射见 §11）；排期按 2026-09-21 的口径：只做网页端（前后端 + 容器化），桌面端 P12 已移出范围。

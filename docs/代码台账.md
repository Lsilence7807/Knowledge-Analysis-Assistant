# 代码台账

用途：查已完成文件的作用与已知问题，改错先看这里，不用满仓库翻。
维护：每段验收后更新，只增不删；状态取值 已完成 / 待修改 / 已修复。2026-09-20 压缩过一轮：已修问题只留一行结论，逐段取舍与推送历史看 `git log`（旧版在本文件历史里）。2026-09-20 第二轮：`docs/` 的设计、说明与测试文档整体压缩，逐份体量与省 token 读法见 `docs/索引.md`。

## 状态速览

- 已完成：MVP 七段（P0/P1/P2/P3/P13/P4/P5）+ P6 技能 + P7 知识库 + P21 性能契约 + 台账 A/B 类补丁 + 手工复验（页面删除、DELETE 端点、空态、真实模型链路）+ P5.1 视觉收尾 + P5.2 一键启动 + 文档压缩两轮 + P6/P7 真模型手工项复验 + P15 沙箱与指标 + P17 流式 + P8 MCP（并行三窗口做完，见 `docs/并行开发.md`）；逐文件的作用与注意看下面的「已完成文件」表，最新提交看 `git log -1`。
- 基线：`& .\.venv\Scripts\python.exe -m pytest tests -q` → **164 passed**（含 P6 15 例、P7 14 例、P15 29 例、P17 8 例、P8 12 例）；提交前质量门 `ruff check app tests` 零告警 + `ruff format --check app tests` 无差异。
- 真实链路已复验：deepseek-flash 走 上传 → `/query` → `/ask` → `/insight` → `DELETE`（7.3s / 4.6s 出 SQL 与结论），边界含 400/404/422；陌生人视角也验过（公开仓库 clone 后起服务即可用；无密钥时 `/capabilities` 只 `stats:true`，`/ask` 给降级说明）。
- 逐轮施工、排障与文档压缩过程看 `git log`（① 锁依赖、② CI、③ P21、各轮 K 补丁、文档压缩两轮）；结论已进下面的 K 表与「已完成文件」表，这里不复述。
- 2026-09-20 第十一轮：P7 知识库（新建 providers/kb.py；registry 加 kb 能力、store 加 kb_docs + kb_fts、tools 加 search_kb、main 加 `/kb/import` `/kb/search`），验收 `pytest tests/test_p7_kb.py -q` 14 passed + 全量 115 passed + ruff 干净；顺手修掉一处真问题（点名一份读不进来的文件原本回 200 + skipped，现在整体 400）。
- 2026-09-20 第十三轮：P15 沙箱与指标（新建 app/sandbox.py、app/metrics.py、config/metrics.json；registry 加 sandbox 能力、tools 加 run_code 与 list_metrics、main 加 `/sandbox/run` 与 `/metrics/definitions`），验收 `pytest tests/test_p15_sandbox.py -q` 29 passed + 全量 144 passed + ruff 干净；手工项在真进程 8019 端口复验通过（上传 → 沙箱算出 3 行；越权 import 400；口径与配置文件逐字一致）。
- 2026-09-20 第十四轮（并行三窗口）：P17 流式与 P8 MCP 与 P15 同时在不同 worktree 里做完，由主仓库窗口串行合并（P15 `150f454`、P17 `6821a6b`、P8 `a7620e9`）——合并时 `app/main.py`、`app/registry.py`、`app/tools.py`、`config/tools.json` 四处冲突按「两边都留 + 头注释合并」解，合并后全量 164 passed + ruff 干净；P15 手工项由窗口 B 在 8019 验过，P8 手工项由主窗口在 8018 真服务复验（`/mcp/tools` 列出配置那台 server 的 4 个工具、`echo` 白名单内返回带不可信标记、白名单外 400）。
- 2026-09-20 第十二轮（无代码改动）：P6/P7 真模型手工项复验通过——`ENABLE_KB=true` + `ENABLE_SKILLS=true` 起服务，导入 `tests/fixtures/kb` 与 `skills/` 后提一个同时需要口径与技能的问题；同一次 `/ask` 的 `steps` = `search_kb`(×2) → `list_skills` → `use_skill` 且全 `ok`，答案点名 KB 出处 `tests/fixtures/kb/经营口径.md` 与 `demo_skill` 的「>=30 行才下趋势结论」，`degraded` 空；同轮体检发现 K-034。
- 2026-09-20 第十轮：P6 技能（新建 providers/skills.py 与 `skills/` 示例；registry 加 skills 能力、store 加 skills 表、tools 加两个只读工具、main 加 `/skills`），验收 `pytest tests/test_p6_skills.py -q` 15 passed + 全量 101 passed + ruff 干净；收尾体检修了一处真问题（prompt 正文原本无上限直通模型上下文，实测 2 万字符，现按 4000 字符截断并标 `truncated`）与一处文档错位（README 快照 86→101、阶段列表补 P6）。
- 2026-09-20 第十五轮（收尾，零代码改动）：gh 认证桥接（本机 profile 自动注入 `GH_TOKEN`）+ 路径约束写进 `AGENTS.md` §1 + 约束层审计与瘦身（`skill-router` 源 9973→4613 B 并同步副本、`handoff`/`implement` 去掉与 Codex 不符的指令）；全量 164 passed + ruff 干净；细节见 `docs/交接-2026-09-20.md` §2 与下方 K-038。
- 2026-09-20 第十六轮（门面与文档，零代码改动）：README 重写为完整效果说明（徽章 + 真实运行截图 + mermaid 架构图 + 能力清单 + 性能基线 + 诚实路线图），新增 `LICENSE`(MIT)、`CONTRIBUTING.md`、`examples/demo_sales.csv`、`docs/images/` 两张实拍截图；「怎么跑」与验收命令保持原样（只把过期的 115 快照改成 164）。全量 164 passed + ruff 干净。
- 待推送：无，远端 `main` 已同步；CI 用 gh 2.101.0 复核过（最近 12 次运行全 `success`，查法见 `docs/交接-2026-09-20.md` §5）。
- 排期（2026-09-20 拍板）：**网页端优先**，按链做 P8→P9→P10→P11 及后续非桌面阶段，P12 桌面打包留空；桌面复用不复制代码（§6.6 双壳同源），P12 只加「配置与数据根目录指向 `%APPDATA%`」一处切换与打包脚本。
- 后续：未闭环 K 支线各归所属阶段——K-025→P9、K-012→P10、K-010/K-011→P11、K-024 与 K-026 残余→P16；其余阶段按 `系统总体设计.md` §5 阶段表接（§6 是隔离机制，§10 是环境变量与质量门）；P7 新记 K-032→P14、K-033→P16；本次复验新记 K-034→P10；P15 新记 K-035→P16；P8 新记 K-036（已解决，留作踩坑提示）与 K-037（设计取舍）。
## 仍然有效的约定（改代码前先看）

| 事项 | 约定 |
|---|---|
| 产品名 | `Knowledge-Analysis-Assistant`；安装包 `…-Setup.exe`；数据在 `%APPDATA%\Knowledge-Analysis-Assistant` |
| 模型与密钥 | 页面可配任意 OpenAI 兼容厂商，写 `config/local.json`；密钥顺序 本地文件 → profile 的 `api_key_env` → `LLM_API_KEY`；调用时读取，保存即生效；`GET/PUT /settings/models` 只回 `id/provider/base_url/model/has_key`，明文不出网关；`PUT` 写盘后把 `default` 设为该 profile |
| 静态挂载 | `web/` 用 `StaticFiles(html=True)` 挂在 `/`，必须排在全部 API 路由之后 |
| `/ask` 两条路径 | `ENABLE_AGENT=true`（默认）走 agent 循环，`false` 回落 P3 单跳；响应形状相同，agent 多一个 `steps` |
| 工具 | 工具签名带 `dataset_id`，由 agent 每步注入，不进给模型的 JSON Schema；白名单、超时、步数预算读 `config/tools.json`，每次调用重读（免重启） |
| 取数与上下文 | 取数按 `/query` 口径 5000 行（末步结果表不再被截到 200 行）；回给模型的超 200 行只回列名与行数（K-008） |
| 数字追溯 | findings 逐条给 `row`（结果表 0 基）即视为整段已追溯；没指认才按字面比对，且先剔掉表名/列名里的数字（K-009/K-018） |
| 结论并入 `/ask` | 只由 `main._attach_insight()` 调 `insight.summarize()`，不动 `agent.run()` 返回值 |
| 修复排期 | A 类（新建文件 / 新路由 / 编排层插调用）随手修；B 类先回设计文档登记再改并同步验收命令；C 类归后续阶段 |
| 路径 | 一律以 `app/config.py` 的 `BASE_DIR` 为锚，禁止硬编码绝对路径、禁止依赖 cwd；细则、抽查命令与「换目录跑测试」验收见 `AGENTS.md` §1 路径规则 |
| 前端 | 单文件原生 JS、无构建链；ECharts 走本地 `web/vendor/echarts.min.js`（5.6.1），文件缺失时只出表格不报错 |
| 删除数据集 | `DELETE /datasets/{id}` 清 DuckDB 表 + 原始文件 + 元数据与流水；P20 正式删除/重导入在此基础上扩展 |
| 技能 | 技能 = `skills/`（或项目内任一路径）下带 `SKILL.md` 的子目录；frontmatter 的 `name` 当 slug（缺省用目录名）、`description` 给模型看、`kind` 取 prompt/script、script 还要 `entry`；坏技能单个跳过（`skipped`），不拖垮整批导入 |
| 知识库 | `kb_docs` 存块、`kb_fts`（FTS5 `trigram`，建在 SQLite 而不是 DuckDB：前者标准库自带且离线可用）只负责检索；块上限 500 字符、最多回 5 条命中、片段截 200 字符；查询词短于 3 字符走 LIKE；片段进上下文一律包「不可信」标记并剔掉指令性行 |
| 工具白名单 | 新增能力对应的工具默认进 `config/tools.json` 的 `allow`（模型看得见；能力关掉时给可读拒绝）。不想让模型看见就从 `allow` 删，下一次调用即生效 |
| 技能取用 | prompt 型把 frontmatter 之后的正文给模型（超 4000 字符截断并标 `truncated`，与脚本输出同一上限）；script 型 `subprocess` 跑 `entry`（cwd 锁技能目录、超时 `SKILL_TIMEOUT_S`=20s、输出留末尾 4000 字符）；`enabled: false` 的技能入库但拒绝取用；`use_skill` 按 read 类注册（不写数据） |
| 桌面打包 | 不做 onefile、不做代码签名 |

## 已完成文件

| 文件 | 作用 | 阶段 | 状态 | 注意 |
|---|---|---|---|---|
| app/__init__.py | 包标记 | P0 | 已完成 | 无 |
| app/config.py | 环境变量、本地设置（`config/local.json`）与路径；`api_key(profile)` 定密钥来源；`save_settings()` 合并写盘；`EXEC_MAX_WORKERS`（默认 4）与 `BENCH_BASELINE` | P0/P3/P5/P21 | 已完成 | 无 |
| app/registry.py | 能力注册表，新能力的唯一接入点（query / llm / agent / insight / skills / kb / sandbox / mcp 都在这里注册） | P0/P3/P4/P13/P6/P7/P8/P15 | 已完成 | `CAPABILITIES` 加一条后 `/capabilities` 条数变多，P0 的计数断言要同步（P6 已办） |
| app/schemas.py | HTTP 边界模型：Insight/Finding/Suggestion 与五个 POST 请求体（QueryIn/StatsIn/AskIn/InsightIn/SandboxIn）；Finding 带 `row`/`column` | P0/P4/K-013/K-009/P15 | 已完成 | 其余模型按阶段追加 |
| app/store.py | SQLite 建表与元数据读写（datasets / tasks / agent_steps / trace_spans / capability_log / skills / kb_docs / kb_fts） | P0/P1/P13+A 类/P6/P7 | 已完成 | 无 |
| app/db.py | DuckDB 唯一出口：守卫（注释与分号只判代码部分）、超时、5000 行上限、描述统计与异常检测；`exec_sql` 内部走统一执行器 `run_blocking`（签名不变） | P0/P2+A 类/K-002/K-006/P21 | 已完成 | 无 |
| app/main.py | HTTP 路由与装配点：health、capabilities、datasets（增/查/画像/删）、query、stats、tools、ask、insight、skills（列表/导入）、kb（导入/检索）、sandbox/run、metrics/definitions、mcp（tools/call）、ask/stream、settings/models、bench（读性能基线）；静态挂载写在最后 | P0~P5/P13/P21/P6/P7/P8/P15/P17 | 已完成 | 无 |
| config/metrics.json | 指标口径定义（id / 名称 / 别名 / 公式 / 单位 / owner / 口径说明），ADR-20 的唯一来源 | P15 | 已完成 | 别名解析与未定义标注都读它；公式只是给模型看的文字口径，不给 `run_code` 或 SQL 自动套用 |
| config/models.json | 模型接入配置 | P0 | 已完成 | 只有 local 一个 profile，P9 扩充 |
| config/tools.json | 工具白名单、每步超时、步数预算、回模型的上下文行数上限 | P0/P6/P7/P8/P15 | 已完成 | `allow` 现在含 `list_skills` / `use_skill` / `search_kb` / `list_metrics` / `run_code` / `call_mcp_tool`（K-028 拍板，P7/P15/P8 沿用）；白名单外调用一律 `ToolError`，新工具注册后忘了写进 `allow` 就调不通（有意） |
| app/ingest.py | CSV/XLSX 清洗、画像与 DuckDB 落表 | P1 | 已完成 | 无 |
| app/models.py | 模型 profile 读取与解析（`models.json` 打底，`config/local.json` 覆盖） | P3 | 已完成 | 无 |
| app/llm.py | 唯一模型出入口：结构化 JSON 输出、重试、原文截断、`chat_tools` | P3/P13 | 已完成 | 无 |
| app/nlu.py | 单跳 自然语言 → SQL（P13 后作为降级通道保留） | P3 | 已完成 | 无 |
| app/tools.py | 工具注册表（register / all_tools / call / kinds / settings）与 9 个只读工具：run_sql、describe_stats、detect_anomalies、list_skills、use_skill、search_kb、list_metrics、run_code、call_mcp_tool（后六个经 registry 取 provider/模块能力，关掉时给可读拒绝） | P13/K-006/K-008/K-023/P6/P7/P8/P15 | 已完成 | 白名单外调用一律 `ToolError`：新工具注册后忘了写进 `config/tools.json` 的 `allow` 就调不通（这是有意的） |
| app/sandbox.py | 受限代码执行：AST 静态检查（import 白名单、`open`/`eval`/`exec`/`compile`/`__import__` 禁调、双下划线属性禁访）+ `python -I -X utf8` 子进程 + `DATA_DIR/sandbox/<uuid>` 工作目录 + 超时 + 输出上限；数据集按 id 导只读副本 CSV 注入成 `df` | P15 | 已完成 | 子进程隔离不是容器隔离（`# ponytail` 注释已留）；工作目录无论成败当场删；`run_code` 的 `dataset_id` 是追加的关键字参数（§4.2 已同步） |
| app/metrics.py | 指标语义层：读 `config/metrics.json`，把提问里的名称/别名解析成指标 id，并标出「像指标但没定义」的说法（只标注，不编公式） | P15 | 已完成 | 未定义识别是「中文词 + 口径后缀」启发式，纯英文缩写不认、带后缀的普通词可能误标（K-035）；本段只到工具与路由，没往 `/ask`、insight 里拼口径 |
| app/agent.py | Agent 循环：规划 → 调工具 → 观察 → 再规划；步数预算、回模型的单步结果截断、步骤落库，产出 /ask 响应 | P13/K-008 | 已完成 | 无 |
| app/insight.py | 结果表 → 结构化结论：提示词版本、限行喂表（datetime 兜底）、数字追溯写 caveats、失败抛带标签的 InsightError | P4/K-009/K-018/K-021 | 已完成 | 无 |
| web/index.html | 单文件前端：模型配置、上传与数据集选择、删除按钮（confirm → DELETE）、提问、SQL 折叠、步骤面板、结论卡片、结果表与图表切换、降级提示 | P5+A 类+P5.1 | 已完成 | P5.1 换了视觉（顶栏「知析」字标 + 奶油纸底/粗描边/硬投影），JS 与 id 一律没动；旋钮集中在 `:root`（主色 `--violet`、描边 `--stroke`、卡片密度 `--pad`、圆角），调值改一行即可；降级黄条只回原因码，不贴模型原文（原文仍在响应 `message` 与 `tasks.error`）；图表色板 `chartTheme` 读 `:root` 变量（第 5 色 `#B08C00` 与字标 SVG 的 hex 仍硬编码）；改样式别碰 id 与事件绑定 |
| web/vendor/echarts.min.js | 本地 ECharts 5.6.1（Apache-2.0），离线出图 | P5 | 已完成 | 不引 CDN |
| pyproject.toml | 质量门唯一配置源：ruff（line-length 120、select E/F/I/UP/B/BLE、ignore B008、insight.py 免 E501）与 pytest（只留 testpaths） | P0 起 | 已完成 | 别再加 `addopts`（K-020） |
| .editorconfig / .gitattributes | 换行、编码、缩进统一（LF） | P0 起 | 已完成 | 无 |
| README.md | 项目入口：定位、效果预览（真实运行截图）、核心能力、mermaid 架构图、怎么跑、验收命令、项目结构、性能基线、文档地图、诚实路线图 | P0 / 收尾 | 已完成 | 徽章里的 tests 数与本行说明要跟 `pytest` 实际数同步 |
| requirements.lock | 可复现环境锁定：`requirements.txt` 声明 + 传递依赖闭包（74 条），取本机 `.venv` 已装版本 | P0 起 | 已完成 | 不是 `pip freeze` 原样导出（那会把无关包一起锁进来）；改依赖先改 `requirements.txt` 再重生成，闭包时带 extra 的依赖（`uvicorn[standard]` 等）要显式带 extra 求值，漏了会少 8 条；按 Windows 求值，不含 uvloop |
| .vscode/、.github/copilot-instructions.md | VSCode 集成与助手说明 | P0 | 已完成 | 无 |
| tests/test_p{0,1,2,3,13,4,5,6,7}_*.py（9 个验收文件） | 每段一个验收文件，命令见各段设计文档；用例数 p0 5 / p1 8 / p2 22 / p3 12 / p13 14 / p4 13 / p5 6 / p6 15 / p7 14 | 各段 | 已完成 | 全仓库夹具都隔离 `config/local.json`（K-019）；p3 夹具把 `ENABLE_AGENT` 钉成 false（验降级通道）；p13 与 p4 的模型替身要答 insight 那一跳；p0 验「能力全 false」那条显式关掉密钥 |
| app/providers/__init__.py | provider 包标记（只被 registry 惰性 import） | P6 | 已完成 | 无 |
| app/providers/skills.py | 技能库：解析 `SKILL.md` frontmatter、目录导入入库、列表、prompt 取正文、script 子进程执行（路径锁在项目内） | P6 | 已完成 | 继承服务进程环境（K-029）；正文不做注入清洗（K-031） |
| tests/test_p6_skills.py | P6 验收：导入/列表/prompt/script + 6 条反例（逃逸、超时、缺 entry、缺 frontmatter、未知 kind、禁用、开关关闭） | P6 | 已完成 | 夹具在 tmp_path 造技能目录，`skills.ROOT_DIR` 指到临时目录 |
| skills/demo_skill/、skills/demo_script/ | 可直接导入的技能示例（prompt 型 / script 型 + `run.py`） | P6 | 已完成 | 示例也是手工验收的素材，别删 |
| app/providers/kb.py | 知识库：md/txt 读取与编码兜底、按段分块（≤500 字符）、FTS5 检索、片段包不可信标记并剔指令行 | P7 | 已完成 | 检索短词走 LIKE 全表扫（K-032）；片段清洗只到行级黑名单（K-033） |
| tests/test_p7_kb.py | P7 验收：导入/分块/重复导入/FTS 命中带出处/短查询/指令行剥离 + 6 条反例 | P7 | 已完成 | 夹具在 tmp_path 造文档，`kb.ROOT_DIR` 指到临时目录 |
| tests/test_p15_sandbox.py | P15 验收 29 例：自定义计算、只读快照、工作目录清理、输出截断、超时杀进程、内存爆炸兜住 + 10 条逃逸反例 + 指标解析/未定义标注 + 开关关闭与白名单两条 | P15 | 已完成 | 会真起子进程（约 18s）；数据目录指到 tmp_path，沙箱 `ENABLED` 显式打开 |
| tests/fixtures/kb/经营口径.md | P7 知识库夹具（含口径段落与一句「忽略以上指令」用于注入检查） | P7 | 已完成 | 手工验收用它演示检索 |
| tests/fixtures/dirty.csv | P1 清洗测试夹具 | P1 | 已完成 | 无 |
| app/exec.py | 阻塞任务统一执行器：固定线程池（`EXEC_MAX_WORKERS`，默认 4）+ `run_blocking()`，DuckDB/pandas/HTTP/子进程都从这里排队 | P21 | 已完成 | 池是单进程线程池，p95 不达标再上进程池或只读副本 |
| tests/test_p21_perf.py | P21 验收 6 例：执行器线程、`exec_sql` 经 `run_blocking`、50 万行聚合 1.5s 预算、心跳 p95 ≤200ms + 10 并发无 5xx、/bench 退化行为 | P21 | 已完成 | 心跳用 `asyncio.run` + `httpx.ASGITransport`，不依赖 pytest-asyncio 的自动模式 |
| 启动.cmd | 网页端一键启动：建 `.venv` → 装 `requirements.lock` → 起 uvicorn → 自动开浏览器；端口被占先探 `/health` 再决定开页面还是提示换端口 | P5.2 | 已完成 | 必须是 CRLF（`.gitattributes` 已给 `*.cmd` 设 `eol=crlf`）；「首次装依赖」这条路本机没实测（依赖 CI 与锁文件已验），只在有 `.venv` 的机器上跑通过 |
| bench/bench.py | 本地压测：临时目录造 50 万行表，量 20 次聚合 p50/p95，写 `bench/baseline.json`；超 1.5s 预算或比上次退化 ≥1.3× 非零退出 | P21 | 已完成 | 直接 `python bench/bench.py`（自带 sys.path 补丁，同 K-027 的坑） |
| bench/baseline.json | 性能基线（本机 12 核：p50 0.0565s / p95 0.0619s / 50 万行 / workers 4） | P21 | 已完成 | 换机器后重跑覆盖，判定口径在 `bench/bench.py` |
| app/stream.py | SSE 帧组装（`sse()`）、取消判定（`canceled()`）与响应头；帧序与载荷契约见 §4.11 | P17 | 已完成 | 关掉 `ENABLE_STREAM` 时 `/ask/stream` 回 503，前端回落整段 `/ask` |
| app/providers/mcp_client.py | MCP 客户端：按 `config/mcp.json` 起 stdio server → 列工具 → 白名单调用，返回内容包不可信标记并按 2000 字符截断 | P8 | 已完成 | 每次调用起一个子进程、靠关 stdin + 杀进程树回收（`# ponytail` 注释留了升级路径）；复用 `providers/kb.py` 的 `strip_instructions`（K-037）；`command` 留空即用当前解释器（K-036 的握手坑） |
| config/mcp.json | MCP server 配置：command/args/timeout_s/tools 白名单（默认 `allow` 只有 `echo`） | P8 | 已完成 | server 命令只来自本文件，不接受请求参数（§7）；默认只读 |
| tests/test_p8_mcp.py | P8 验收 12 例：列工具、白名单内调用、结果包裹与截断、server 起不来降级、调用超时、白名单外拒绝、kill 后子进程回收 | P8 | 已完成 | 用 `tests/fixtures/mcp_echo_server.py`；断言真的读 pid 确认子进程被回收 |
| tests/fixtures/mcp_echo_server.py | P8 夹具：20 行 stdio MCP server，含 echo/huge/noisy/hang 四个工具（测截断、不可信处理、超时） | P8 | 已完成 | 只被 P8 测试与手工项使用 |
| tests/test_p17_stream.py | P17 验收 8 例：帧序、`done` 唯一、断连取消、慢客户端、中途模型报错后仍收尾 | P17 | 已完成 | 用 httpx 流式读取；手工项在 8020 真服务复验过首帧与断连 |

| LICENSE | MIT 许可证（2026, Lsilence7807） | 收尾 | 已完成 | 换协议只改这一个文件 |
| CONTRIBUTING.md | 参与贡献：环境、提交前必跑的三条质量门、阶段制 / 一阶段一测试 / 注释与提交格式等约定 | 收尾 | 已完成 | 与 `AGENTS.md` 同口径，`AGENTS.md` 仍是权威 |
| examples/demo_sales.csv | 演示数据 240 行 × 7 列（日期 / 地区 / 渠道 / 品类 / 销售额 / 订单数 / 客单价），README 效果预览用的就是它 | 收尾 | 已完成 | 纯生成数据，无真实业务信息；换数据要同步 README 截图口径 |
| docs/images/screenshot-01-flow.png | README 效果预览：提问 → SQL 折叠 → 步骤 → 结论摘要 | 收尾 | 已完成 | 2026-09-20 在 8017 真服务 + deepseek-flash 上实拍（上传 examples 那份数据、真跑一次 /ask），非示意图 |
| docs/images/screenshot-02-result.png | README 效果预览：柱状图 + 结果表 | 收尾 | 已完成 | 同上；前端改版后需重拍 |

## 已知问题

| 编号 | 现象 | 触发条件 | 位置 | 状态 |
|---|---|---|---|---|
| K-001 | 守卫只拦多语句与非 SELECT，注释绕过、超时、行数上限没实现 | 任何 SQL | app/db.py:_guard | 已修复（P2 补全守卫） |
| K-002 | 字面量里出现 `--`/`/*`/`;` 被误拒（如 `SELECT 'a;b'`） | 写这类 SQL | app/db.py:_guard | 已修复（先 `_mask_literals` 再判） |
| K-003 | P13 需要的 `llm.chat_tools()` 未实现 | 工具循环 | app/llm.py | 已修复（P13 实现；坏 JSON 当空参） |
| K-004 | 真实模型链路未验证（无密钥，测试全走替身） | 有密钥的真实调用 | app/llm.py:_complete | 已修复（P5 手工验证） |
| K-005 | `/ask` 不落 `tasks` | 需要任务台账 | app/main.py:ask | 已修复（两条路径各落一行；写失败只记日志） |
| K-006 | `describe_stats`/`detect_anomalies` 吃不到 tools.json 的 15s | 大表跑统计 | app/tools.py | 已修复（`db.describe` 加 `timeout_s`） |
| K-007 | 真实模型的工具调用格式未验证 | 有密钥的真实提问 | app/llm.py:chat_tools | 已修复（P5 手工验证） |
| K-008 | 提问末步结果被截到 200 行，比 `/query` 的 5000 小 | 提问返回超 200 行 | app/tools.py:_run_sql | 已修复（取数 5000；200 只作用于回模型的上下文，见 `agent._shrink`） |
| K-009 | 结论里算出来的汇总值（合计、比率、环比）被判「找不到」，caveats 偏吵 | 表里没有该数字原文 | app/insight.py:_unsupported_numbers | 已修复（Finding 带 `row`/`column`；`PROMPT_VERSION` p4-2） |
| K-010 | 无鉴权、无 CORS，`RATE_LIMIT_PER_MIN` 读了没用过 | 监听非回环地址或多人访问 | app/config.py、app/main.py | 设计取舍，归 P11 |
| K-011 | SQL 只拦写不限读：能读任意表（含 information_schema、别的数据集表） | 拿到接口的人 | app/db.py:_guard | 设计取舍，归 P11/P12；单机自用可接受 |
| K-012 | `session_id` 一路透传但没人用，`insight.summarize()` 的 `prior` 恒空 | 期望会话上下文 | app/agent.py、app/main.py | 设计取舍，归 P10 |
| K-013 | 请求体是裸 dict：字段错给 400 而不是 422，也没有真实 OpenAPI | 前端传错字段 | app/main.py 四个 POST | 已修复（换 pydantic 请求体，语义错误仍走 400/404） |
| K-014 | 超时只用 1s 递归查询验过，默认 10s 没压测 | 真实大表长查询 | app/db.py:exec_sql | 已修复（2026-09-20 压测：默认 10s 处被 `interrupt()` 打断，实测 10.0s） |
| K-015 | 存储只增不减：上传文件不清理，也没有删除接口 | 长期使用 | app/main.py、app/store.py | 已修复（`DELETE /datasets/{id}`；上传失败当场删文件） |
| K-016 | 并发无策略：DuckDB 连接各开各的、SQLite 每次新建连接 | 并发请求 | app/db.py、app/store.py | 已修复（P21：`exec_sql` 内部走 `run_blocking`，签名不变；连接仍按次新建，成瓶颈再上连接池） |
| K-017 | 测试在沙箱里跑会因 `tmp_path` 不可写 40+ ERROR | 换机器或接 CI | tests/* | 环境限制不是代码 bug：测试必须授权运行，接 CI 前先解决 |
| K-018 | 表名/数据集 id 里的数字被当可疑数字（`ds_d_fc466e97` → 2、3、466、97） | 结论里提到表名 | app/insight.py:_unsupported_numbers | 已修复（比对前用 `IDENT` 剔标识符） |
| K-019 | 夹具不隔离 `config/local.json`：配过密钥的机器上真出网，断言失败还烧额度 | 本机有 `api_keys` 时跑测试 | tests 夹具 | 已修复（夹具隔离 `LOCAL_SETTINGS`，断言未改） |
| K-020 | `pyproject.toml` 的 `addopts=-q` 与命令里的 `-q` 叠成 `-qq`，汇总行被吃掉 | 跑 `pytest tests -q` | pyproject.toml | 已修复（去掉 `addopts`） |
| K-021 | 结果表含 datetime 列时 `/ask`、`/insight` 直接 500（`TypeError: datetime is not JSON serializable`） | 数据集有日期列且要走结论 | app/insight.py:_user_prompt | 已修复（`json.dumps(..., default=str)`） |
| K-022 | `/capabilities` 里 `stats` 恒为 false，但 `POST /stats` 正常可用 | 看能力面板判断「统计能不能用」 | app/registry.py | 已修复（B 类补丁选 ①：`registry.register("stats", ...)` 返回 `db.describe`；§5 已登记、P0 断言同步。不删 CAPABILITIES 是为保住「声明过就有工厂」这条不变式） |
| K-023 | `store.log_capability()` 长期只有 agent 一处调用：能力上下线不落 `capability_log`，且 `tools.call()` 自己不查白名单（只在 `all_tools()` 过滤给模型看的清单） | 想查能力流水，或从新入口直调工具时 | app/tools.py:call | 已修复（2026-09-20 第六轮：白名单校验收进 `tools.call()`，越权抛 `ToolError` 并记 `capability_log(capability=tools, event=error)`；已回 §4.8 登记。原描述「零调用者」有误——`agent._run_step` 自 P13 起就在调它，同轮更正。能力上下线暂无可记事件：`ENABLE_*` 都在 import 时求值，没有运行期切换，等有运行期开关的阶段再接） |
| K-024 | `trace_spans` 只建表没人写：没有成本与耗时台账 | 想算成本或看调用链 | app/store.py:30 | 设计取舍，归 P16 |
| K-025 | 模型 profile 的 `purpose` 只存不读：写 `["insight"]` 也照样用它出 SQL | 配多 profile 分用途 | app/main.py:268 | 设计取舍，归 P9（§4.6 要求 SQL 用小模型、结论用强模型） |
| K-026 | 删除数据集只沿 `tasks` 表级联：`tasks` 行缺失时（K-005 之前的老数据、`insert_task` 写失败、进程在两步之间崩掉）`agent_steps` 会留成永久孤儿，再删数据集也带不走 | 清理或核对流水时 | app/store.py:139 delete_dataset | 本机残留已清（2026-09-20 清掉 6 行历史孤儿，备份在 `%TEMP%\meta-before-orphan-clean.sqlite`）；彻底防住要给 `agent_steps` 加 `dataset_id`，那是改 §4.4 冻结表结构，不做；`tasks` 写入失败时的兜底随 P16 观测一起做 |
| K-027 | CI 上 7 个测试模块在收集阶段全 ModuleNotFoundError | GitHub runner 以控制台脚本方式启动 pytest，仓库根不在 sys.path | pyproject.toml（[tool.pytest.ini_options]） | 已修复（`pythonpath=["."]`，`3762671`） |
| K-028 | `list_skills` / `use_skill` 注册了却不在 `config/tools.json` 的默认 `allow` 里：开了 `ENABLE_SKILLS` 模型也看不到技能工具 | 想用技能但只设了开关 | config/tools.json | 已修复（2026-09-20：两个名字加进默认 `allow`，P13 `/tools` 断言同步为 5 个名字；`ENABLE_SKILLS=false` 时调用给可读拒绝，要藏起来就从 `allow` 删） |
| K-029 | 技能脚本继承服务进程环境（含 API 密钥），只有 cwd + 超时限制，不是容器级隔离 | 导入来路不明的 script 型技能 | app/providers/skills.py:run_script | 设计取舍（P15 沙箱落在 `run_code` 这一条，技能脚本仍按「项目内可信代码」处理，本段没动 skills.py）；代码里留了 ponytail 注释 |
| K-030 | 技能只能导入与取用，没有启用/停用端点：禁用要改 `SKILL.md` 的 `enabled: false` 再导入 | 想在页面上直接开关某个技能 | app/providers/skills.py | 设计取舍（§4.3 只冻结了 `GET/POST /skills`）；等有前端技能面板时再加 |
| K-031 | prompt 型技能正文原样进上下文，没做 §7 的注入清洗（包裹标记 + 剥离指令性内容） | 导入别人给的技能 | app/providers/skills.py:use_skill | 设计取舍（技能是用户自己装的提示词，按项目内可信处理）；来路不明的技能正文要清洗，归 P16 注入专项 |
| K-032 | 知识库检索有两点上限：查询词短于 3 字符走 LIKE 全表扫（库大时会慢），中文只按整串短语命中（没有分词与同义扩展） | 库很大，或问法换词 | app/providers/kb.py、app/store.py:search_kb | 设计取舍（FTS5 `trigram` 是纯标准库方案里唯一能按子串命中中文的；`unicode61` 实测搜不到连续中文）；分词/同义/语义召回归 P14 |
| K-033 | 知识库片段的注入防护只到「包标记 + 行级黑名单」：拆行、换说法的指令仍可能带进去 | 导入来路不明的文档 | app/providers/kb.py:strip_instructions | 设计取舍，完整注入专项归 P16（与 K-031 同类） |
| K-034 | `insight.summarize()` 吃不到知识库召回片段与技能正文：§4.5「输入」承诺了「知识库召回片段（带出处）+ 启用的 skill」，实际只拿到问题/SQL/结果表/画像 | 走真模型链路提问，agent 明明检索过知识库 | app/main.py:300 _attach_insight、app/insight.py:_user_prompt | 真缺口（P4 早于 P6/P7）：实测同一次 `/ask` 里 agent 用了 `search_kb` 并引用了出处，insight 段却写「未检索到知识库口径说明」，同一响应自相矛盾；`prior` 恒空是 K-012，本条是 KB/skill 那半边，归 P10 上下文拼装（与 K-012 同处改，改 `summarize` 入参要同步 P4 验收） |
| K-035 | 未定义指标只认「中文词 + 口径后缀」（`app/metrics.py` 的 `_SUFFIXES`/`_CANDIDATE`）：`ROI`、`DAU` 这类纯英文缩写不会被标出来；反过来带后缀的普通词（如「数量」）可能被误标成未定义指标 | 提问里混用英文缩写，或句子里出现带后缀的非指标词 | app/metrics.py:undefined_terms | 设计取舍（ADR-20 只认定义文件，宁可少标也不臆造公式；正则只求覆盖常见中文说法）；要更准得把判定交给模型或加同义词词典，归 P16 评测与安全加固 |
| K-036 | `mcp` 2.x 的 `ClientSession` 不再自动握手：直接发 `tools/list` 会被 server 拒成 -32602 | 想用低层 ClientSession 手搓 MCP 客户端 | app/providers/mcp_client.py:_session | 已解决（改用高层 `mcp.Client(params, mode="auto")`，握手交给 SDK 协商）；留此条免得 P9 及后续再踩 |
| K-037 | `mcp_client` 复用 `providers/kb.py` 的 `strip_instructions`，形成 provider 之间的横向 import | 想保持 provider 互为叶子 | app/providers/mcp_client.py | 设计取舍（§7 的指令剥离只留一份实现，复制等于两处维护）；要彻底解耦就把 `strip_instructions` 上提到公共模块，成本很小 |
| K-038 | 约束与技能文件散在四处（全局 `~/.codex/AGENTS.md`、技能源 `Documents\skill\`、已安装副本 `~/.agents/skills`、插件缓存），改副本会被 `sync.ps1` 或重装覆盖；`handoff`/`implement` 的正文还与仓库流程冲突（交接写临时目录、引用不存在的 `/tdd`） | 改约束、技能或交接流程时 | 全局 AGENTS.md、skill-router、handoff、implement | 已解决（2026-09-20：权威地图写进全局 AGENTS.md「改约束文件前先答两个问题」块；skill-router 改源后同步、两份 SHA256 一致；handoff 加「仓库流程优先」；implement 去掉 `/tdd`、`/code-review` 与 typecheck） |

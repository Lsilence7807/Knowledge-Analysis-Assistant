# 代码台账

用途：记录每个已完成代码文件的作用、所属阶段与已知问题。查错、改错先看这里，不用满仓库翻。

## 维护规则

- 每段验收通过后更新一次，由 AGENT 负责，只增不删。
- 状态取值：已完成 / 待修改 / 已修复。
- 「已知问题」写清现象、触发条件、所在函数、是否已修；修好后改状态并保留原行。

## 已拍板决策

| 日期 | 事项 | 决定 | 影响 |
|---|---|---|---|
| 2026-09-20 | 项目名称 | 统一为「AI 数据分析助手」（README 已改） | 文档、汇报口径一致 |
| 2026-09-20 | P3 模型厂商 | DeepSeek；密钥走环境变量 `LLM_API_KEY`。`config/models.json` 已是 `api.deepseek.com/v1` + `deepseek-chat`，不用改配置 | 密钥来源已被下一行覆盖：改为本地文件优先 |
| 2026-09-20 | 前端图表库 | P5 把 ECharts 落到本地 `web/vendor/echarts.min.js`（单文件、离线可用、不走 CDN） | 桌面离线端可出图；文件缺失时页面只出表格不报错 |
| 2026-09-20 | 模型密钥注入 | 本地文件 `config/local.json` 的 `llm_api_key` 优先（页面写入），环境变量 `LLM_API_KEY` 兜底；调用时读取，保存即生效；页面配置入口在 P5 的 `GET/PUT /settings/model` | 不用 `setx`、不用重启服务；密钥不进 git（`.gitignore` 已挡）（按 profile 存密钥与路由名由下一行细化） |
| 2026-09-20 | 模型接入方式 | 页面可配任意 OpenAI 兼容厂商：P5 的 `GET/PUT /settings/models` 写 `config/local.json`（`models` 与 `models.json` 合并、`api_keys` 按 profile 存）；密钥解析顺序 本地文件 → 该 profile 的 `api_key_env` → 通用 `LLM_API_KEY` | 换厂商不用改代码；用户已明确要求「任何大模型 API Key 都能在页面上配」 |
| 2026-09-20 | P13 范围 | P13 只做 `GET /tools`；`GET /sessions/{sid}/steps` 归 P10 | 避免提前发明会话语义；步骤已随 `/ask` 响应与 `agent_steps` 表落库，P10 补读路由即可 |
| 2026-09-20 | `/ask` 默认路径 | `ENABLE_AGENT=true`（默认）走 agent 循环，`false` 回落 P3 单跳；P3 测试文件用 autouse 夹具钉住 `false`，验的就是那条降级通道 | P0-P3 全绿不变；P5 拿到的响应形状与 P3 相同，只多出 `steps` |
| 2026-09-20 | 数据集上下文传给工具 | 工具函数签名带 `dataset_id`，由 `agent` 每步注入参数，不放进给模型的 JSON Schema | 注册表在 import 时就完整（`/tools` 随时可列），进程内没有跨题共享状态，两轮提问不会串数据集 |
| 2026-09-20 | P4 结论怎么并进 `/ask` | `insight.summarize()` 只由 `main.py` 的 `_attach_insight()` 调用：agent 与 P3 两条路径的响应都补 `insight`；不动 `agent.run()` 返回值和 `AgentResult` | 加 insight 不用改 P13 契约；`test_p3_nlu.py`、`test_p13_agent.py` 只补了模型替身（多答一跳 insight），断言未改 |
| 2026-09-20 | 数字回溯与空表 | 结论里出现、结果表里找不到的数字写进 `caveats`（不整体降级）；结果表 0 行时不调模型、`insight` 留空且不记 degraded | 数字可疑时用户仍看得到结论与提示；空表省一次模型调用 |
| 2026-09-20 | 修复排期分三类 | A 类零风险（只做新建文件 / 新路由 / 编排层插调用，如 K-005、K-015）；B 类要先回设计文档登记并同步改验收命令（K-002、K-006、K-008、K-009、K-013）；C 类属后续阶段，现在不做（K-010、K-011、K-012、K-014、K-016） | A 类随手修且全量回归必绿；B 类会碰老测试文件，属流程允许的「同步改验收命令」；C 类提前做等于越界，违反设计 §5/§7 |
| 2026-09-20 | 顺序：P5 先做 | 先把 P5 前端做完并手工验真实链路（一次验掉 K-004/K-007 与 P4 的 insight），再回头清台账 A/B 类 | 真实模型链路是回归测试发现不了的盲区，越早验越便宜；台账修复不阻塞 P5 |
| 2026-09-20 | ECharts 已落地 | 已用本机代理下载 ECharts 5.6.1（Apache-2.0，1.0MB）到 `web/vendor/echarts.min.js` 并提交 | P5 直接引用本地文件；文件表等 P5 验收时一起登记 |
| 2026-09-20 | P5 保存即启用 | `PUT /settings/models` 写盘后把 `default` 设成该 profile：页面点「保存并启用」后，下一个请求就用它，不用重启 | 换厂商/换密钥只有一条生效路径，不另造「激活」概念 |
| 2026-09-20 | P5 密钥只回 has_key | `GET/PUT /settings/models` 只回 `id/provider/base_url/model/has_key` | 密钥明文不出网关；页面不回显，改密钥必须重新输入 |
| 2026-09-20 | P5 静态挂载位置 | `web/` 用 `StaticFiles(html=True)` 挂在 `/`，且必须排在全部 API 路由之后 | `Mount("/")` 兜住所有未匹配路径，挂前面会抢走 API 路由 |

## 已完成文件

| 文件 | 作用 | 阶段 | 状态 | 已知问题 |
|---|---|---|---|---|
| app/__init__.py | 包标记 | P0 | 已完成 | 无 |
| app/config.py | 集中读环境变量、本地设置（config/local.json）与路径；`api_key(profile)` 决定密钥来源；P5 加 `save_settings()` 合并写盘 | P0/P3/P5 | 已完成 | 无 |
| app/registry.py | 能力注册表，新能力的唯一接入点（P3 注册 query、llm，P13 注册 agent，P4 注册 insight） | P0/P3/P4/P13 | 已完成 | 无 |
| app/schemas.py | HTTP 边界模型（P4 加 Insight/Finding/Suggestion） | P0/P4 | 已完成 | 其余模型按阶段追加 |
| app/store.py | SQLite 建表与元数据读写（P13 加 agent_steps、capability_log 的写入） | P0/P1/P13 | 已完成 | 无 |
| app/db.py | DuckDB 唯一出口：查询守卫、超时、行数上限、描述统计与异常检测 | P0/P2 | 已完成 | 见 K-002 |
| app/main.py | HTTP 路由与装配点（P2 加 /query、/stats，P3 加 /ask，P13 加 /tools 与 agent 路径，P4 加 /insight 并把结论并入 /ask，P5 加静态挂载与 /settings/models） | P0/P1/P2/P3/P4/P5/P13 | 已完成 | 无 |
| config/models.json | 模型接入配置 | P0 | 已完成 | 仅 local 一个 profile，P9 扩充 |
| config/tools.json | 工具白名单、超时、步数预算 | P0 | 已完成 | 工具未注册，P13 生效 |
| tests/test_p0_skeleton.py | P0 验收测试 | P0 | 已完成 | 能力全 false 那条显式关掉密钥，本机配了密钥也不会误红 |
| app/ingest.py | CSV/XLSX 清洗、画像与 DuckDB 落表 | P1 | 已完成 | 无 |
| tests/test_p1_ingest.py | P1 数据摄入验收测试 | P1 | 已完成 | 无 |
| tests/test_p2_sql.py | P2 查询与统计验收测试（守卫、超时、截断、统计与异常） | P2 | 已完成 | 无 |
| app/models.py | 模型 profile 读取与解析（models.json 打底，config/local.json 里的页面配置覆盖） | P3 | 已完成 | 无 |
| app/llm.py | 唯一模型出入口：结构化 JSON 输出、重试与原文截断；P13 加 `chat_tools` | P3/P13 | 已完成 | 见 K-004、K-007 |
| app/nlu.py | 单跳 自然语言 → SQL（P13 后作为降级通道保留） | P3 | 已完成 | 见 K-004 |
| tests/test_p3_nlu.py | P3 验收测试（成功、无密钥、非法输出、危险 SQL 降级） | P3 | 已完成 | 有 autouse 夹具把 `ENABLE_AGENT` 钉成 false；P13 后它验的是降级通道，不是 `/ask` 默认路径；P4 后模型替身要答 insight 那一跳；`client` 夹具已隔离 `LOCAL_SETTINGS`（见 K-019） |
| tests/fixtures/dirty.csv | P1 清洗测试夹具 | P1 | 已完成 | 无 |
| .vscode/、.github/copilot-instructions.md | VSCode 集成与助手说明 | P0 | 已完成 | 无 |
| app/tools.py | 工具注册表（register / all_tools / call）与 3 个只读工具 run_sql、describe_stats、detect_anomalies | P13 | 已完成 | 见 K-006 |
| app/agent.py | Agent 循环：plan → 调工具 → 观察 → 再规划；步数预算、单步截断、步骤落库，产出 /ask 响应 | P13 | 已完成 | 见 K-006、K-007 |
| tests/test_p13_agent.py | P13 验收测试（多跳、单跳、落库、白名单、未知工具、超预算、工具异常、单步截断、降级、/tools） | P13 | 已完成 | 夹具补了 insight 一跳的模型替身（P4 起 `/ask` 多一次 chat_json） |
| app/insight.py | 结果表 → 结构化结论：提示词版本、限行喂表、数字回溯写 caveats、失败抛带标签的 InsightError | P4 | 已完成 | 见 K-009 |
| tests/test_p4_insight.py | P4 验收测试（字段齐全、数字回溯、缺字段与模型不可用的降级、空表不调模型、/ask 两条路径带结论） | P4 | 已完成 | 无 |
| web/index.html | P5 单文件前端（原生 JS、无构建）：模型配置表单、上传与数据集选择、提问、SQL 折叠、步骤面板、结论卡片、结果表、ECharts 图表切换、降级提示条；ECharts 缺失时只提示不出图 | P5 | 已完成 | 无 |
| web/vendor/echarts.min.js | 本地 ECharts 5.6.1（Apache-2.0，1.0MB），离线出图，不走 CDN | P5 | 已完成 | 无 |
| tests/test_p5_flow.py | P5 验收测试（首页与静态资源、上传→提问→结论全链路、/settings/models 不回传密钥且保存即生效、缺字段 400、密钥可清空） | P5 | 已完成 | 无 |

## 已知问题汇总

| 编号 | 现象 | 触发条件 | 位置 | 状态 |
|---|---|---|---|---|
| K-001 | SQL 守卫只拦多语句与非 SELECT，注释绕过、超时、行数上限未实现 | 任何 SQL 执行 | app/db.py:_guard | 已修复（P2：按解析结果判语句类型 + 拒注释与分号 + 10s 中断 + 5000 行截断） |
| K-002 | 守卫一刀切：字符串字面量里出现 `--`、`/*` 或 `;` 也会被拒（如 `SELECT 'a;b'`） | 写这类 SQL | app/db.py:_guard | 设计取舍，暂不修；要放开需按引号切分后再判注释与分号 |
| K-003 | `llm.chat_tools()` 未实现（设计 §3.1 契约里有），P13 才需要 | 调工具循环 | app/llm.py | 已修复（P13 实现：tool_calls 解析成 dict，参数 JSON 坏了当空参，交给工具层报「参数不匹配」） |
| K-004 | 真实模型链路未验证：本机没有密钥，P3/P4 测试全部走 `_complete` 替身，未出网（P4 的 insight 一跳同样只在替身下验过） | 有密钥的真实调用 | app/llm.py:_complete | 已修复（P5 手工验证：页面配 `deepseek-flash` 后真实出网，`/ask` 出 SQL 与结果表、insight 出完整结论；单跳与多跳同一批一起验） |
| K-005 | `/ask` 不落 `tasks` 表，`task_id` 只回给调用方（agent 路径同样不写） | 需要任务台账时 | app/main.py:ask | 待修改（P16；步骤已落 agent_steps） |
| K-006 | `describe_stats` / `detect_anomalies` 没有独立超时：tools.json 写的 15s 不生效，实际走 db.describe 固定的 10s | 大表跑统计 | app/tools.py:_describe_stats、_detect_anomalies | 待修改（要让 `db.describe` 收 `timeout_s`，属改 P2 契约，需先回设计文档登记） |
| K-007 | 真实模型的工具调用链路未验证：P13 测试全走 `llm.chat_tools` 替身，未出网（含 tools 参数与 tool_calls 回填的真实格式） | 有密钥的真实提问 | app/llm.py:chat_tools | 已修复（P5 手工验证：真实提问跑出 2 步 `run_sql`，说明 tools 参数与 tool_calls 回填的真实格式可用） |
| K-008 | 单步截断用 `max_rows_per_step`(200) 当 `db.exec_sql` 的 limit：提问最后一步行数上限 200，比 `/query` 的 5000 小 | 提问返回超过 200 行 | app/tools.py:_run_sql | 设计取舍，暂不修；要放宽改 config/tools.json |
| K-009 | 数字回溯只认字面数字：模型从表里算出来的汇总值（合计、比率、环比）在表里没有原文，会被判成「找不到」写进 caveats，数字多的结论 caveats 会偏吵 | 结果表里没有该数字原文时 | app/insight.py:_unsupported_numbers | 设计取舍，暂不修；要做对得让模型输出引用的行号或单元格，属改 P4 契约 |
| K-010 | 无鉴权、无 CORS，`RATE_LIMIT_PER_MIN` 读了从没用过：服务只该监听回环地址 | 监听非回环地址或多人访问 | app/config.py:18、app/main.py | 设计取舍，归 P11（设计 §7 明写「不做鉴权与多用户」） |
| K-011 | SQL 只拦写不限读：`/query` 与 agent 的 run_sql 能读库内任意表（含 information_schema、其他数据集表） | 任何拿到接口的人 | app/db.py:_guard、app/tools.py:_run_sql | 设计取舍，归 P11/P12；单机自用可接受 |
| K-012 | `session_id` 一路透传但无人使用，`insight.summarize()` 的 `prior` 恒传空列表 | 期望会话上下文时 | app/agent.py:run、app/main.py:ask | 设计取舍，归 P10（设计 §7 明写「不写 session 逻辑」） |
| K-013 | HTTP 请求体是裸 `dict`：字段错是 400 或默认值而不是 422，也生成不出真实 OpenAPI 契约 | 前端传错字段 | app/main.py（/query、/stats、/ask、/insight） | 待修改（B 类：改 HTTP 契约，先回设计文档登记 + 同步改验收命令） |
| K-014 | 超时只用 1s 递归查询验过：默认 10s 没压测，机制是 `threading.Timer` + `conn.interrupt()` | 真实大表长查询 | app/db.py:exec_sql | 待验证，归 P16/P21 |
| K-015 | 存储只增不减：上传文件落在 `data/files/` 无清理，也没有数据集删除接口 | 长期使用 | app/main.py、app/store.py | 待修改（A 类：新增函数与路由即可，零风险；删除/重导入端点归 P20） |
| K-016 | 并发无策略：DuckDB 写连接与只读连接各开各的、SQLite 每次新建连接，Windows 上并发上传+查询可能撞锁 | 并发请求 | app/db.py、app/store.py | 设计取舍，归 P21（`exec_sql` 内部改 run_blocking，签名不变） |
| K-017 | 测试必须在授权（非沙箱）下跑：沙箱内系统临时目录不可写，`tmp_path` 夹具直接 40+ ERROR | 换机器或接 CI | tests/* | 环境限制，不是代码 bug；接 CI 前先解决 |
| K-018 | 数字回溯会把表名/数据集 id 里的数字当成可疑数字：真实提问的结论里提到表 `ds_d_fc466e97`，caveats 就出现「以下数字在结果表里找不到，可能是模型编的：2、3、466、97」 | 表名或数据集 id 出现在结论文本里（真实提问几乎必然） | app/insight.py:_unsupported_numbers | 待修改（B 类，与 K-009 同源：修 K-009 时一并处理，现在不动） |
| K-019 | 测试夹具不隔离 `config/local.json`：页面配过密钥的机器上，`test_ask_without_key_degrades_and_service_stays_usable` 会把真实密钥当可用密钥，真的出网调用（实测 11s、`degraded == []`），断言失败且花掉用户额度 | 本机 `config/local.json` 里有 `api_keys` 时跑 `tests/test_p3_nlu.py` | tests/test_p3_nlu.py:client 夹具 | 已修复（P5 后补：夹具加 `monkeypatch.setattr(config, "LOCAL_SETTINGS", tmp_path / "local.json")`，断言未改；同批未见其他文件复发，`test_p13_agent.py` 夹具同样没隔离但这批替身全覆盖、暂时无害，留观） |

## 待开工清单（MVP 七段）

| 阶段 | 计划文件 | 验收命令 | 状态 |
|---|---|---|---|
| P0 | app/{main,config,registry,schemas,db,store}.py | test_p0_skeleton | 已完成（5 passed） |
| P1 | app/ingest.py | test_p1_ingest | 已完成（11 passed，全量回归） |
| P2 | app/db.py 补守卫；/query、/stats | test_p2_sql | 已完成（19 passed，全量回归 30 passed） |
| P3 | app/llm.py、app/models.py、app/nlu.py | test_p3_nlu | 已完成（11 passed，全量回归 41 passed） |
| P13 | app/tools.py、app/agent.py | test_p13_agent | 已完成（11 passed，全量回归 52 passed） |
| P4 | app/insight.py | test_p4_insight | 已完成（8 passed，全量回归 60 passed） |
| P5 | web/index.html | test_p5_flow | 已完成（5 passed，全量回归 65 passed；浏览器手测 8 条见交接文档） |

## 待推送记录

| 阶段 | 提交 | 状态 | 说明 |
|---|---|---|---|
| — | — | — | 远端未配置时在此登记 |
| P2 查询与统计 | `89826d0` | 已推送 | 与决定登记 `1fe3adf` 同批推送成功；交接文档已改写为 P3 起点 |
| P3 自然语言转 SQL | 见下一次交接 | 已推送 | 后续阶段按同一流程登记 |
| P3 完成与密钥改造、文档对齐 | 见下一次交接 | 已推送 | 交接文档已改写为 P13 起点（`7e7fc82`→`52501f0`，全量 40 passed） |
| P13 Agent 循环与工具调用 | `7e27c31` | 已推送 | 全量 52 passed；交接文档已改写为 P4 起点 |
| 交接文档改写为 P4 起点 | 见下一次交接 | 已推送 | 与本次 docs 提交同批推送 |
| P4 洞察生成 | `bb75cb8` | 已推送 | 全量 60 passed；交接文档已改写为 P5 起点 |
| 交接文档改写为 P5 起点 | 见下一次交接 | 已推送 | 与本次 docs 提交同批推送 |
| 问题登记与修复排期（K-010~K-017） | 见下一次交接 | 已推送 | docs only，无代码改动；全量回归仍 60 passed |
| P5 前置：ECharts 本地化 | 见下一次交接 | 已推送 | `web/vendor/echarts.min.js`（5.6.1），代码未改，不影响 60 passed |
| P5 前端闭环 | 见下一次交接 | 已推送 | 全量 65 passed；真实模型链路已出网（K-004、K-007 转已修复） |
| P5 后补：K-019 测试隔离 | 见下一次交接 | 已推送 | 一行夹具修复，断言未改；全量 65 passed，耗时 18.5s → 9.9s（不再有真实网络调用） |
| P5 收尾：文档对齐 | 见下一次交接 | 已推送 | docs only：6 处漂移（跑法缺页面地址、手测口径 3 条 vs 8 条、大文档 P5 漏步骤面板、MVP 测试清单缺反例、回归矩阵缺 config.py、功能说明「可复制」口径）；交接文档改写为清台账起点 |

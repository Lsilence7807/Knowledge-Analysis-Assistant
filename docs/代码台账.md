# 代码台账

用途：查已完成文件的作用与已知问题，改错先看这里，不用满仓库翻。
维护：每段验收后更新，只增不删；状态取值 已完成 / 待修改 / 已修复。2026-09-20 压缩过一轮：已修问题只留一行结论，逐段取舍与推送历史看 `git log`（旧版在本文件历史里）。2026-09-20 第二轮：`docs/` 的设计、说明与测试文档整体压缩，逐份体量与省 token 读法见 `docs/索引.md`。

## 状态速览

- 已完成：MVP 七段（P0/P1/P2/P3/P13/P4/P5）+ 台账 A/B 类补丁 + 手工复验（页面删除按钮、DELETE 端点、空态、真实模型链路）+ P5.1 视觉收尾（降级黄条砍成一句、四个视觉旋钮收进 `:root` 变量，不加配置项）。最新提交看 `git log -1`。
- 基线：`& .\.venv\Scripts\python.exe -m pytest tests -q` → **79 passed**；提交前质量门 `ruff check app tests` 零告警 + `ruff format --check app tests` 无差异。
- 2026-09-20 第二轮收尾：真实链路复验通过（上传 → /query → /ask → /insight → DELETE，含 400/404/422 边界；真实模型 7.3s / 4.6s 出 SQL 与结论）；文档对齐一轮（P5.1 标 ✅、§4.2 补 `hint`、§4.4 已建表清单、§4.5 补 `insight:llm`、§4.6 现状说明、§4.8 工具引入阶段、§7 上传后缀、§10 补 `ENABLE_AGENT`、测试清单夹具口径）；K-022（`/capabilities.stats` 恒 false）按 ① 修掉（registry 补注册 + P0 断言按依赖分组）；同轮顺手：`README.md` 对齐端口与 venv 口径、台账补 K-023/K-024/K-025 三条「声明未接线」、图表色板 `chartTheme` 改读 `:root` 变量（主色从此改一处生效）；清掉 `data/meta.sqlite` 里 6 行历史孤儿 `agent_steps`（K-026）；§5 依赖图对齐——P20 从 P5 的分支改挂 P10 链，与 P20 行「前置 P10」一致。**下一步计划（锁依赖 → 加 CI → 开 P21）已写进 `交接-2026-09-20.md` §2，新会话照它开工。**
- 待推送：无，远端 `main` 已同步。
- 2026-09-20 第三轮：① 锁依赖完成——新建 `requirements.lock`（74 条，含传递依赖，无代理/密钥），`README.md` 补一行复现说明；验收 `pytest tests -q` → 79 passed。② CI、③ P21 未做，照 `交接-2026-09-20.md` §2 接。
- 2026-09-20 第四轮：② CI 完成——`.github/workflows/ci.yml`（checkout@v5 / setup-python@v6 → `pip install -r requirements.lock` → `ruff check` / `ruff format --check` → `pytest tests -q`）。根因：runner 上 `pytest` 以控制台脚本启动，仓库根不在 `sys.path`，7 个测试模块收集阶段全 ModuleNotFoundError（K-027）；修法 `pyproject.toml` 加 `pythonpath = ["."]`（`3762671`），随后撤掉临时失败注解步骤（`e8c76b8`）。验收：GitHub Actions 变绿 + 本地 `pytest tests -q` 79 passed。③ P21 未做。
- 2026-09-20 第五轮：③ P21 完成——新建 `app/exec.py`（统一阻塞执行器，线程池大小读 `EXEC_MAX_WORKERS` 默认 4）、`bench/bench.py` + `bench/baseline.json`（50 万行聚合 p95 实测 0.0619s，预算 1.5s）、`tests/test_p21_perf.py`（6 例：执行器线程、`exec_sql` 经 `run_blocking`、50 万行预算、心跳 p95 ≤200ms + 10 并发无 5xx、/bench 退化）；改动 `db.exec_sql` 内部走 `run_blocking`（签名不变）、`config` 加 `EXEC_MAX_WORKERS`/`BENCH_BASELINE`、`main` 加 `GET /bench`；顺带清 K-016、补压 K-014（默认 10s 超时实测 10.0s 打断）。验收 `pytest tests/test_p21_perf.py -q` 6 passed + 全量 85 passed + ruff 干净。
- 文档：`系统总体设计.md` 856→553 行（§4 契约段一字未动）、`系统测试清单.md` 280→113 行、`功能介绍与技术栈.md` 204→67 行；MVP 三份（系统设计/功能说明/测试清单）并成 `最小可用集.md`；设计文档的「验收总表」并入 §5，末节改为「运行与配置」。
- 后续：C 类问题（K-010、K-011、K-012、K-014、K-016）归后续阶段，做到那几段再清；其余阶段按 `系统总体设计.md` §5 阶段表接（§6 是隔离机制，§10 是环境变量与质量门）。

## 仍然有效的约定（改代码前先看）

| 事项 | 约定 |
|---|---|
| 产品名 | `Knowledge-Analysis-Assistant`；安装包 `…-Setup.exe`；数据在 `%APPDATA%\Knowledge-Analysis-Assistant` |
| 模型与密钥 | 页面可配任意 OpenAI 兼容厂商，写 `config/local.json`；密钥顺序 本地文件 → profile 的 `api_key_env` → `LLM_API_KEY`；调用时读取，保存即生效；`GET/PUT /settings/models` 只回 `id/provider/base_url/model/has_key`，明文不出网关；`PUT` 写盘后把 `default` 设为该 profile |
| 静态挂载 | `web/` 用 `StaticFiles(html=True)` 挂在 `/`，必须排在全部 API 路由之后 |
| `/ask` 两条路径 | `ENABLE_AGENT=true`（默认）走 agent 循环，`false` 回落 P3 单跳；响应形状相同，agent 多一个 `steps` |
| 工具 | 工具签名带 `dataset_id`，由 agent 每步注入，不进给模型的 JSON Schema；白名单、超时、步数预算读 `config/tools.json`，每次调用重读（免重启） |
| 取数与上下文 | 取数按 `/query` 口径 5000 行（末步结果表不再被截到 200 行）；回给模型的超 200 行只回列名与行数（K-008） |
| 数字追溯 | findings 逐条给 `row`（结果表 0 基）即视为整段已追溯；没指认才按字面比对，且先剔掉表名/列名里的数字（K-009/K-018） |
| 结论并入 `/ask` | 只由 `main._attach_insight()` 调 `insight.summarize()`，不动 `agent.run()` 返回值 |
| 修复排期 | A 类（新建文件 / 新路由 / 编排层插调用）随手修；B 类先回设计文档登记再改并同步验收命令；C 类归后续阶段 |
| 前端 | 单文件原生 JS、无构建链；ECharts 走本地 `web/vendor/echarts.min.js`（5.6.1），文件缺失时只出表格不报错 |
| 删除数据集 | `DELETE /datasets/{id}` 清 DuckDB 表 + 原始文件 + 元数据与流水；P20 正式删除/重导入在此基础上扩展 |
| 桌面打包 | 不做 onefile、不做代码签名 |

## 已完成文件

| 文件 | 作用 | 阶段 | 状态 | 注意 |
|---|---|---|---|---|
| app/__init__.py | 包标记 | P0 | 已完成 | 无 |
| app/config.py | 环境变量、本地设置（`config/local.json`）与路径；`api_key(profile)` 定密钥来源；`save_settings()` 合并写盘；`EXEC_MAX_WORKERS`（默认 4）与 `BENCH_BASELINE` | P0/P3/P5/P21 | 已完成 | 无 |
| app/registry.py | 能力注册表，新能力的唯一接入点（query / llm / agent / insight 都在这里注册） | P0/P3/P4/P13 | 已完成 | 无 |
| app/schemas.py | HTTP 边界模型：Insight/Finding/Suggestion 与四个 POST 请求体（QueryIn/StatsIn/AskIn/InsightIn）；Finding 带 `row`/`column` | P0/P4/K-013/K-009 | 已完成 | 其余模型按阶段追加 |
| app/store.py | SQLite 建表与元数据读写（datasets / tasks / agent_steps / trace_spans / capability_log） | P0/P1/P13+A 类 | 已完成 | 无 |
| app/db.py | DuckDB 唯一出口：守卫（注释与分号只判代码部分）、超时、5000 行上限、描述统计与异常检测；`exec_sql` 内部走统一执行器 `run_blocking`（签名不变） | P0/P2+A 类/K-002/K-006/P21 | 已完成 | 无 |
| app/main.py | HTTP 路由与装配点：health、capabilities、datasets（增/查/画像/删）、query、stats、tools、ask、insight、settings/models、bench（读性能基线）；静态挂载写在最后 | P0~P5/P13/P21 | 已完成 | 无 |
| config/models.json | 模型接入配置 | P0 | 已完成 | 只有 local 一个 profile，P9 扩充 |
| config/tools.json | 工具白名单、每步超时、步数预算、回模型的上下文行数上限 | P0 | 已完成 | 无 |
| app/ingest.py | CSV/XLSX 清洗、画像与 DuckDB 落表 | P1 | 已完成 | 无 |
| app/models.py | 模型 profile 读取与解析（`models.json` 打底，`config/local.json` 覆盖） | P3 | 已完成 | 无 |
| app/llm.py | 唯一模型出入口：结构化 JSON 输出、重试、原文截断、`chat_tools` | P3/P13 | 已完成 | 无 |
| app/nlu.py | 单跳 自然语言 → SQL（P13 后作为降级通道保留） | P3 | 已完成 | 无 |
| app/tools.py | 工具注册表（register / all_tools / call / kinds / settings）与 3 个只读工具：run_sql、describe_stats、detect_anomalies | P13/K-006/K-008 | 已完成 | 无 |
| app/agent.py | Agent 循环：规划 → 调工具 → 观察 → 再规划；步数预算、回模型的单步结果截断、步骤落库，产出 /ask 响应 | P13/K-008 | 已完成 | 无 |
| app/insight.py | 结果表 → 结构化结论：提示词版本、限行喂表（datetime 兜底）、数字追溯写 caveats、失败抛带标签的 InsightError | P4/K-009/K-018/K-021 | 已完成 | 无 |
| web/index.html | 单文件前端：模型配置、上传与数据集选择、删除按钮（confirm → DELETE）、提问、SQL 折叠、步骤面板、结论卡片、结果表与图表切换、降级提示 | P5+A 类+P5.1 | 已完成 | P5.1 换了视觉（顶栏「知析」字标 + 奶油纸底/粗描边/硬投影），JS 与 id 一律没动；旋钮集中在 `:root`（主色 `--violet`、描边 `--stroke`、卡片密度 `--pad`、圆角），调值改一行即可；降级黄条只回原因码，不贴模型原文（原文仍在响应 `message` 与 `tasks.error`）；图表色板 `chartTheme` 读 `:root` 变量（第 5 色 `#B08C00` 与字标 SVG 的 hex 仍硬编码）；改样式别碰 id 与事件绑定 |
| web/vendor/echarts.min.js | 本地 ECharts 5.6.1（Apache-2.0），离线出图 | P5 | 已完成 | 不引 CDN |
| pyproject.toml | 质量门唯一配置源：ruff（line-length 120、select E/F/I/UP/B/BLE、ignore B008、insight.py 免 E501）与 pytest（只留 testpaths） | P0 起 | 已完成 | 别再加 `addopts`（K-020） |
| .editorconfig / .gitattributes | 换行、编码、缩进统一（LF） | P0 起 | 已完成 | 无 |
| README.md | 项目入口：定位、怎么跑、验收命令、文档地图 | P0 | 已完成 | 无 |
| requirements.lock | 可复现环境锁定：`requirements.txt` 声明 + 传递依赖闭包（74 条），取本机 `.venv` 已装版本 | P0 起 | 已完成 | 不是 `pip freeze` 原样导出；改依赖先改 `requirements.txt` 再重生成；按 Windows 求值，不含 uvloop |
| .vscode/、.github/copilot-instructions.md | VSCode 集成与助手说明 | P0 | 已完成 | 无 |
| tests/test_p{0,1,2,3,13,4,5}_*.py（7 个验收文件） | 每段一个验收文件，命令见各段设计文档；用例数 p0 5 / p1 8 / p2 22 / p3 12 / p13 13 / p4 13 / p5 6 | 各段 | 已完成 | 全仓库夹具都隔离 `config/local.json`（K-019）；p3 夹具把 `ENABLE_AGENT` 钉成 false（验降级通道）；p13 与 p4 的模型替身要答 insight 那一跳；p0 验「能力全 false」那条显式关掉密钥 |
| tests/fixtures/dirty.csv | P1 清洗测试夹具 | P1 | 已完成 | 无 |
| app/exec.py | 阻塞任务统一执行器：固定线程池（`EXEC_MAX_WORKERS`，默认 4）+ `run_blocking()`，DuckDB/pandas/HTTP/子进程都从这里排队 | P21 | 已完成 | 池是单进程线程池，p95 不达标再上进程池或只读副本 |
| tests/test_p21_perf.py | P21 验收 6 例：执行器线程、`exec_sql` 经 `run_blocking`、50 万行聚合 1.5s 预算、心跳 p95 ≤200ms + 10 并发无 5xx、/bench 退化行为 | P21 | 已完成 | 心跳用 `asyncio.run` + `httpx.ASGITransport`，不依赖 pytest-asyncio 的自动模式 |
| bench/bench.py | 本地压测：临时目录造 50 万行表，量 20 次聚合 p50/p95，写 `bench/baseline.json`；超 1.5s 预算或比上次退化 ≥1.3× 非零退出 | P21 | 已完成 | 直接 `python bench/bench.py`（自带 sys.path 补丁，同 K-027 的坑） |
| bench/baseline.json | 性能基线（本机 12 核：p50 0.0565s / p95 0.0619s / 50 万行 / workers 4） | P21 | 已完成 | 换机器后重跑覆盖，判定口径在 `bench/bench.py` |

## 已知问题

| 编号 | 现象 | 触发条件 | 位置 | 状态 |
|---|---|---|---|---|
| K-001 | 守卫只拦多语句与非 SELECT，注释绕过、超时、行数上限没实现 | 任何 SQL | app/db.py:_guard | 已修复（P2 补全：判语句类型 + 拒注释与分号 + 10s 中断 + 5000 行截断） |
| K-002 | 字面量里出现 `--`/`/*`/`;` 被误拒（如 `SELECT 'a;b'`） | 写这类 SQL | app/db.py:_guard | 已修复（先按引号切分 `_mask_literals` 再判，未闭合引号交解析器兜底） |
| K-003 | P13 需要的 `llm.chat_tools()` 未实现 | 工具循环 | app/llm.py | 已修复（P13 实现；参数 JSON 坏了当空参，交工具层报「参数不匹配」） |
| K-004 | 真实模型链路未验证（无密钥，测试全走替身） | 有密钥的真实调用 | app/llm.py:_complete | 已修复（P5 手工验证：真实出网出 SQL、结果表与结论） |
| K-005 | `/ask` 不落 `tasks` | 需要任务台账 | app/main.py:ask | 已修复（两条路径之后各落一行；`status` 由 degraded 决定，写失败只记日志） |
| K-006 | `describe_stats`/`detect_anomalies` 吃不到 tools.json 的 15s | 大表跑统计 | app/tools.py | 已修复（`db.describe` 加 `timeout_s`，两个工具按 tools.json 传入） |
| K-007 | 真实模型的工具调用格式未验证 | 有密钥的真实提问 | app/llm.py:chat_tools | 已修复（P5 手工验证：真实多步 run_sql 可跑） |
| K-008 | 提问末步结果被截到 200 行，比 `/query` 的 5000 小 | 提问返回超 200 行 | app/tools.py:_run_sql | 已修复（取数改 5000；200 只作用于回模型的上下文，见 `agent._shrink`） |
| K-009 | 结论里算出来的汇总值（合计、比率、环比）被判「找不到」，caveats 偏吵 | 表里没有该数字原文 | app/insight.py:_unsupported_numbers | 已修复（Finding 带 `row`/`column`，逐条指认即整体追溯；`PROMPT_VERSION` p4-2） |
| K-010 | 无鉴权、无 CORS，`RATE_LIMIT_PER_MIN` 读了没用过 | 监听非回环地址或多人访问 | app/config.py、app/main.py | 设计取舍，归 P11 |
| K-011 | SQL 只拦写不限读：能读任意表（含 information_schema、别的数据集表） | 拿到接口的人 | app/db.py:_guard | 设计取舍，归 P11/P12；单机自用可接受 |
| K-012 | `session_id` 一路透传但没人用，`insight.summarize()` 的 `prior` 恒空 | 期望会话上下文 | app/agent.py、app/main.py | 设计取舍，归 P10 |
| K-013 | 请求体是裸 dict：字段错给 400 而不是 422，也没有真实 OpenAPI | 前端传错字段 | app/main.py 四个 POST | 已修复（换 pydantic 请求体；只声明类型不加 `min_length`，语义错误仍走 400/404） |
| K-014 | 超时只用 1s 递归查询验过，默认 10s 没压测 | 真实大表长查询 | app/db.py:exec_sql | 已修复（2026-09-20 压测：20 万行三表笛卡尔积在默认 10s 处被 `interrupt()` 打断，实测 10.0s 并回「查询超过 10s 已中断」） |
| K-015 | 存储只增不减：上传文件不清理，也没有删除接口 | 长期使用 | app/main.py、app/store.py | 已修复（`DELETE /datasets/{id}` 清表 + 文件 + 元数据 + 流水；上传失败当场删文件） |
| K-016 | 并发无策略：DuckDB 连接各开各的、SQLite 每次新建连接 | 并发请求 | app/db.py、app/store.py | 已修复（P21 按台账指定修法：`exec_sql` 内部改走 app/exec.py 固定线程池，签名不变；连接仍按次新建，若成瓶颈再引入连接池） |
| K-017 | 测试在沙箱里跑会因 `tmp_path` 不可写 40+ ERROR | 换机器或接 CI | tests/* | 环境限制不是代码 bug：测试必须授权运行，接 CI 前先解决 |
| K-018 | 表名/数据集 id 里的数字被当可疑数字（`ds_d_fc466e97` → 2、3、466、97） | 结论里提到表名 | app/insight.py:_unsupported_numbers | 已修复（字面比对前用 `IDENT` 剔掉标识符） |
| K-019 | 夹具不隔离 `config/local.json`：配过密钥的机器上真出网，断言失败还烧额度 | 本机有 `api_keys` 时跑测试 | tests 夹具 | 已修复（`test_p3_nlu`、`test_p13_agent` 夹具隔离 `LOCAL_SETTINGS`，断言未改） |
| K-020 | `pyproject.toml` 的 `addopts=-q` 与命令里的 `-q` 叠成 `-qq`，汇总行被吃掉 | 跑 `pytest tests -q` | pyproject.toml | 已修复（去掉 `addopts`，只留 `testpaths`） |
| K-021 | 结果表含 datetime 列时 `/ask`、`/insight` 直接 500（`TypeError: datetime is not JSON serializable`） | 数据集有日期列且要走结论 | app/insight.py:_user_prompt | 已修复（`json.dumps(..., default=str)`；手工复验同一数据集复跑 200 OK） |
| K-022 | `/capabilities` 里 `stats` 恒为 false，但 `POST /stats` 正常可用 | 看能力面板判断「统计能不能用」 | app/registry.py | 已修复（B 类补丁，选 ①：`registry.register("stats", ...)` 返回 `db.describe`；已回 `系统总体设计.md` §5 P0/P2 登记，P0 验收断言同步改为「无密钥时 stats 必须 true、其余四个必须 false」。不删 CAPABILITIES 的理由：保住「声明过就有工厂」这条不变式，后段加能力照抄一处） |
| K-023 | `store.log_capability()` 零调用者：越权和能力上下线都没落 `capability_log`（设计 §7 / §4.8 要求记流水） | 想查能力流水时 | app/store.py:174、app/tools.py:call | 待接线：`tools.call()` 自己不查白名单（只在 `all_tools()` 过滤给模型看的清单），补校验与落流水要回 §4.8 走 B 类登记 |
| K-024 | `trace_spans` 只建表没人写：没有成本与耗时台账 | 想算成本或看调用链 | app/store.py:30 | 设计取舍，归 P16 |
| K-025 | 模型 profile 的 `purpose` 只存不读：写 `["insight"]` 也照样用它出 SQL | 配多 profile 分用途 | app/main.py:268 | 设计取舍，归 P9（§4.6 要求 SQL 用小模型、结论用强模型） |
| K-026 | 删除数据集只沿 `tasks` 表级联：`tasks` 行缺失时（K-005 之前的老数据、`insert_task` 写失败、进程在两步之间崩掉）`agent_steps` 会留成永久孤儿，再删数据集也带不走 | 清理或核对流水时 | app/store.py:139 delete_dataset | 本机残留已清（2026-09-20 清掉 6 行历史孤儿，备份在 `%TEMP%\meta-before-orphan-clean.sqlite`）；彻底防住要给 `agent_steps` 加 `dataset_id`，那是改 §4.4 冻结表结构，不做；`tasks` 写入失败时的兜底随 P16 观测一起做 |
| K-027 | CI 上 7 个测试模块在收集阶段全 ModuleNotFoundError | GitHub runner 以控制台脚本方式启动 pytest，仓库根不在 sys.path | pyproject.toml（[tool.pytest.ini_options]） | 已修复（加 `pythonpath = ["."]`；提交 3762671，Actions 变绿） |

# AGENTS.md — AI 数据分析助手

本文件在该目录树内工作时自动加载。它管七件事：阶段流程、注释与路径规范、验证、台账、提交推送、阶段隔离、交接。
与已常驻的 ponytail（少写代码）和 benjamin-plus（少花步骤）叠加使用，不重复它们的内容；验证环节配合 skill `verification-before-completion`，收尾合分支时用 `finishing-a-development-branch`。
文档入口：`docs/索引.md`（全部设计、功能、台账文档的作用与阅读顺序都在里面）。

## 0. 每个阶段的固定动作（顺序不要变）

1. 开段前跑一遍上一段的回归命令，确认还是绿的。
2. 只改设计文档「改动范围」里写死的文件，越界即停。
3. 写完跑本段验收命令，必须 exit 0。
4. 跑全部已完成阶段的测试，必须全绿（这是"下一步不破坏上一步"的唯一证据）。
5. 更新 `docs/代码台账.md`：新增文件、作用、阶段、已知问题。
6. 提交并推送（见第 4 节）。
7. 汇报时贴真实输出，不写"应该没问题"。

## 1. 注释与路径规范

- 每个新建文件开头必须有一段文件头注释，格式固定：

```
# 文件：backend/app/services/ingest.py
# 作用：把上传的 CSV/XLSX 清洗后落成 DuckDB 数据集，并生成数据画像
# 阶段：P1 数据摄入
# 依赖：pandas、duckdb、app/services/store.py
```

- 公共函数用一行 docstring 写清「做什么 + 返回什么」。
- 关键代码（分支、边界、兜底、取舍）前加一行注释说明**为什么**这么做，不复述代码在做什么。
- 禁止：# 遍历列表 / # 返回结果 这类废话注释；禁止裸 TODO（要留就写 `# ponytail: <上限>，<何时升级>`）。
- 注释与报错信息用中文，标识符用英文。

### 路径规则（新代码必须守，验收时抽查）
- **v3.1 骨架改造（F1 后端骨架已落地）**：Python 代码在 `backend/app/`（`api/` 路由 + `core/` 配置与执行器 + `services/` 业务 + `providers/` 能力 + `schemas/` 边界模型），`pythonpath=["backend"]` 兜住所有 `import app.*`；前端源码在 `frontend/`（F2）、构建产物在 `web/dist`（gitignore）。

- 路径一律锚在 `backend/app/core/config.py` 的 `BASE_DIR`（仓库根：`Path(__file__).resolve().parents[3]`），**禁止依赖当前工作目录**。
- 禁止硬编码绝对路径：盘符、`/Users/...`、`/home/...`、用户名、机器名、`%APPDATA%` 字面量都不许出现在代码与 `config/*.json` 里。
- 新增配置、目录、基准文件的路径先加进 `backend/app/core/config.py`，其它模块只从 `config` 取，不在模块里自己拼。
- 测试夹具用 `Path(__file__).parent`，项目外的临时目录用 `tempfile`；不许假定 cwd 是项目根。
- 起子进程显式给 `cwd`（范例：`backend/app/providers/mcp_client.py` 给 `BASE_DIR`、`backend/app/providers/skills.py` 给技能目录）。
- 落库或回给用户的路径存**相对项目根的 posix 路径**（范例 `backend/app/providers/kb.py:_relative`），不存运行时绝对路径。
- 抽查命令（应无输出）：`rg -n --glob '!**/vendor/**' -g '!config/local.json' -e '[A-Za-z]:\\[^nrt]' -e '/Users/' -e '/home/' -e '%APPDATA%' backend/app config tests`
- 换目录也要能跑：`cd $env:TEMP; & <仓库>\.venv\Scripts\python.exe -m pytest <仓库>\tests -q` 必须全绿。

## 2. 代码台账

- 位置：`docs/代码台账.md`，每段验收通过后更新，只增不删。
- 「已完成文件」表：文件 / 作用 / 阶段 / 状态 / 已知问题。
- 已知问题写具体：现象、触发条件、所在函数、是否已修。修好了把状态改成「已修复」并保留行。
- 「待推送记录」记远端不可用时的待办，远端恢复后补推。
- 改错前先查台账定位文件和函数，不要满仓库翻。

## 3. 验证规范

- 验收命令以设计文档为准，不许自己发明新命令或新测试框架；每段一个 `tests/test_p*.py`。
- 绝不为了让测试通过而修改断言。断言不满足就改代码。
- 失败先分清是环境问题还是代码问题；缺依赖就去装，环境问题也算自己的。
- 同一段连续两次同样的失败，说明方案错了：换一个方案，不叠加补丁。
- 不顺手修无关 bug，发现了记进台账「已知问题」。
- ponytail 要求的「可运行自检」在本仓库落在 `tests/test_p<段>.py`（pytest），不在 `backend/app/` 里加 `demo()` / `if __name__ == "__main__"`。

## 4. 提交与推送

- 时机：第 0 节第 4 步全绿之后才允许提交。没跑通的东西不提交。
- 一次提交只包含一个阶段，不混提交。
- commit 格式：`P1 数据摄入：验收 test_p1_ingest 通过`
- 提交前确认没把密钥、`data/`、数据库文件加进暂存区（`.gitignore` 已挡，抽查一次）。
- 远端未配置或推送失败：不阻塞开发，把状态写进台账「待推送记录」，下一段开工前先补推。
- 不 force push、不改写历史、不动别人的分支。
- 文档与工具类改动用 `docs: <说明>`；一次提交只做一类改动，不与阶段提交混在一起。

## 5. 阶段隔离

- 扩展已完成的系统只允许做三件事：新建文件、注册表加一行、编排层插调用。
- 禁止修改已验收阶段的函数体；确实要改契约，先回设计文档登记并同步改验收命令。
- 禁止下层 import 上层；`providers/*` 只被 `registry.py` 惰性 import。
- 禁止为「以后可能用到」提前加抽象层、配置项、目录。

## 6. 交接触发与流程

触发条件（命中任意一条，AI 必须主动提醒交接，不等用户问）：

提醒不等于自己开跑：命中后先提醒一句「该交接了 + 理由」，等用户点头再走下面六步；用户显式说「收尾 / 交接 / 体检 / 检查对齐 / 压缩文档」时直接开跑（全局 skill session-protocol 与此同规则）。

1. 一个阶段验收通过、准备进入下一阶段时
2. 同一会话里已经完成 2 个及以上阶段
3. 需要修改已冻结的契约（签名、端点路径、表名、配置键）时
4. 连续两次回读同一份文档才能继续工作（说明上下文已经吃紧）
5. 话题从「施工」切换到「设计 / 重构 / 排期」
6. 设了目标预算且使用超过 70% 时

交接流程（六步，缺一步不算交接完）：

1. 调用 skill `handoff`，生成 `docs/交接-YYYY-MM-DD.md`，固定四块内容：
   - 已完成阶段与对应提交号
   - 未完成、已知损坏或有坑的地方
   - 下一步要做的第一件事（精确到文件与命令）
   - 需要人拍板的问题
2. 更新 `docs/代码台账.md`
3. 该提交的东西全部提交（工作区不留未提交改动）
4. 提交并推送
5. 告诉用户：新会话第一句话发什么
6. 新会话开场固定用这句：
   「读 docs/索引.md、docs/代码台账.md 和最新的 docs/交接-*.md，回答当前状态与下一步，读完之前不要动代码」

换会话不丢状态靠这四样：`AGENTS.md` 自动加载、`docs/索引.md` 给文档地图、`docs/代码台账.md` 给已完成文件与已知问题、交接文档补对话级细节（决定、坑、待定问题）。
